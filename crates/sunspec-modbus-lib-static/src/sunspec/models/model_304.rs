// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_304::*;

#[repr(C)]
pub struct Model304CallbackAdapter {
    context: *mut c_void,
    /// X (Inclx)
    ///
    /// X-Axis inclination
    incl_x_callback: extern "C" fn(*const c_void) -> i32,
    /// Y (Incly)
    ///
    /// Y-Axis inclination
    incl_y_callback: Option<extern "C" fn(*const c_void) -> i32>,
    /// Z (Inclz)
    ///
    /// Z-Axis inclination
    incl_z_callback: Option<extern "C" fn(*const c_void) -> i32>,
}

impl ReadAdapter for Model304CallbackAdapter {
    fn incl_x(&self) -> i32 {
        (self.incl_x_callback)(self.context)
    }

    fn incl_y(&self) -> Option<i32> {
        self.incl_y_callback
            .map(|callback| (callback)(self.context))
    }

    fn incl_z(&self) -> Option<i32> {
        self.incl_z_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 304. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_304: StaticModelSpec = StaticModelSpec {
    id: 304,
    length: model_304_c_length,
    writable: false,
    visit_read: model_304_c_visit_read,
    visit_write: model_304_c_visit_write,
};

fn model_304_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model304).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model304CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_304_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model304;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model304CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_304_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_304_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model304;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 304 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_304_callback(
    adapter: *mut Model304CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_304,
        adapter: adapter as *mut c_void,
    }
}
