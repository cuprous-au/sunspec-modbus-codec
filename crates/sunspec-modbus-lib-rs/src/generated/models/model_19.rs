// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model
// definitions in https://github.com/sunspec/models. Do not edit by hand - run
// `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::{CStr, c_char, c_void};

static POINTS: [PointDetails<()>; 12] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Name,
        size: 4,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::Rate,
        size: 2,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Bits,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::Parity,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::Duplex,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::FlowControl,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::Authentication,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::Username,
        size: 12,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::Password,
        size: 6,
        start_address: 25,
    },
    PointDetails {
        point: |()| Point::Pad,
        size: 1,
        start_address: 31,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Name,
    Rate,
    Bits,
    Parity,
    Duplex,
    FlowControl,
    Authentication,
    Username,
    Password,
    Pad,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 19. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model19;

impl<'ad> ModelSpec<'ad> for Model19 {
    const MODEL_ID: u16 = 19;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        32
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model19,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(19);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Name => {
            if let Some(value) = adapter.name() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Rate => {
            buffer.write_u32(adapter.rate(), offset);
        }
        Point::Bits => {
            buffer.write_u16(adapter.bits());
        }
        Point::Parity => {
            buffer.write_u16(adapter.parity() as u16);
        }
        Point::Duplex => {
            if let Some(value) = adapter.duplex() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::FlowControl => {
            if let Some(value) = adapter.flow_control() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::Authentication => {
            if let Some(value) = adapter.authentication() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::Username => {
            if let Some(value) = adapter.username() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Password => {
            if let Some(value) = adapter.password() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Pad => {
            buffer.write_u16(0);
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model19,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::Name => {
            let value = buffer.read_string::<8>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_name(str_value);
            Ok(())
        }
        Point::Rate => {
            let value = buffer.read_u32()?;
            adapter.set_rate(value);
            Ok(())
        }
        Point::Bits => {
            let value = buffer.read_u16()?;
            adapter.set_bits(value);
            Ok(())
        }
        Point::Parity => {
            let value = buffer.read_u16()?;
            let enum_value = Pty::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_parity(enum_value);
            Ok(())
        }
        Point::Duplex => {
            let value = buffer.read_u16()?;
            let enum_value = Dup::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_duplex(enum_value);
            Ok(())
        }
        Point::FlowControl => {
            let value = buffer.read_u16()?;
            let enum_value = Flw::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_flow_control(enum_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    fn name(&self) -> Option<&CStr> {
        None
    }

    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    fn rate(&self) -> u32;

    /// Bits (Bits)
    ///
    /// Number of data bits per character
    fn bits(&self) -> u16;

    /// Parity (Pty)
    ///
    /// Parity setting
    fn parity(&self) -> Pty;

    /// Duplex (Dup)
    ///
    /// Duplex mode
    fn duplex(&self) -> Option<Dup> {
        None
    }

    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    fn flow_control(&self) -> Option<Flw> {
        None
    }

    /// Authentication (Auth)
    ///
    /// Authentication method
    fn authentication(&self) -> Option<Auth> {
        None
    }

    /// Username (UsrNam)
    ///
    /// Username for authentication
    fn username(&self) -> Option<&CStr> {
        None
    }

    /// Password (Pw)
    ///
    /// Password for authentication
    fn password(&self) -> Option<&CStr> {
        None
    }
}

pub trait WriteAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    fn set_name(&mut self, value: &CStr) {}

    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    fn set_rate(&mut self, value: u32);

    /// Bits (Bits)
    ///
    /// Number of data bits per character
    fn set_bits(&mut self, value: u16);

    /// Parity (Pty)
    ///
    /// Parity setting
    fn set_parity(&mut self, value: Pty);

    /// Duplex (Dup)
    ///
    /// Duplex mode
    fn set_duplex(&mut self, value: Dup) {}

    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    fn set_flow_control(&mut self, value: Flw) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model19Auth {
    None = 0,
    Pap = 1,
    Chap = 2,
}

impl Model19Auth {
    pub fn from_repr(repr: u16) -> Option<Model19Auth> {
        match repr {
            0 => Some(Model19Auth::None),
            1 => Some(Model19Auth::Pap),
            2 => Some(Model19Auth::Chap),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model19Auth`] - only `Model19Auth` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Auth` distinct from another model's point of the same name.
pub type Auth = Model19Auth;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model19Dup {
    Full = 0,
    Half = 1,
}

impl Model19Dup {
    pub fn from_repr(repr: u16) -> Option<Model19Dup> {
        match repr {
            0 => Some(Model19Dup::Full),
            1 => Some(Model19Dup::Half),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model19Dup`] - only `Model19Dup` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Dup` distinct from another model's point of the same name.
pub type Dup = Model19Dup;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model19Flw {
    None = 0,
    Hw = 1,
    Xonxoff = 2,
}

impl Model19Flw {
    pub fn from_repr(repr: u16) -> Option<Model19Flw> {
        match repr {
            0 => Some(Model19Flw::None),
            1 => Some(Model19Flw::Hw),
            2 => Some(Model19Flw::Xonxoff),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model19Flw`] - only `Model19Flw` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Flw` distinct from another model's point of the same name.
pub type Flw = Model19Flw;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model19Pty {
    None = 0,
    Odd = 1,
    Even = 2,
}

impl Model19Pty {
    pub fn from_repr(repr: u16) -> Option<Model19Pty> {
        match repr {
            0 => Some(Model19Pty::None),
            1 => Some(Model19Pty::Odd),
            2 => Some(Model19Pty::Even),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model19Pty`] - only `Model19Pty` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Pty` distinct from another model's point of the same name.
pub type Pty = Model19Pty;

#[repr(C)]
pub struct Model19CallbackAdapter {
    context: *mut c_void,
    /// Name (Nam)
    ///
    /// Interface name
    name_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Name (Nam)
    ///
    /// Interface name
    set_name_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    rate_callback: extern "C" fn(*const c_void) -> u32,
    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    set_rate_callback: extern "C" fn(u32, *mut c_void),
    /// Bits (Bits)
    ///
    /// Number of data bits per character
    bits_callback: extern "C" fn(*const c_void) -> u16,
    /// Bits (Bits)
    ///
    /// Number of data bits per character
    set_bits_callback: extern "C" fn(u16, *mut c_void),
    /// Parity (Pty)
    ///
    /// Parity setting
    parity_callback: extern "C" fn(*const c_void) -> Model19Pty,
    /// Parity (Pty)
    ///
    /// Parity setting
    set_parity_callback: extern "C" fn(Model19Pty, *mut c_void),
    /// Duplex (Dup)
    ///
    /// Duplex mode
    duplex_callback: Option<extern "C" fn(*const c_void) -> Model19Dup>,
    /// Duplex (Dup)
    ///
    /// Duplex mode
    set_duplex_callback: Option<extern "C" fn(Model19Dup, *mut c_void)>,
    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    flow_control_callback: Option<extern "C" fn(*const c_void) -> Model19Flw>,
    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    set_flow_control_callback: Option<extern "C" fn(Model19Flw, *mut c_void)>,
    /// Authentication (Auth)
    ///
    /// Authentication method
    authentication_callback: Option<extern "C" fn(*const c_void) -> Model19Auth>,
    /// Username (UsrNam)
    ///
    /// Username for authentication
    username_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Password (Pw)
    ///
    /// Password for authentication
    password_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
}

impl ReadAdapter for Model19CallbackAdapter {
    fn name(&self) -> Option<&CStr> {
        self.name_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn rate(&self) -> u32 {
        (self.rate_callback)(self.context)
    }

    fn bits(&self) -> u16 {
        (self.bits_callback)(self.context)
    }

    fn parity(&self) -> Pty {
        (self.parity_callback)(self.context)
    }

    fn duplex(&self) -> Option<Dup> {
        self.duplex_callback
            .map(|callback| (callback)(self.context))
    }

    fn flow_control(&self) -> Option<Flw> {
        self.flow_control_callback
            .map(|callback| (callback)(self.context))
    }

    fn authentication(&self) -> Option<Auth> {
        self.authentication_callback
            .map(|callback| (callback)(self.context))
    }

    fn username(&self) -> Option<&CStr> {
        self.username_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn password(&self) -> Option<&CStr> {
        self.password_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }
}

impl WriteAdapter for Model19CallbackAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    fn set_name(&mut self, value: &CStr) {
        if let Some(callback) = self.set_name_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }

    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    fn set_rate(&mut self, value: u32) {
        (self.set_rate_callback)(value, self.context);
    }

    /// Bits (Bits)
    ///
    /// Number of data bits per character
    fn set_bits(&mut self, value: u16) {
        (self.set_bits_callback)(value, self.context);
    }

    /// Parity (Pty)
    ///
    /// Parity setting
    fn set_parity(&mut self, value: Pty) {
        (self.set_parity_callback)(value, self.context);
    }

    /// Duplex (Dup)
    ///
    /// Duplex mode
    fn set_duplex(&mut self, value: Dup) {
        if let Some(callback) = self.set_duplex_callback {
            (callback)(value, self.context);
        };
    }

    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    fn set_flow_control(&mut self, value: Flw) {
        if let Some(callback) = self.set_flow_control_callback {
            (callback)(value, self.context);
        };
    }
}

#[repr(C)]
pub struct Model19StatefulAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    pub name: [c_char; 8],
    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    pub rate: u32,
    /// Bits (Bits)
    ///
    /// Number of data bits per character
    pub bits: u16,
    /// Parity (Pty)
    ///
    /// Parity setting
    pub parity: Model19Pty,
    /// Duplex (Dup)
    ///
    /// Duplex mode
    pub duplex: Model19Dup,
    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    pub flow_control: Model19Flw,
    /// Authentication (Auth)
    ///
    /// Authentication method
    pub authentication: Model19Auth,
    /// Username (UsrNam)
    ///
    /// Username for authentication
    pub username: [c_char; 24],
    /// Password (Pw)
    ///
    /// Password for authentication
    pub password: [c_char; 12],
}

impl ReadAdapter for Model19StatefulAdapter {
    fn name(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.name.as_ptr()) })
    }

    fn rate(&self) -> u32 {
        self.rate
    }

    fn bits(&self) -> u16 {
        self.bits
    }

    fn parity(&self) -> Pty {
        self.parity
    }

    fn duplex(&self) -> Option<Dup> {
        Some(self.duplex)
    }

    fn flow_control(&self) -> Option<Flw> {
        Some(self.flow_control)
    }

    fn authentication(&self) -> Option<Auth> {
        Some(self.authentication)
    }

    fn username(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.username.as_ptr()) })
    }

    fn password(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.password.as_ptr()) })
    }
}

impl WriteAdapter for Model19StatefulAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    fn set_name(&mut self, value: &CStr) {
        for (dest, src) in self.name.iter_mut().zip(value.to_bytes_with_nul().iter()) {
            *dest = *src as c_char;
        }
    }

    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    fn set_rate(&mut self, value: u32) {
        self.rate = value;
    }

    /// Bits (Bits)
    ///
    /// Number of data bits per character
    fn set_bits(&mut self, value: u16) {
        self.bits = value;
    }

    /// Parity (Pty)
    ///
    /// Parity setting
    fn set_parity(&mut self, value: Pty) {
        self.parity = value;
    }

    /// Duplex (Dup)
    ///
    /// Duplex mode
    fn set_duplex(&mut self, value: Dup) {
        self.duplex = value;
    }

    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    fn set_flow_control(&mut self, value: Flw) {
        self.flow_control = value;
    }
}

/// C-FFI dispatch descriptor for SunSpec model 19. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_19: StaticModelSpec = StaticModelSpec {
    id: 19,
    length: model_19_c_length,
    writable: true,
    visit_read: model_19_c_visit_read,
    visit_write: model_19_c_visit_write,
};

fn model_19_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model19).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model19{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_19_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model19;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model19StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model19CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_19_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_19_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model19;
    let adapter: Option<&mut dyn WriteAdapter> = match kind {
        1 => {
            Some(unsafe { &mut *(adapter as *mut Model19StatefulAdapter) } as &mut dyn WriteAdapter)
        }
        2 => {
            Some(unsafe { &mut *(adapter as *mut Model19CallbackAdapter) } as &mut dyn WriteAdapter)
        }
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}
