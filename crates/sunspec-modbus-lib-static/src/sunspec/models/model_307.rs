// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_307::*;

#[repr(C)]
pub struct Model307CallbackAdapter {
    context: *mut c_void,
    /// Ambient Temperature (TmpAmb)
    ambient_temperature_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Relative Humidity (RH)
    relative_humidity_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Barometric Pressure (Pres)
    barometric_pressure_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Wind Speed (WndSpd)
    wind_speed_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Wind Direction (WndDir)
    wind_direction_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Rainfall (Rain)
    rainfall_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Snow Depth (Snw)
    snow_depth_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Precipitation Type (PPT)
    ///
    /// Precipitation Type (WMO 4680 SYNOP code reference)
    precipitation_type_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Electric Field (ElecFld)
    electric_field_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Surface Wetness (SurWet)
    surface_wetness_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Soil Wetness (SoilWet)
    soil_wetness_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model307CallbackAdapter {
    fn ambient_temperature(&self) -> Option<i16> {
        self.ambient_temperature_callback
            .map(|callback| (callback)(self.context))
    }

    fn relative_humidity(&self) -> Option<i16> {
        self.relative_humidity_callback
            .map(|callback| (callback)(self.context))
    }

    fn barometric_pressure(&self) -> Option<i16> {
        self.barometric_pressure_callback
            .map(|callback| (callback)(self.context))
    }

    fn wind_speed(&self) -> Option<i16> {
        self.wind_speed_callback
            .map(|callback| (callback)(self.context))
    }

    fn wind_direction(&self) -> Option<i16> {
        self.wind_direction_callback
            .map(|callback| (callback)(self.context))
    }

    fn rainfall(&self) -> Option<i16> {
        self.rainfall_callback
            .map(|callback| (callback)(self.context))
    }

    fn snow_depth(&self) -> Option<i16> {
        self.snow_depth_callback
            .map(|callback| (callback)(self.context))
    }

    fn precipitation_type(&self) -> Option<i16> {
        self.precipitation_type_callback
            .map(|callback| (callback)(self.context))
    }

    fn electric_field(&self) -> Option<i16> {
        self.electric_field_callback
            .map(|callback| (callback)(self.context))
    }

    fn surface_wetness(&self) -> Option<i16> {
        self.surface_wetness_callback
            .map(|callback| (callback)(self.context))
    }

    fn soil_wetness(&self) -> Option<i16> {
        self.soil_wetness_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 307. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_307: StaticModelSpec = StaticModelSpec {
    id: 307,
    length: model_307_c_length,
    writable: false,
    visit_read: model_307_c_visit_read,
    visit_write: model_307_c_visit_write,
};

fn model_307_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model307).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model307CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_307_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model307;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model307CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_307_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_307_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model307;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 307 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_307_callback(
    adapter: *mut Model307CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_307,
        adapter: adapter as *mut c_void,
    }
}
