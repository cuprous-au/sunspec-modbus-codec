// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_713::*;

#[repr(C)]
pub struct Model713CallbackAdapter {
    context: *mut c_void,
    /// Energy Rating (WHRtg)
    ///
    /// Energy rating of the DER storage.
    energy_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Energy Available (WHAvail)
    ///
    /// Energy available of the DER storage (WHAvail = WHRtg * SoC * SoH)
    energy_available_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// State of Charge (SoC)
    ///
    /// State of charge of the DER storage.
    ///
    /// SOC shall be fixed to 0% for DER without storage capabilities.
    state_of_charge_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// State of Health (SoH)
    ///
    /// State of health of the DER storage.
    state_of_health_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Status (Sta)
    ///
    /// Storage status.
    status_callback: Option<extern "C" fn(*const c_void) -> Model713Sta>,
    /// Energy Scale Factor (WH_SF)
    ///
    /// Scale factor for energy capacity.
    energy_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Percent Scale Factor (Pct_SF)
    ///
    /// Scale factor for percentage.
    percent_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model713CallbackAdapter {
    fn energy_rating(&self) -> Option<u16> {
        self.energy_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn energy_available(&self) -> Option<u16> {
        self.energy_available_callback
            .map(|callback| (callback)(self.context))
    }

    fn state_of_charge(&self) -> Option<u16> {
        self.state_of_charge_callback
            .map(|callback| (callback)(self.context))
    }

    fn state_of_health(&self) -> Option<u16> {
        self.state_of_health_callback
            .map(|callback| (callback)(self.context))
    }

    fn status(&self) -> Option<Sta> {
        self.status_callback
            .map(|callback| (callback)(self.context))
    }

    fn energy_scale_factor(&self) -> Option<i16> {
        self.energy_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn percent_scale_factor(&self) -> Option<i16> {
        self.percent_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 713. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_713: StaticModelSpec = StaticModelSpec {
    id: 713,
    length: model_713_c_length,
    writable: false,
    visit_read: model_713_c_visit_read,
    visit_write: model_713_c_visit_write,
};

fn model_713_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model713).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model713CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_713_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model713;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model713CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_713_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_713_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model713;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 713 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_713_callback(
    adapter: *mut Model713CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_713,
        adapter: adapter as *mut c_void,
    }
}
