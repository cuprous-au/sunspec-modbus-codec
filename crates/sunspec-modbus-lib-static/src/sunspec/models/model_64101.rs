// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_64101::*;

#[repr(C)]
pub struct Model64101CallbackAdapter {
    context: *mut c_void,
    /// Eltek_Country_Code
    eltek_country_code_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_Feeding_Phase
    eltek_feeding_phase_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_APD_Method
    eltek_apd_method_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_APD_Power_Ref
    eltek_apd_power_ref_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_Method
    eltek_rps_method_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_Q_Ref
    eltek_rps_q_ref_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_CosPhi_Ref
    eltek_rps_cos_phi_ref_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model64101CallbackAdapter {
    fn eltek_country_code(&self) -> Option<u16> {
        self.eltek_country_code_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_feeding_phase(&self) -> Option<u16> {
        self.eltek_feeding_phase_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_apd_method(&self) -> Option<u16> {
        self.eltek_apd_method_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_apd_power_ref(&self) -> Option<u16> {
        self.eltek_apd_power_ref_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_method(&self) -> Option<u16> {
        self.eltek_rps_method_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_q_ref(&self) -> Option<u16> {
        self.eltek_rps_q_ref_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_cos_phi_ref(&self) -> Option<i16> {
        self.eltek_rps_cos_phi_ref_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 64101. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_64101: StaticModelSpec = StaticModelSpec {
    id: 64101,
    length: model_64101_c_length,
    writable: false,
    visit_read: model_64101_c_visit_read,
    visit_write: model_64101_c_visit_write,
};

fn model_64101_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model64101).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model64101CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_64101_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model64101;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model64101CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_64101_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_64101_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model64101;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 64101 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_64101_callback(
    adapter: *mut Model64101CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_64101,
        adapter: adapter as *mut c_void,
    }
}
