// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::{CStr, c_char, c_void};
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_305::*;

#[repr(C)]
pub struct Model305CallbackAdapter {
    context: *mut c_void,
    /// Tm (Tm)
    ///
    /// UTC 24 hour time stamp to millisecond hhmmss.sssZ format
    tm_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Date (Date)
    ///
    /// UTC Date string YYYYMMDD format
    date_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Location (Loc)
    ///
    /// Location string (40 chars max)
    location_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Lat (Lat)
    ///
    /// Latitude with seven degrees of precision
    lat_callback: Option<extern "C" fn(*const c_void) -> i32>,
    /// Long (Long)
    ///
    /// Longitude with seven degrees of precision
    long_callback: Option<extern "C" fn(*const c_void) -> i32>,
    /// Altitude (Alt)
    ///
    /// Altitude measurement in meters
    altitude_callback: Option<extern "C" fn(*const c_void) -> i32>,
}

impl ReadAdapter for Model305CallbackAdapter {
    fn tm(&self) -> Option<&CStr> {
        self.tm_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn date(&self) -> Option<&CStr> {
        self.date_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn location(&self) -> Option<&CStr> {
        self.location_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn lat(&self) -> Option<i32> {
        self.lat_callback.map(|callback| (callback)(self.context))
    }

    fn long(&self) -> Option<i32> {
        self.long_callback.map(|callback| (callback)(self.context))
    }

    fn altitude(&self) -> Option<i32> {
        self.altitude_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 305. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_305: StaticModelSpec = StaticModelSpec {
    id: 305,
    length: model_305_c_length,
    writable: false,
    visit_read: model_305_c_visit_read,
    visit_write: model_305_c_visit_write,
};

fn model_305_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model305).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model305CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_305_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model305;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model305CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_305_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_305_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model305;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 305 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_305_callback(
    adapter: *mut Model305CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_305,
        adapter: adapter as *mut c_void,
    }
}
