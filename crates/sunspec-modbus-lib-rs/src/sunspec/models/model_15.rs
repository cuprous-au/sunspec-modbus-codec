// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 15] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Clear,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::InputCount,
        size: 2,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::InputUnicastCount,
        size: 2,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::InputNonUnicastCount,
        size: 2,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::InputDiscardedCount,
        size: 2,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::InputErrorCount,
        size: 2,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::InputUnknownCount,
        size: 2,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::OutputCount,
        size: 2,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::OutputUnicastCount,
        size: 2,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::OutputNonUnicastCount,
        size: 2,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::OutputDiscardedCount,
        size: 2,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::OutputErrorCount,
        size: 2,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::Pad,
        size: 1,
        start_address: 25,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Clear,
    InputCount,
    InputUnicastCount,
    InputNonUnicastCount,
    InputDiscardedCount,
    InputErrorCount,
    InputUnknownCount,
    OutputCount,
    OutputUnicastCount,
    OutputNonUnicastCount,
    OutputDiscardedCount,
    OutputErrorCount,
    Pad,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 15. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model15;

impl<'ad> ModelSpec<'ad> for Model15 {
    const MODEL_ID: u16 = 15;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        26
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model15,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(15);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Clear => {
            if let Some(value) = adapter.clear() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InputCount => {
            if let Some(value) = adapter.input_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputUnicastCount => {
            if let Some(value) = adapter.input_unicast_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputNonUnicastCount => {
            if let Some(value) = adapter.input_non_unicast_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputDiscardedCount => {
            if let Some(value) = adapter.input_discarded_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputErrorCount => {
            if let Some(value) = adapter.input_error_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputUnknownCount => {
            if let Some(value) = adapter.input_unknown_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputCount => {
            if let Some(value) = adapter.output_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputUnicastCount => {
            if let Some(value) = adapter.output_unicast_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputNonUnicastCount => {
            if let Some(value) = adapter.output_non_unicast_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputDiscardedCount => {
            if let Some(value) = adapter.output_discarded_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputErrorCount => {
            if let Some(value) = adapter.output_error_count() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Pad => {
            buffer.write_u16(0);
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model15,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::Clear => {
            let value = buffer.read_u16()?;
            adapter.set_clear(value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Clear (Clr)
    ///
    /// Write a "1" to clear all counters
    fn clear(&self) -> Option<u16> {
        None
    }

    /// Input Count (InCnt)
    ///
    /// Number of bytes received
    fn input_count(&self) -> Option<u32> {
        None
    }

    /// Input Unicast Count (InUcCnt)
    ///
    /// Number of Unicast packets received
    fn input_unicast_count(&self) -> Option<u32> {
        None
    }

    /// Input Non-Unicast Count (InNUcCnt)
    ///
    /// Number of non-Unicast packets received
    fn input_non_unicast_count(&self) -> Option<u32> {
        None
    }

    /// Input Discarded Count (InDscCnt)
    ///
    /// Number of inbound packets received on the interface but discarded
    fn input_discarded_count(&self) -> Option<u32> {
        None
    }

    /// Input Error Count (InErrCnt)
    ///
    /// Number of inbound packets that contain errors (excluding discards)
    fn input_error_count(&self) -> Option<u32> {
        None
    }

    /// Input Unknown Count (InUnkCnt)
    ///
    /// Number of inbound packets with unknown protocol
    fn input_unknown_count(&self) -> Option<u32> {
        None
    }

    /// Output Count (OutCnt)
    ///
    /// Total number of bytes transmitted on this interface
    fn output_count(&self) -> Option<u32> {
        None
    }

    /// Output Unicast Count (OutUcCnt)
    ///
    /// Number of Unicast packets transmitted
    fn output_unicast_count(&self) -> Option<u32> {
        None
    }

    /// Output Non-Unicast Count (OutNUcCnt)
    ///
    /// Number of Non-Unicast packets transmitted
    fn output_non_unicast_count(&self) -> Option<u32> {
        None
    }

    /// Output Discarded Count (OutDscCnt)
    ///
    /// Number of Discarded output packets
    fn output_discarded_count(&self) -> Option<u32> {
        None
    }

    /// Output Error Count (OutErrCnt)
    ///
    /// Number of outbound error packets
    fn output_error_count(&self) -> Option<u32> {
        None
    }
}

pub trait WriteAdapter {
    /// Clear (Clr)
    ///
    /// Write a "1" to clear all counters
    fn set_clear(&mut self, value: u16) {}
}
