// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 18] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::DcaSf,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::DcAhrSf,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::DcvSf,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::DcwSf,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::DcWhSf,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Rating,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::N,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::Event,
        size: 2,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::VendorEvent,
        size: 2,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::Amps,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::AmpHours,
        size: 2,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::Voltage,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::Temp,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::Watts,
        size: 1,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::Pr,
        size: 1,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::WattHours,
        size: 2,
        start_address: 20,
    },
];

static STRING_POINTS: [PointDetails<()>; 10] = [
    PointDetails {
        point: |()| Point::StringId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::StringInputEvent,
        size: 2,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::StringVendorEvent,
        size: 2,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::StringAmps,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::StringAmpHours,
        size: 2,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::StringVoltage,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::StringWatts,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::StringWattHours,
        size: 2,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::StringPr,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::StringN,
        size: 1,
        start_address: 13,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    DcaSf,
    DcAhrSf,
    DcvSf,
    DcwSf,
    DcWhSf,
    Rating,
    N,
    Event,
    VendorEvent,
    Amps,
    AmpHours,
    Voltage,
    Temp,
    Watts,
    Pr,
    WattHours,
    StringId,
    StringInputEvent,
    StringVendorEvent,
    StringAmps,
    StringAmpHours,
    StringVoltage,
    StringWatts,
    StringWattHours,
    StringPr,
    StringN,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 402. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model402;

impl<'ad> ModelSpec<'ad> for Model402 {
    const MODEL_ID: u16 = 402;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        36
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                STRING_POINTS
                    .iter()
                    .map(move |p| (22 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                STRING_POINTS
                    .iter()
                    .map(move |p| (22 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model402,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(402);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::DcaSf => {
            buffer.write_i16(adapter.dca_sf());
        }
        Point::DcAhrSf => {
            if let Some(value) = adapter.dc_ahr_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DcvSf => {
            if let Some(value) = adapter.dcv_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DcwSf => {
            if let Some(value) = adapter.dcw_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DcWhSf => {
            buffer.write_i16(adapter.dc_wh_sf());
        }
        Point::Rating => {
            if let Some(value) = adapter.rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::N => {
            if let Some(value) = adapter.n() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Event => {
            buffer.write_u32(adapter.event(), offset);
        }
        Point::VendorEvent => {
            if let Some(value) = adapter.vendor_event() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Amps => {
            buffer.write_i16(adapter.amps());
        }
        Point::AmpHours => {
            if let Some(value) = adapter.amp_hours() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Voltage => {
            if let Some(value) = adapter.voltage() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Temp => {
            if let Some(value) = adapter.temp() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Watts => {
            if let Some(value) = adapter.watts() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Pr => {
            if let Some(value) = adapter.pr() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WattHours => {
            buffer.write_u32(adapter.watt_hours(), offset);
        }
        Point::StringId => {
            buffer.write_u16(adapter.string_id());
        }
        Point::StringInputEvent => {
            buffer.write_u32(adapter.string_input_event(), offset);
        }
        Point::StringVendorEvent => {
            if let Some(value) = adapter.string_vendor_event() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::StringAmps => {
            buffer.write_i16(adapter.string_amps());
        }
        Point::StringAmpHours => {
            if let Some(value) = adapter.string_amp_hours() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::StringVoltage => {
            if let Some(value) = adapter.string_voltage() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StringWatts => {
            if let Some(value) = adapter.string_watts() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StringWattHours => {
            if let Some(value) = adapter.string_watt_hours() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::StringPr => {
            if let Some(value) = adapter.string_pr() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StringN => {
            if let Some(value) = adapter.string_n() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model402,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// DCA_SF
    ///
    /// Current scale factor
    fn dca_sf(&self) -> i16;

    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    fn dc_ahr_sf(&self) -> Option<i16> {
        None
    }

    /// DCV_SF
    ///
    /// Voltage scale factor
    fn dcv_sf(&self) -> Option<i16> {
        None
    }

    /// DCW_SF
    ///
    /// Power scale factor
    fn dcw_sf(&self) -> Option<i16> {
        None
    }

    /// DCWh_SF
    ///
    /// Energy scale factor
    fn dc_wh_sf(&self) -> i16;

    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    fn rating(&self) -> Option<u16> {
        None
    }

    /// N (N)
    ///
    /// Number of Inputs
    fn n(&self) -> Option<u16> {
        None
    }

    /// Event (Evt)
    ///
    /// Events
    fn event(&self) -> u32;

    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    fn vendor_event(&self) -> Option<u32> {
        None
    }

    /// Amps (DCA)
    ///
    /// Total measured current
    fn amps(&self) -> i16;

    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    fn amp_hours(&self) -> Option<u32> {
        None
    }

    /// Voltage (DCV)
    ///
    /// Output Voltage
    fn voltage(&self) -> Option<u16> {
        None
    }

    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    fn temp(&self) -> Option<i16> {
        None
    }

    /// Watts (DCW)
    ///
    /// Output power
    fn watts(&self) -> Option<i16> {
        None
    }

    /// PR (DCPR)
    ///
    /// DC Performance ratio value
    fn pr(&self) -> Option<u16> {
        None
    }

    /// Watt-hours (DCWh)
    ///
    /// Output energy
    fn watt_hours(&self) -> u32;

    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    fn string_id(&self) -> u16;

    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    fn string_input_event(&self) -> u32;

    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    fn string_vendor_event(&self) -> Option<u32> {
        None
    }

    /// Amps (InDCA)
    ///
    /// String Input Current
    fn string_amps(&self) -> i16;

    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    fn string_amp_hours(&self) -> Option<u32> {
        None
    }

    /// Voltage (InDCV)
    ///
    /// String Input Voltage
    fn string_voltage(&self) -> Option<u16> {
        None
    }

    /// Watts (InDCW)
    ///
    /// String Input Power
    fn string_watts(&self) -> Option<i16> {
        None
    }

    /// Watt-hours (InDCWh)
    ///
    /// String Input Energy
    fn string_watt_hours(&self) -> Option<u32> {
        None
    }

    /// PR (InDCPR)
    ///
    /// String Performance Ratio
    fn string_pr(&self) -> Option<u16> {
        None
    }

    /// N (InN)
    ///
    /// Number of modules in this input string
    fn string_n(&self) -> Option<u16> {
        None
    }
}

pub trait WriteAdapter {}

#[repr(C)]
pub struct Model402CallbackAdapter {
    context: *mut c_void,
    /// DCA_SF
    ///
    /// Current scale factor
    dca_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    dc_ahr_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// DCV_SF
    ///
    /// Voltage scale factor
    dcv_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// DCW_SF
    ///
    /// Power scale factor
    dcw_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// DCWh_SF
    ///
    /// Energy scale factor
    dc_wh_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// N (N)
    ///
    /// Number of Inputs
    n_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Event (Evt)
    ///
    /// Events
    event_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    vendor_event_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Amps (DCA)
    ///
    /// Total measured current
    amps_callback: extern "C" fn(*const c_void) -> i16,
    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    amp_hours_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Voltage (DCV)
    ///
    /// Output Voltage
    voltage_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    temp_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Watts (DCW)
    ///
    /// Output power
    watts_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// PR (DCPR)
    ///
    /// DC Performance ratio value
    pr_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Watt-hours (DCWh)
    ///
    /// Output energy
    watt_hours_callback: extern "C" fn(*const c_void) -> u32,
    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    string_id_callback: extern "C" fn(*const c_void) -> u16,
    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    string_input_event_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    string_vendor_event_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Amps (InDCA)
    ///
    /// String Input Current
    string_amps_callback: extern "C" fn(*const c_void) -> i16,
    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    string_amp_hours_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Voltage (InDCV)
    ///
    /// String Input Voltage
    string_voltage_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Watts (InDCW)
    ///
    /// String Input Power
    string_watts_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Watt-hours (InDCWh)
    ///
    /// String Input Energy
    string_watt_hours_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// PR (InDCPR)
    ///
    /// String Performance Ratio
    string_pr_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// N (InN)
    ///
    /// Number of modules in this input string
    string_n_callback: Option<extern "C" fn(*const c_void) -> u16>,
}

impl ReadAdapter for Model402CallbackAdapter {
    fn dca_sf(&self) -> i16 {
        (self.dca_sf_callback)(self.context)
    }

    fn dc_ahr_sf(&self) -> Option<i16> {
        self.dc_ahr_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn dcv_sf(&self) -> Option<i16> {
        self.dcv_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn dcw_sf(&self) -> Option<i16> {
        self.dcw_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn dc_wh_sf(&self) -> i16 {
        (self.dc_wh_sf_callback)(self.context)
    }

    fn rating(&self) -> Option<u16> {
        self.rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn n(&self) -> Option<u16> {
        self.n_callback.map(|callback| (callback)(self.context))
    }

    fn event(&self) -> u32 {
        (self.event_callback)(self.context)
    }

    fn vendor_event(&self) -> Option<u32> {
        self.vendor_event_callback
            .map(|callback| (callback)(self.context))
    }

    fn amps(&self) -> i16 {
        (self.amps_callback)(self.context)
    }

    fn amp_hours(&self) -> Option<u32> {
        self.amp_hours_callback
            .map(|callback| (callback)(self.context))
    }

    fn voltage(&self) -> Option<u16> {
        self.voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn temp(&self) -> Option<i16> {
        self.temp_callback.map(|callback| (callback)(self.context))
    }

    fn watts(&self) -> Option<i16> {
        self.watts_callback.map(|callback| (callback)(self.context))
    }

    fn pr(&self) -> Option<u16> {
        self.pr_callback.map(|callback| (callback)(self.context))
    }

    fn watt_hours(&self) -> u32 {
        (self.watt_hours_callback)(self.context)
    }

    fn string_id(&self) -> u16 {
        (self.string_id_callback)(self.context)
    }

    fn string_input_event(&self) -> u32 {
        (self.string_input_event_callback)(self.context)
    }

    fn string_vendor_event(&self) -> Option<u32> {
        self.string_vendor_event_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_amps(&self) -> i16 {
        (self.string_amps_callback)(self.context)
    }

    fn string_amp_hours(&self) -> Option<u32> {
        self.string_amp_hours_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_voltage(&self) -> Option<u16> {
        self.string_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_watts(&self) -> Option<i16> {
        self.string_watts_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_watt_hours(&self) -> Option<u32> {
        self.string_watt_hours_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_pr(&self) -> Option<u16> {
        self.string_pr_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_n(&self) -> Option<u16> {
        self.string_n_callback
            .map(|callback| (callback)(self.context))
    }
}

#[repr(C)]
pub struct Model402StatefulAdapter {
    /// DCA_SF
    ///
    /// Current scale factor
    pub dca_sf: i16,
    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    pub dc_ahr_sf: i16,
    /// DCV_SF
    ///
    /// Voltage scale factor
    pub dcv_sf: i16,
    /// DCW_SF
    ///
    /// Power scale factor
    pub dcw_sf: i16,
    /// DCWh_SF
    ///
    /// Energy scale factor
    pub dc_wh_sf: i16,
    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    pub rating: u16,
    /// N (N)
    ///
    /// Number of Inputs
    pub n: u16,
    /// Event (Evt)
    ///
    /// Events
    pub event: u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    pub vendor_event: u32,
    /// Amps (DCA)
    ///
    /// Total measured current
    pub amps: i16,
    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    pub amp_hours: u32,
    /// Voltage (DCV)
    ///
    /// Output Voltage
    pub voltage: u16,
    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    pub temp: i16,
    /// Watts (DCW)
    ///
    /// Output power
    pub watts: i16,
    /// PR (DCPR)
    ///
    /// DC Performance ratio value
    pub pr: u16,
    /// Watt-hours (DCWh)
    ///
    /// Output energy
    pub watt_hours: u32,
    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    pub string_id: u16,
    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    pub string_input_event: u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    pub string_vendor_event: u32,
    /// Amps (InDCA)
    ///
    /// String Input Current
    pub string_amps: i16,
    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    pub string_amp_hours: u32,
    /// Voltage (InDCV)
    ///
    /// String Input Voltage
    pub string_voltage: u16,
    /// Watts (InDCW)
    ///
    /// String Input Power
    pub string_watts: i16,
    /// Watt-hours (InDCWh)
    ///
    /// String Input Energy
    pub string_watt_hours: u32,
    /// PR (InDCPR)
    ///
    /// String Performance Ratio
    pub string_pr: u16,
    /// N (InN)
    ///
    /// Number of modules in this input string
    pub string_n: u16,
}

impl ReadAdapter for Model402StatefulAdapter {
    fn dca_sf(&self) -> i16 {
        self.dca_sf
    }

    fn dc_ahr_sf(&self) -> Option<i16> {
        Some(self.dc_ahr_sf)
    }

    fn dcv_sf(&self) -> Option<i16> {
        Some(self.dcv_sf)
    }

    fn dcw_sf(&self) -> Option<i16> {
        Some(self.dcw_sf)
    }

    fn dc_wh_sf(&self) -> i16 {
        self.dc_wh_sf
    }

    fn rating(&self) -> Option<u16> {
        Some(self.rating)
    }

    fn n(&self) -> Option<u16> {
        Some(self.n)
    }

    fn event(&self) -> u32 {
        self.event
    }

    fn vendor_event(&self) -> Option<u32> {
        Some(self.vendor_event)
    }

    fn amps(&self) -> i16 {
        self.amps
    }

    fn amp_hours(&self) -> Option<u32> {
        Some(self.amp_hours)
    }

    fn voltage(&self) -> Option<u16> {
        Some(self.voltage)
    }

    fn temp(&self) -> Option<i16> {
        Some(self.temp)
    }

    fn watts(&self) -> Option<i16> {
        Some(self.watts)
    }

    fn pr(&self) -> Option<u16> {
        Some(self.pr)
    }

    fn watt_hours(&self) -> u32 {
        self.watt_hours
    }

    fn string_id(&self) -> u16 {
        self.string_id
    }

    fn string_input_event(&self) -> u32 {
        self.string_input_event
    }

    fn string_vendor_event(&self) -> Option<u32> {
        Some(self.string_vendor_event)
    }

    fn string_amps(&self) -> i16 {
        self.string_amps
    }

    fn string_amp_hours(&self) -> Option<u32> {
        Some(self.string_amp_hours)
    }

    fn string_voltage(&self) -> Option<u16> {
        Some(self.string_voltage)
    }

    fn string_watts(&self) -> Option<i16> {
        Some(self.string_watts)
    }

    fn string_watt_hours(&self) -> Option<u32> {
        Some(self.string_watt_hours)
    }

    fn string_pr(&self) -> Option<u16> {
        Some(self.string_pr)
    }

    fn string_n(&self) -> Option<u16> {
        Some(self.string_n)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 402. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_402: StaticModelSpec = StaticModelSpec {
    id: 402,
    length: model_402_c_length,
    writable: false,
    visit_read: model_402_c_visit_read,
    visit_write: model_402_c_visit_write,
};

fn model_402_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model402).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model402{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_402_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model402;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model402StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model402CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_402_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_402_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (kind, adapter, buffer);
    let model = Model402;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}
