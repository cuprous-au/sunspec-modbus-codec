// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::{CStr, c_char, c_void};

static POINTS: [PointDetails<()>; 7] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::LogEventModeEnable,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::HttpMessageModeEnable,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::Comm004Certificate,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::SubscribedResourceUrl,
        size: 64,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::SubscribtionEnable,
        size: 1,
        start_address: 69,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    LogEventModeEnable,
    HttpMessageModeEnable,
    Comm004Certificate,
    SubscribedResourceUrl,
    SubscribtionEnable,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 64415. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model64415;

impl<'ad> ModelSpec<'ad> for Model64415 {
    const MODEL_ID: u16 = 64415;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        70
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model64415,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(64415);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::LogEventModeEnable => {
            if let Some(value) = adapter.log_event_mode_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::HttpMessageModeEnable => {
            if let Some(value) = adapter.http_message_mode_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::Comm004Certificate => {
            if let Some(value) = adapter.comm_004_certificate() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::SubscribedResourceUrl => {
            if let Some(value) = adapter.subscribed_resource_url() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::SubscribtionEnable => {
            if let Some(value) = adapter.subscribtion_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model64415,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::LogEventModeEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                LogEventEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_log_event_mode_enable(enum_value);
            Ok(())
        }
        Point::HttpMessageModeEnable => {
            let value = buffer.read_u16()?;
            let enum_value = HttpMsg::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_http_message_mode_enable(enum_value);
            Ok(())
        }
        Point::Comm004Certificate => {
            let value = buffer.read_u16()?;
            let enum_value =
                Comm004Cert::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_comm_004_certificate(enum_value);
            Ok(())
        }
        Point::SubscribedResourceUrl => {
            let value = buffer.read_string::<128>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_subscribed_resource_url(str_value);
            Ok(())
        }
        Point::SubscribtionEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                SubscriptionEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_subscribtion_enable(enum_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    fn log_event_mode_enable(&self) -> Option<LogEventEna> {
        None
    }

    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    fn http_message_mode_enable(&self) -> Option<HttpMsg> {
        None
    }

    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    fn comm_004_certificate(&self) -> Option<Comm004Cert> {
        None
    }

    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    fn subscribed_resource_url(&self) -> Option<&CStr> {
        None
    }

    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    fn subscribtion_enable(&self) -> Option<SubscriptionEna> {
        None
    }
}

pub trait WriteAdapter {
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    fn set_log_event_mode_enable(&mut self, value: LogEventEna) {}

    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    fn set_http_message_mode_enable(&mut self, value: HttpMsg) {}

    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    fn set_comm_004_certificate(&mut self, value: Comm004Cert) {}

    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    fn set_subscribed_resource_url(&mut self, value: &CStr) {}

    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    fn set_subscribtion_enable(&mut self, value: SubscriptionEna) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model64415Comm004Cert {
    /// DEFAULT
    ///
    /// Default Certificate
    DefaultCertificate = 0,
    /// COMM-004A
    ///
    /// Chain Length Two Certificate
    Comm004a = 1,
    /// COMM-004B
    ///
    /// Chain Length Three Certificate
    Comm004b = 2,
    /// COMM-004C
    ///
    /// Chain Length Four Certificate
    Comm004c = 3,
    /// COMM-004D
    ///
    /// Invalid MICA Extended Key Critical Value
    Comm004d = 4,
    /// COMM-004E
    ///
    /// Invalid MICA Name Non-Critical Value
    Comm004e = 5,
    /// COMM-004F
    ///
    /// Invalid MICA Policy Mapping Non-Critical Value
    Comm004f = 6,
    /// COMM-004G
    ///
    /// Self-signed device certificate
    Comm004g = 7,
}

impl Model64415Comm004Cert {
    pub fn from_repr(repr: u16) -> Option<Model64415Comm004Cert> {
        match repr {
            0 => Some(Model64415Comm004Cert::DefaultCertificate),
            1 => Some(Model64415Comm004Cert::Comm004a),
            2 => Some(Model64415Comm004Cert::Comm004b),
            3 => Some(Model64415Comm004Cert::Comm004c),
            4 => Some(Model64415Comm004Cert::Comm004d),
            5 => Some(Model64415Comm004Cert::Comm004e),
            6 => Some(Model64415Comm004Cert::Comm004f),
            7 => Some(Model64415Comm004Cert::Comm004g),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model64415Comm004Cert`] - only `Model64415Comm004Cert` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Comm004Cert` distinct from another model's point of the same name.
pub type Comm004Cert = Model64415Comm004Cert;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model64415HttpMsg {
    /// Disabled
    ///
    /// HTTP Message Mode Disabled
    Disabled = 0,
    /// Enabled
    ///
    /// HTTP Message Mode Enabled
    Enabled = 1,
}

impl Model64415HttpMsg {
    pub fn from_repr(repr: u16) -> Option<Model64415HttpMsg> {
        match repr {
            0 => Some(Model64415HttpMsg::Disabled),
            1 => Some(Model64415HttpMsg::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model64415HttpMsg`] - only `Model64415HttpMsg` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `HttpMsg` distinct from another model's point of the same name.
pub type HttpMsg = Model64415HttpMsg;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model64415LogEventEna {
    /// Disabled
    ///
    /// LogEvent Mode Disabled
    Disabled = 0,
    /// Enabled
    ///
    /// LogEvent Mode Enabled
    Enabled = 1,
}

impl Model64415LogEventEna {
    pub fn from_repr(repr: u16) -> Option<Model64415LogEventEna> {
        match repr {
            0 => Some(Model64415LogEventEna::Disabled),
            1 => Some(Model64415LogEventEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model64415LogEventEna`] - only `Model64415LogEventEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `LogEventEna` distinct from another model's point of the same name.
pub type LogEventEna = Model64415LogEventEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model64415SubscriptionEna {
    /// Disabled
    ///
    /// Subscription Mode Disabled
    Disabled = 0,
    /// Enabled
    ///
    /// Subscription Mode Enabled
    Enabled = 1,
}

impl Model64415SubscriptionEna {
    pub fn from_repr(repr: u16) -> Option<Model64415SubscriptionEna> {
        match repr {
            0 => Some(Model64415SubscriptionEna::Disabled),
            1 => Some(Model64415SubscriptionEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model64415SubscriptionEna`] - only `Model64415SubscriptionEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `SubscriptionEna` distinct from another model's point of the same name.
pub type SubscriptionEna = Model64415SubscriptionEna;

#[repr(C)]
pub struct Model64415CallbackAdapter {
    context: *mut c_void,
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    log_event_mode_enable_callback: Option<extern "C" fn(*const c_void) -> Model64415LogEventEna>,
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    set_log_event_mode_enable_callback: Option<extern "C" fn(Model64415LogEventEna, *mut c_void)>,
    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    http_message_mode_enable_callback: Option<extern "C" fn(*const c_void) -> Model64415HttpMsg>,
    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    set_http_message_mode_enable_callback: Option<extern "C" fn(Model64415HttpMsg, *mut c_void)>,
    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    comm_004_certificate_callback: Option<extern "C" fn(*const c_void) -> Model64415Comm004Cert>,
    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    set_comm_004_certificate_callback: Option<extern "C" fn(Model64415Comm004Cert, *mut c_void)>,
    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    subscribed_resource_url_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    set_subscribed_resource_url_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    subscribtion_enable_callback: Option<extern "C" fn(*const c_void) -> Model64415SubscriptionEna>,
    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    set_subscribtion_enable_callback: Option<extern "C" fn(Model64415SubscriptionEna, *mut c_void)>,
}

impl ReadAdapter for Model64415CallbackAdapter {
    fn log_event_mode_enable(&self) -> Option<LogEventEna> {
        self.log_event_mode_enable_callback
            .map(|callback| (callback)(self.context))
    }

    fn http_message_mode_enable(&self) -> Option<HttpMsg> {
        self.http_message_mode_enable_callback
            .map(|callback| (callback)(self.context))
    }

    fn comm_004_certificate(&self) -> Option<Comm004Cert> {
        self.comm_004_certificate_callback
            .map(|callback| (callback)(self.context))
    }

    fn subscribed_resource_url(&self) -> Option<&CStr> {
        self.subscribed_resource_url_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn subscribtion_enable(&self) -> Option<SubscriptionEna> {
        self.subscribtion_enable_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model64415CallbackAdapter {
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    fn set_log_event_mode_enable(&mut self, value: LogEventEna) {
        if let Some(callback) = self.set_log_event_mode_enable_callback {
            (callback)(value, self.context);
        };
    }

    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    fn set_http_message_mode_enable(&mut self, value: HttpMsg) {
        if let Some(callback) = self.set_http_message_mode_enable_callback {
            (callback)(value, self.context);
        };
    }

    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    fn set_comm_004_certificate(&mut self, value: Comm004Cert) {
        if let Some(callback) = self.set_comm_004_certificate_callback {
            (callback)(value, self.context);
        };
    }

    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    fn set_subscribed_resource_url(&mut self, value: &CStr) {
        if let Some(callback) = self.set_subscribed_resource_url_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }

    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    fn set_subscribtion_enable(&mut self, value: SubscriptionEna) {
        if let Some(callback) = self.set_subscribtion_enable_callback {
            (callback)(value, self.context);
        };
    }
}

#[repr(C)]
pub struct Model64415StatefulAdapter {
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    pub log_event_mode_enable: Model64415LogEventEna,
    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    pub http_message_mode_enable: Model64415HttpMsg,
    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    pub comm_004_certificate: Model64415Comm004Cert,
    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    pub subscribed_resource_url: [c_char; 128],
    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    pub subscribtion_enable: Model64415SubscriptionEna,
}

impl ReadAdapter for Model64415StatefulAdapter {
    fn log_event_mode_enable(&self) -> Option<LogEventEna> {
        Some(self.log_event_mode_enable)
    }

    fn http_message_mode_enable(&self) -> Option<HttpMsg> {
        Some(self.http_message_mode_enable)
    }

    fn comm_004_certificate(&self) -> Option<Comm004Cert> {
        Some(self.comm_004_certificate)
    }

    fn subscribed_resource_url(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.subscribed_resource_url.as_ptr()) })
    }

    fn subscribtion_enable(&self) -> Option<SubscriptionEna> {
        Some(self.subscribtion_enable)
    }
}

impl WriteAdapter for Model64415StatefulAdapter {
    /// LogEvent Mode Enable (LogEventEna)
    ///
    /// Enable or disable the LogEvent mode
    fn set_log_event_mode_enable(&mut self, value: LogEventEna) {
        self.log_event_mode_enable = value;
    }

    /// HTTP Message Mode Enable (HTTPMsg)
    ///
    /// Enable or disable the HTTP Message mode
    fn set_http_message_mode_enable(&mut self, value: HttpMsg) {
        self.http_message_mode_enable = value;
    }

    /// COMM-004 Certificate (COMM004Cert)
    ///
    /// Select COMM-004 certificate type
    fn set_comm_004_certificate(&mut self, value: Comm004Cert) {
        self.comm_004_certificate = value;
    }

    /// Subscribed Resource URL (SubscribedResource)
    ///
    /// The URL of the resource to subscribe to
    fn set_subscribed_resource_url(&mut self, value: &CStr) {
        for (dest, src) in self
            .subscribed_resource_url
            .iter_mut()
            .zip(value.to_bytes_with_nul().iter())
        {
            *dest = *src as c_char;
        }
    }

    /// Subscribtion Enable (SubscriptionEna)
    ///
    /// Enable or disable the Subscription mode
    fn set_subscribtion_enable(&mut self, value: SubscriptionEna) {
        self.subscribtion_enable = value;
    }
}

/// C-FFI dispatch descriptor for SunSpec model 64415. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_64415: StaticModelSpec = StaticModelSpec {
    id: 64415,
    length: model_64415_c_length,
    writable: true,
    visit_read: model_64415_c_visit_read,
    visit_write: model_64415_c_visit_write,
};

fn model_64415_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model64415).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model64415{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_64415_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model64415;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model64415StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model64415CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_64415_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_64415_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model64415;
    let adapter: Option<&mut dyn WriteAdapter> =
        match kind {
            1 => Some(unsafe { &mut *(adapter as *mut Model64415StatefulAdapter) }
                as &mut dyn WriteAdapter),
            2 => Some(unsafe { &mut *(adapter as *mut Model64415CallbackAdapter) }
                as &mut dyn WriteAdapter),
            _ => None,
        };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}
