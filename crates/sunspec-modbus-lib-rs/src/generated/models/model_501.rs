// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model
// definitions in https://github.com/sunspec/models. Do not edit by hand - run
// `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 19] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Status,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::VendorStatus,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::Events,
        size: 2,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::VendorModuleEventFlags,
        size: 2,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Control,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::VendorControl,
        size: 2,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::ControlValue,
        size: 2,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::Timestamp,
        size: 2,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::OutputCurrent,
        size: 2,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::OutputVoltage,
        size: 2,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::OutputEnergy,
        size: 2,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::OutputPower,
        size: 2,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::Temp,
        size: 2,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::InputCurrent,
        size: 2,
        start_address: 25,
    },
    PointDetails {
        point: |()| Point::InputVoltage,
        size: 2,
        start_address: 27,
    },
    PointDetails {
        point: |()| Point::InputEnergy,
        size: 2,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::InputPower,
        size: 2,
        start_address: 31,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Status,
    VendorStatus,
    Events,
    VendorModuleEventFlags,
    Control,
    VendorControl,
    ControlValue,
    Timestamp,
    OutputCurrent,
    OutputVoltage,
    OutputEnergy,
    OutputPower,
    Temp,
    InputCurrent,
    InputVoltage,
    InputEnergy,
    InputPower,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 501. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model501;

impl<'ad> ModelSpec<'ad> for Model501 {
    const MODEL_ID: u16 = 501;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        33
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model501,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(501);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Status => {
            buffer.write_u16(adapter.status() as u16);
        }
        Point::VendorStatus => {
            if let Some(value) = adapter.vendor_status() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Events => {
            buffer.write_u32(adapter.events(), offset);
        }
        Point::VendorModuleEventFlags => {
            if let Some(value) = adapter.vendor_module_event_flags() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Control => {
            if let Some(value) = adapter.control() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VendorControl => {
            if let Some(value) = adapter.vendor_control() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ControlValue => {
            if let Some(value) = adapter.control_value() {
                buffer.write_i32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Timestamp => {
            if let Some(value) = adapter.timestamp() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputCurrent => {
            if let Some(value) = adapter.output_current() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputVoltage => {
            if let Some(value) = adapter.output_voltage() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputEnergy => {
            if let Some(value) = adapter.output_energy() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::OutputPower => {
            if let Some(value) = adapter.output_power() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Temp => {
            if let Some(value) = adapter.temp() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputCurrent => {
            if let Some(value) = adapter.input_current() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputVoltage => {
            if let Some(value) = adapter.input_voltage() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputEnergy => {
            if let Some(value) = adapter.input_energy() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::InputPower => {
            if let Some(value) = adapter.input_power() {
                buffer.write_f32(value, offset);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model501,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::Control => {
            let value = buffer.read_u16()?;
            adapter.set_control(value);
            Ok(())
        }
        Point::VendorControl => {
            let value = buffer.read_u32()?;
            adapter.set_vendor_control(value);
            Ok(())
        }
        Point::ControlValue => {
            let value = buffer.read_i32()?;
            adapter.set_control_value(value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Status (Stat)
    ///
    /// Module Status Code
    fn status(&self) -> Stat;

    /// Vendor Status (StatVend)
    ///
    /// Module Vendor Status Code
    fn vendor_status(&self) -> Option<u16> {
        None
    }

    /// Events (Evt)
    ///
    /// Module Event Flags
    fn events(&self) -> u32;

    /// Vendor Module Event Flags (EvtVend)
    ///
    /// Vendor specific flags
    fn vendor_module_event_flags(&self) -> Option<u32> {
        None
    }

    /// Control (Ctl)
    ///
    /// Module Control
    fn control(&self) -> Option<u16> {
        None
    }

    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    fn vendor_control(&self) -> Option<u32> {
        None
    }

    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    fn control_value(&self) -> Option<i32> {
        None
    }

    /// Timestamp (Tms)
    ///
    /// Time in seconds since 2000 epoch
    fn timestamp(&self) -> Option<u32> {
        None
    }

    /// Output Current (OutA)
    ///
    /// Output Current
    fn output_current(&self) -> Option<f32> {
        None
    }

    /// Output Voltage (OutV)
    ///
    /// Output Voltage
    fn output_voltage(&self) -> Option<f32> {
        None
    }

    /// Output Energy (OutWh)
    ///
    /// Output Energy
    fn output_energy(&self) -> Option<f32> {
        None
    }

    /// Output Power (OutW)
    ///
    /// Output Power
    fn output_power(&self) -> Option<f32> {
        None
    }

    /// Temp (Tmp)
    ///
    /// Module Temperature
    fn temp(&self) -> Option<f32> {
        None
    }

    /// Input Current (InA)
    ///
    /// Input Current
    fn input_current(&self) -> Option<f32> {
        None
    }

    /// Input Voltage (InV)
    ///
    /// Input Voltage
    fn input_voltage(&self) -> Option<f32> {
        None
    }

    /// Input Energy (InWh)
    ///
    /// Input Energy
    fn input_energy(&self) -> Option<f32> {
        None
    }

    /// Input Power (InW)
    ///
    /// Input Power
    fn input_power(&self) -> Option<f32> {
        None
    }
}

pub trait WriteAdapter {
    /// Control (Ctl)
    ///
    /// Module Control
    fn set_control(&mut self, value: u16) {}

    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    fn set_vendor_control(&mut self, value: u32) {}

    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    fn set_control_value(&mut self, value: i32) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model501Stat {
    Off = 1,
    Sleeping = 2,
    Starting = 3,
    Mppt = 4,
    Throttled = 5,
    ShuttingDown = 6,
    Fault = 7,
    Standby = 8,
    Test = 9,
    Other = 10,
}

impl Model501Stat {
    pub fn from_repr(repr: u16) -> Option<Model501Stat> {
        match repr {
            1 => Some(Model501Stat::Off),
            2 => Some(Model501Stat::Sleeping),
            3 => Some(Model501Stat::Starting),
            4 => Some(Model501Stat::Mppt),
            5 => Some(Model501Stat::Throttled),
            6 => Some(Model501Stat::ShuttingDown),
            7 => Some(Model501Stat::Fault),
            8 => Some(Model501Stat::Standby),
            9 => Some(Model501Stat::Test),
            10 => Some(Model501Stat::Other),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model501Stat`] - only `Model501Stat` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Stat` distinct from another model's point of the same name.
pub type Stat = Model501Stat;

#[repr(C)]
pub struct Model501CallbackAdapter {
    context: *mut c_void,
    /// Status (Stat)
    ///
    /// Module Status Code
    status_callback: extern "C" fn(*const c_void) -> Model501Stat,
    /// Vendor Status (StatVend)
    ///
    /// Module Vendor Status Code
    vendor_status_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Events (Evt)
    ///
    /// Module Event Flags
    events_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Module Event Flags (EvtVend)
    ///
    /// Vendor specific flags
    vendor_module_event_flags_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Control (Ctl)
    ///
    /// Module Control
    control_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Control (Ctl)
    ///
    /// Module Control
    set_control_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    vendor_control_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    set_vendor_control_callback: Option<extern "C" fn(u32, *mut c_void)>,
    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    control_value_callback: Option<extern "C" fn(*const c_void) -> i32>,
    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    set_control_value_callback: Option<extern "C" fn(i32, *mut c_void)>,
    /// Timestamp (Tms)
    ///
    /// Time in seconds since 2000 epoch
    timestamp_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Output Current (OutA)
    ///
    /// Output Current
    output_current_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Output Voltage (OutV)
    ///
    /// Output Voltage
    output_voltage_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Output Energy (OutWh)
    ///
    /// Output Energy
    output_energy_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Output Power (OutW)
    ///
    /// Output Power
    output_power_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Temp (Tmp)
    ///
    /// Module Temperature
    temp_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Input Current (InA)
    ///
    /// Input Current
    input_current_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Input Voltage (InV)
    ///
    /// Input Voltage
    input_voltage_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Input Energy (InWh)
    ///
    /// Input Energy
    input_energy_callback: Option<extern "C" fn(*const c_void) -> f32>,
    /// Input Power (InW)
    ///
    /// Input Power
    input_power_callback: Option<extern "C" fn(*const c_void) -> f32>,
}

impl ReadAdapter for Model501CallbackAdapter {
    fn status(&self) -> Stat {
        (self.status_callback)(self.context)
    }

    fn vendor_status(&self) -> Option<u16> {
        self.vendor_status_callback
            .map(|callback| (callback)(self.context))
    }

    fn events(&self) -> u32 {
        (self.events_callback)(self.context)
    }

    fn vendor_module_event_flags(&self) -> Option<u32> {
        self.vendor_module_event_flags_callback
            .map(|callback| (callback)(self.context))
    }

    fn control(&self) -> Option<u16> {
        self.control_callback
            .map(|callback| (callback)(self.context))
    }

    fn vendor_control(&self) -> Option<u32> {
        self.vendor_control_callback
            .map(|callback| (callback)(self.context))
    }

    fn control_value(&self) -> Option<i32> {
        self.control_value_callback
            .map(|callback| (callback)(self.context))
    }

    fn timestamp(&self) -> Option<u32> {
        self.timestamp_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_current(&self) -> Option<f32> {
        self.output_current_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_voltage(&self) -> Option<f32> {
        self.output_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_energy(&self) -> Option<f32> {
        self.output_energy_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_power(&self) -> Option<f32> {
        self.output_power_callback
            .map(|callback| (callback)(self.context))
    }

    fn temp(&self) -> Option<f32> {
        self.temp_callback.map(|callback| (callback)(self.context))
    }

    fn input_current(&self) -> Option<f32> {
        self.input_current_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_voltage(&self) -> Option<f32> {
        self.input_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_energy(&self) -> Option<f32> {
        self.input_energy_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_power(&self) -> Option<f32> {
        self.input_power_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model501CallbackAdapter {
    /// Control (Ctl)
    ///
    /// Module Control
    fn set_control(&mut self, value: u16) {
        if let Some(callback) = self.set_control_callback {
            (callback)(value, self.context);
        };
    }

    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    fn set_vendor_control(&mut self, value: u32) {
        if let Some(callback) = self.set_vendor_control_callback {
            (callback)(value, self.context);
        };
    }

    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    fn set_control_value(&mut self, value: i32) {
        if let Some(callback) = self.set_control_value_callback {
            (callback)(value, self.context);
        };
    }
}

#[repr(C)]
pub struct Model501StatefulAdapter {
    /// Status (Stat)
    ///
    /// Module Status Code
    pub status: Model501Stat,
    /// Vendor Status (StatVend)
    ///
    /// Module Vendor Status Code
    pub vendor_status: u16,
    /// Events (Evt)
    ///
    /// Module Event Flags
    pub events: u32,
    /// Vendor Module Event Flags (EvtVend)
    ///
    /// Vendor specific flags
    pub vendor_module_event_flags: u32,
    /// Control (Ctl)
    ///
    /// Module Control
    pub control: u16,
    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    pub vendor_control: u32,
    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    pub control_value: i32,
    /// Timestamp (Tms)
    ///
    /// Time in seconds since 2000 epoch
    pub timestamp: u32,
    /// Output Current (OutA)
    ///
    /// Output Current
    pub output_current: f32,
    /// Output Voltage (OutV)
    ///
    /// Output Voltage
    pub output_voltage: f32,
    /// Output Energy (OutWh)
    ///
    /// Output Energy
    pub output_energy: f32,
    /// Output Power (OutW)
    ///
    /// Output Power
    pub output_power: f32,
    /// Temp (Tmp)
    ///
    /// Module Temperature
    pub temp: f32,
    /// Input Current (InA)
    ///
    /// Input Current
    pub input_current: f32,
    /// Input Voltage (InV)
    ///
    /// Input Voltage
    pub input_voltage: f32,
    /// Input Energy (InWh)
    ///
    /// Input Energy
    pub input_energy: f32,
    /// Input Power (InW)
    ///
    /// Input Power
    pub input_power: f32,
}

impl ReadAdapter for Model501StatefulAdapter {
    fn status(&self) -> Stat {
        self.status
    }

    fn vendor_status(&self) -> Option<u16> {
        Some(self.vendor_status)
    }

    fn events(&self) -> u32 {
        self.events
    }

    fn vendor_module_event_flags(&self) -> Option<u32> {
        Some(self.vendor_module_event_flags)
    }

    fn control(&self) -> Option<u16> {
        Some(self.control)
    }

    fn vendor_control(&self) -> Option<u32> {
        Some(self.vendor_control)
    }

    fn control_value(&self) -> Option<i32> {
        Some(self.control_value)
    }

    fn timestamp(&self) -> Option<u32> {
        Some(self.timestamp)
    }

    fn output_current(&self) -> Option<f32> {
        Some(self.output_current)
    }

    fn output_voltage(&self) -> Option<f32> {
        Some(self.output_voltage)
    }

    fn output_energy(&self) -> Option<f32> {
        Some(self.output_energy)
    }

    fn output_power(&self) -> Option<f32> {
        Some(self.output_power)
    }

    fn temp(&self) -> Option<f32> {
        Some(self.temp)
    }

    fn input_current(&self) -> Option<f32> {
        Some(self.input_current)
    }

    fn input_voltage(&self) -> Option<f32> {
        Some(self.input_voltage)
    }

    fn input_energy(&self) -> Option<f32> {
        Some(self.input_energy)
    }

    fn input_power(&self) -> Option<f32> {
        Some(self.input_power)
    }
}

impl WriteAdapter for Model501StatefulAdapter {
    /// Control (Ctl)
    ///
    /// Module Control
    fn set_control(&mut self, value: u16) {
        self.control = value;
    }

    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    fn set_vendor_control(&mut self, value: u32) {
        self.vendor_control = value;
    }

    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    fn set_control_value(&mut self, value: i32) {
        self.control_value = value;
    }
}

/// C-FFI dispatch descriptor for SunSpec model 501. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_501: StaticModelSpec = StaticModelSpec {
    id: 501,
    length: model_501_c_length,
    writable: true,
    visit_read: model_501_c_visit_read,
    visit_write: model_501_c_visit_write,
};

fn model_501_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model501).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model501{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_501_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model501;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model501StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model501CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_501_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_501_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model501;
    let adapter: Option<&mut dyn WriteAdapter> = match kind {
        1 => {
            Some(unsafe { &mut *(adapter as *mut Model501StatefulAdapter) } as &mut dyn WriteAdapter)
        }
        2 => {
            Some(unsafe { &mut *(adapter as *mut Model501CallbackAdapter) } as &mut dyn WriteAdapter)
        }
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}
