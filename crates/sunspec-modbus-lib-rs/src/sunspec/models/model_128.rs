// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 16] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::ArGraMod,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::ArGraSag,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::ArGraSwell,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::ModEna,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::FilTms,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::DbVMin,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::DbVMax,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::BlkZnV,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::HysBlkZnV,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::BlkZnTmms,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::HoldTmms,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::ArGraSf,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::VRefPctSf,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::Pad,
        size: 1,
        start_address: 15,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    ArGraMod,
    ArGraSag,
    ArGraSwell,
    ModEna,
    FilTms,
    DbVMin,
    DbVMax,
    BlkZnV,
    HysBlkZnV,
    BlkZnTmms,
    HoldTmms,
    ArGraSf,
    VRefPctSf,
    Pad,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 128. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model128;

impl<'ad> ModelSpec<'ad> for Model128 {
    const MODEL_ID: u16 = 128;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        16
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model128,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(128);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::ArGraMod => {
            buffer.write_u16(adapter.ar_gra_mod() as u16);
        }
        Point::ArGraSag => {
            buffer.write_u16(adapter.ar_gra_sag());
        }
        Point::ArGraSwell => {
            buffer.write_u16(adapter.ar_gra_swell());
        }
        Point::ModEna => {
            buffer.write_u16(adapter.mod_ena());
        }
        Point::FilTms => {
            if let Some(value) = adapter.fil_tms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DbVMin => {
            if let Some(value) = adapter.db_v_min() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DbVMax => {
            if let Some(value) = adapter.db_v_max() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::BlkZnV => {
            if let Some(value) = adapter.blk_zn_v() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::HysBlkZnV => {
            if let Some(value) = adapter.hys_blk_zn_v() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::BlkZnTmms => {
            if let Some(value) = adapter.blk_zn_tmms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::HoldTmms => {
            if let Some(value) = adapter.hold_tmms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ArGraSf => {
            buffer.write_i16(adapter.ar_gra_sf());
        }
        Point::VRefPctSf => {
            if let Some(value) = adapter.v_ref_pct_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Pad => {
            buffer.write_u16(0);
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model128,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::ArGraMod => {
            let value = buffer.read_u16()?;
            let enum_value = ArGraMod::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_ar_gra_mod(enum_value);
            Ok(())
        }
        Point::ArGraSag => {
            let value = buffer.read_u16()?;
            adapter.set_ar_gra_sag(value);
            Ok(())
        }
        Point::ArGraSwell => {
            let value = buffer.read_u16()?;
            adapter.set_ar_gra_swell(value);
            Ok(())
        }
        Point::ModEna => {
            let value = buffer.read_u16()?;
            adapter.set_mod_ena(value);
            Ok(())
        }
        Point::FilTms => {
            let value = buffer.read_u16()?;
            adapter.set_fil_tms(value);
            Ok(())
        }
        Point::DbVMin => {
            let value = buffer.read_u16()?;
            adapter.set_db_v_min(value);
            Ok(())
        }
        Point::DbVMax => {
            let value = buffer.read_u16()?;
            adapter.set_db_v_max(value);
            Ok(())
        }
        Point::BlkZnV => {
            let value = buffer.read_u16()?;
            adapter.set_blk_zn_v(value);
            Ok(())
        }
        Point::HysBlkZnV => {
            let value = buffer.read_u16()?;
            adapter.set_hys_blk_zn_v(value);
            Ok(())
        }
        Point::BlkZnTmms => {
            let value = buffer.read_u16()?;
            adapter.set_blk_zn_tmms(value);
            Ok(())
        }
        Point::HoldTmms => {
            let value = buffer.read_u16()?;
            adapter.set_hold_tmms(value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// ArGraMod (ArGraMod)
    ///
    /// Indicates if gradients trend toward zero at the edges of the deadband or trend toward zero at the center of the deadband.
    fn ar_gra_mod(&self) -> ArGraMod;

    /// ArGraSag (ArGraSag)
    ///
    /// The gradient used to increase capacitive dynamic current. A value of 0 indicates no additional reactive current support.
    fn ar_gra_sag(&self) -> u16;

    /// ArGraSwell (ArGraSwell)
    ///
    /// The gradient used to increase inductive dynamic current. A value of 0 indicates no additional reactive current support.
    fn ar_gra_swell(&self) -> u16;

    /// ModEna (ModEna)
    ///
    /// Activate dynamic reactive current model
    fn mod_ena(&self) -> u16;

    /// FilTms (FilTms)
    ///
    /// The time window used to calculate the moving average voltage.
    fn fil_tms(&self) -> Option<u16> {
        None
    }

    /// DbVMin (DbVMin)
    ///
    /// The lower delta voltage limit for which negative voltage deviations less than this value no dynamic vars are produced.
    fn db_v_min(&self) -> Option<u16> {
        None
    }

    /// DbVMax (DbVMax)
    ///
    /// The upper delta voltage limit for which positive voltage deviations less than this value no dynamic current produced.
    fn db_v_max(&self) -> Option<u16> {
        None
    }

    /// BlkZnV (BlkZnV)
    ///
    /// Block zone voltage which defines a lower voltage boundary below which no dynamic current is produced.
    fn blk_zn_v(&self) -> Option<u16> {
        None
    }

    /// HysBlkZnV (HysBlkZnV)
    ///
    /// Hysteresis voltage used with BlkZnV.
    fn hys_blk_zn_v(&self) -> Option<u16> {
        None
    }

    /// BlkZnTmms (BlkZnTmms)
    ///
    /// Block zone time the time before which reactive current support remains active regardless of how low the voltage drops.
    fn blk_zn_tmms(&self) -> Option<u16> {
        None
    }

    /// HoldTmms (HoldTmms)
    ///
    /// Hold time during which reactive current support continues after the average voltage has entered the dead zone.
    fn hold_tmms(&self) -> Option<u16> {
        None
    }

    /// ArGra_SF (ArGra_SF)
    ///
    /// Scale factor for the gradients.
    fn ar_gra_sf(&self) -> i16;

    /// VRefPct_SF (VRefPct_SF)
    ///
    /// Scale factor for the voltage zone and limit settings.
    fn v_ref_pct_sf(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {
    /// ArGraMod (ArGraMod)
    ///
    /// Indicates if gradients trend toward zero at the edges of the deadband or trend toward zero at the center of the deadband.
    fn set_ar_gra_mod(&mut self, value: ArGraMod);

    /// ArGraSag (ArGraSag)
    ///
    /// The gradient used to increase capacitive dynamic current. A value of 0 indicates no additional reactive current support.
    fn set_ar_gra_sag(&mut self, value: u16);

    /// ArGraSwell (ArGraSwell)
    ///
    /// The gradient used to increase inductive dynamic current. A value of 0 indicates no additional reactive current support.
    fn set_ar_gra_swell(&mut self, value: u16);

    /// ModEna (ModEna)
    ///
    /// Activate dynamic reactive current model
    fn set_mod_ena(&mut self, value: u16);

    /// FilTms (FilTms)
    ///
    /// The time window used to calculate the moving average voltage.
    fn set_fil_tms(&mut self, value: u16) {}

    /// DbVMin (DbVMin)
    ///
    /// The lower delta voltage limit for which negative voltage deviations less than this value no dynamic vars are produced.
    fn set_db_v_min(&mut self, value: u16) {}

    /// DbVMax (DbVMax)
    ///
    /// The upper delta voltage limit for which positive voltage deviations less than this value no dynamic current produced.
    fn set_db_v_max(&mut self, value: u16) {}

    /// BlkZnV (BlkZnV)
    ///
    /// Block zone voltage which defines a lower voltage boundary below which no dynamic current is produced.
    fn set_blk_zn_v(&mut self, value: u16) {}

    /// HysBlkZnV (HysBlkZnV)
    ///
    /// Hysteresis voltage used with BlkZnV.
    fn set_hys_blk_zn_v(&mut self, value: u16) {}

    /// BlkZnTmms (BlkZnTmms)
    ///
    /// Block zone time the time before which reactive current support remains active regardless of how low the voltage drops.
    fn set_blk_zn_tmms(&mut self, value: u16) {}

    /// HoldTmms (HoldTmms)
    ///
    /// Hold time during which reactive current support continues after the average voltage has entered the dead zone.
    fn set_hold_tmms(&mut self, value: u16) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model128ArGraMod {
    Edge = 0,
    Center = 1,
}

impl Model128ArGraMod {
    pub fn from_repr(repr: u16) -> Option<Model128ArGraMod> {
        match repr {
            0 => Some(Model128ArGraMod::Edge),
            1 => Some(Model128ArGraMod::Center),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model128ArGraMod`] - only `Model128ArGraMod` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `ArGraMod` distinct from another model's point of the same name.
pub type ArGraMod = Model128ArGraMod;
