// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::{CStr, c_char, c_void};
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_160::*;

#[repr(C)]
pub struct Model160CallbackAdapter {
    context: *mut c_void,
    /// Current Scale Factor (DCA_SF)
    current_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Voltage Scale Factor (DCV_SF)
    voltage_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Power Scale Factor (DCW_SF)
    power_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Energy Scale Factor (DCWH_SF)
    energy_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Global Events (Evt)
    global_events_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Number of Modules (N)
    number_of_modules_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Timestamp Period (TmsPer)
    timestamp_period_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Input ID (ID)
    module_input_id_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Input ID String (IDStr)
    module_input_id_string_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// DC Current (DCA)
    module_dc_current_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// DC Voltage (DCV)
    module_dc_voltage_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// DC Power (DCW)
    module_dc_power_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Lifetime Energy (DCWH)
    module_lifetime_energy_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Timestamp (Tms)
    module_timestamp_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Temperature (Tmp)
    module_temperature_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Operating State (DCSt)
    module_operating_state_callback: Option<extern "C" fn(*const c_void) -> Model160DcSt>,
    /// Module Events (DCEvt)
    module_module_events_callback: Option<extern "C" fn(*const c_void) -> u32>,
}

impl ReadAdapter for Model160CallbackAdapter {
    fn current_scale_factor(&self) -> Option<i16> {
        self.current_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn voltage_scale_factor(&self) -> Option<i16> {
        self.voltage_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn power_scale_factor(&self) -> Option<i16> {
        self.power_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn energy_scale_factor(&self) -> Option<i16> {
        self.energy_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn global_events(&self) -> Option<u32> {
        self.global_events_callback
            .map(|callback| (callback)(self.context))
    }

    fn number_of_modules(&self) -> Option<u16> {
        self.number_of_modules_callback
            .map(|callback| (callback)(self.context))
    }

    fn timestamp_period(&self) -> Option<u16> {
        self.timestamp_period_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_input_id(&self) -> Option<u16> {
        self.module_input_id_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_input_id_string(&self) -> Option<&CStr> {
        self.module_input_id_string_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn module_dc_current(&self) -> Option<u16> {
        self.module_dc_current_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_dc_voltage(&self) -> Option<u16> {
        self.module_dc_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_dc_power(&self) -> Option<u16> {
        self.module_dc_power_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_lifetime_energy(&self) -> Option<u32> {
        self.module_lifetime_energy_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_timestamp(&self) -> Option<u32> {
        self.module_timestamp_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_temperature(&self) -> Option<i16> {
        self.module_temperature_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_operating_state(&self) -> Option<DcSt> {
        self.module_operating_state_callback
            .map(|callback| (callback)(self.context))
    }

    fn module_module_events(&self) -> Option<u32> {
        self.module_module_events_callback
            .map(|callback| (callback)(self.context))
    }
}

/// C-FFI dispatch descriptor for SunSpec model 160. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_160: StaticModelSpec = StaticModelSpec {
    id: 160,
    length: model_160_c_length,
    writable: false,
    visit_read: model_160_c_visit_read,
    visit_write: model_160_c_visit_write,
};

fn model_160_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model160).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model160CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_160_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model160;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model160CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_160_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_160_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model160;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 160 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_160_callback(
    adapter: *mut Model160CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_160,
        adapter: adapter as *mut c_void,
    }
}
