// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 9] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::EltekCountryCode,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::EltekFeedingPhase,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::EltekApdMethod,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::EltekApdPowerRef,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::EltekRpsMethod,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::EltekRpsQRef,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::EltekRpsCosPhiRef,
        size: 1,
        start_address: 8,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    EltekCountryCode,
    EltekFeedingPhase,
    EltekApdMethod,
    EltekApdPowerRef,
    EltekRpsMethod,
    EltekRpsQRef,
    EltekRpsCosPhiRef,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 64101. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model64101;

impl<'ad> ModelSpec<'ad> for Model64101 {
    const MODEL_ID: u16 = 64101;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        9
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model64101,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(64101);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::EltekCountryCode => {
            if let Some(value) = adapter.eltek_country_code() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekFeedingPhase => {
            if let Some(value) = adapter.eltek_feeding_phase() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekApdMethod => {
            if let Some(value) = adapter.eltek_apd_method() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekApdPowerRef => {
            if let Some(value) = adapter.eltek_apd_power_ref() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekRpsMethod => {
            if let Some(value) = adapter.eltek_rps_method() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekRpsQRef => {
            if let Some(value) = adapter.eltek_rps_q_ref() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EltekRpsCosPhiRef => {
            if let Some(value) = adapter.eltek_rps_cos_phi_ref() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model64101,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// Eltek_Country_Code
    fn eltek_country_code(&self) -> Option<u16> {
        None
    }

    /// Eltek_Feeding_Phase
    fn eltek_feeding_phase(&self) -> Option<u16> {
        None
    }

    /// Eltek_APD_Method
    fn eltek_apd_method(&self) -> Option<u16> {
        None
    }

    /// Eltek_APD_Power_Ref
    fn eltek_apd_power_ref(&self) -> Option<u16> {
        None
    }

    /// Eltek_RPS_Method
    fn eltek_rps_method(&self) -> Option<u16> {
        None
    }

    /// Eltek_RPS_Q_Ref
    fn eltek_rps_q_ref(&self) -> Option<u16> {
        None
    }

    /// Eltek_RPS_CosPhi_Ref
    fn eltek_rps_cos_phi_ref(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {}

#[repr(C)]
pub struct Model64101CallbackAdapter {
    context: *mut c_void,
    /// Eltek_Country_Code
    eltek_country_code_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_Feeding_Phase
    eltek_feeding_phase_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_APD_Method
    eltek_apd_method_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_APD_Power_Ref
    eltek_apd_power_ref_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_Method
    eltek_rps_method_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_Q_Ref
    eltek_rps_q_ref_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Eltek_RPS_CosPhi_Ref
    eltek_rps_cos_phi_ref_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model64101CallbackAdapter {
    fn eltek_country_code(&self) -> Option<u16> {
        self.eltek_country_code_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_feeding_phase(&self) -> Option<u16> {
        self.eltek_feeding_phase_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_apd_method(&self) -> Option<u16> {
        self.eltek_apd_method_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_apd_power_ref(&self) -> Option<u16> {
        self.eltek_apd_power_ref_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_method(&self) -> Option<u16> {
        self.eltek_rps_method_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_q_ref(&self) -> Option<u16> {
        self.eltek_rps_q_ref_callback
            .map(|callback| (callback)(self.context))
    }

    fn eltek_rps_cos_phi_ref(&self) -> Option<i16> {
        self.eltek_rps_cos_phi_ref_callback
            .map(|callback| (callback)(self.context))
    }
}

#[repr(C)]
pub struct Model64101StatefulAdapter {
    /// Eltek_Country_Code
    pub eltek_country_code: u16,
    /// Eltek_Feeding_Phase
    pub eltek_feeding_phase: u16,
    /// Eltek_APD_Method
    pub eltek_apd_method: u16,
    /// Eltek_APD_Power_Ref
    pub eltek_apd_power_ref: u16,
    /// Eltek_RPS_Method
    pub eltek_rps_method: u16,
    /// Eltek_RPS_Q_Ref
    pub eltek_rps_q_ref: u16,
    /// Eltek_RPS_CosPhi_Ref
    pub eltek_rps_cos_phi_ref: i16,
}

impl ReadAdapter for Model64101StatefulAdapter {
    fn eltek_country_code(&self) -> Option<u16> {
        Some(self.eltek_country_code)
    }

    fn eltek_feeding_phase(&self) -> Option<u16> {
        Some(self.eltek_feeding_phase)
    }

    fn eltek_apd_method(&self) -> Option<u16> {
        Some(self.eltek_apd_method)
    }

    fn eltek_apd_power_ref(&self) -> Option<u16> {
        Some(self.eltek_apd_power_ref)
    }

    fn eltek_rps_method(&self) -> Option<u16> {
        Some(self.eltek_rps_method)
    }

    fn eltek_rps_q_ref(&self) -> Option<u16> {
        Some(self.eltek_rps_q_ref)
    }

    fn eltek_rps_cos_phi_ref(&self) -> Option<i16> {
        Some(self.eltek_rps_cos_phi_ref)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 64101. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_64101: StaticModelSpec = StaticModelSpec {
    id: 64101,
    length: model_64101_c_length,
    writable: false,
    visit_read: model_64101_c_visit_read,
    visit_write: model_64101_c_visit_write,
};

fn model_64101_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model64101).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model64101{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_64101_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model64101;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model64101StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model64101CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_64101_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_64101_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (kind, adapter, buffer);
    let model = Model64101;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}
