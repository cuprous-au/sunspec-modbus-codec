[package]
name = "sunspec-modbus-derive"
version.workspace = true
license.workspace = true
repository.workspace = true
edition = "2024"
description = "Provides a derive macro for convenience declaratively defining a ModelList for use in the sunspec-modbus-lib-rs library"
publish = true

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true, features = ["full"] }
