// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 10] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::RampUpRate,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::NomRmpDnRte,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::EmergencyRampUpRate,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::EmergencyRampDownRate,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::ConnectRampUpRate,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::ConnectRampDownRate,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::DefaultRampRate,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::RampRateScaleFactor,
        size: 1,
        start_address: 9,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    RampUpRate,
    NomRmpDnRte,
    EmergencyRampUpRate,
    EmergencyRampDownRate,
    ConnectRampUpRate,
    ConnectRampDownRate,
    DefaultRampRate,
    RampRateScaleFactor,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 145. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model145;

impl<'ad> ModelSpec<'ad> for Model145 {
    const MODEL_ID: u16 = 145;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        10
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model145,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(145);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::RampUpRate => {
            if let Some(value) = adapter.ramp_up_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::NomRmpDnRte => {
            if let Some(value) = adapter.nom_rmp_dn_rte() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EmergencyRampUpRate => {
            if let Some(value) = adapter.emergency_ramp_up_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EmergencyRampDownRate => {
            if let Some(value) = adapter.emergency_ramp_down_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ConnectRampUpRate => {
            if let Some(value) = adapter.connect_ramp_up_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ConnectRampDownRate => {
            if let Some(value) = adapter.connect_ramp_down_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DefaultRampRate => {
            if let Some(value) = adapter.default_ramp_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::RampRateScaleFactor => {
            if let Some(value) = adapter.ramp_rate_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model145,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::RampUpRate => {
            let value = buffer.read_u16()?;
            adapter.set_ramp_up_rate(value);
            Ok(())
        }
        Point::NomRmpDnRte => {
            let value = buffer.read_u16()?;
            adapter.set_nom_rmp_dn_rte(value);
            Ok(())
        }
        Point::EmergencyRampUpRate => {
            let value = buffer.read_u16()?;
            adapter.set_emergency_ramp_up_rate(value);
            Ok(())
        }
        Point::EmergencyRampDownRate => {
            let value = buffer.read_u16()?;
            adapter.set_emergency_ramp_down_rate(value);
            Ok(())
        }
        Point::ConnectRampUpRate => {
            let value = buffer.read_u16()?;
            adapter.set_connect_ramp_up_rate(value);
            Ok(())
        }
        Point::ConnectRampDownRate => {
            let value = buffer.read_u16()?;
            adapter.set_connect_ramp_down_rate(value);
            Ok(())
        }
        Point::DefaultRampRate => {
            let value = buffer.read_u16()?;
            adapter.set_default_ramp_rate(value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Ramp Up Rate (NomRmpUpRte)
    ///
    /// Ramp up rate as a percentage of max current.
    fn ramp_up_rate(&self) -> Option<u16> {
        None
    }

    /// NomRmpDnRte (NomRmpDnRte)
    ///
    /// Ramp down rate as a percentage of max current.
    fn nom_rmp_dn_rte(&self) -> Option<u16> {
        None
    }

    /// Emergency Ramp Up Rate (EmgRmpUpRte)
    ///
    /// Emergency ramp up rate as a percentage of max current.
    fn emergency_ramp_up_rate(&self) -> Option<u16> {
        None
    }

    /// Emergency Ramp Down Rate (EmgRmpDnRte)
    ///
    /// Emergency ramp down rate as a percentage of max current.
    fn emergency_ramp_down_rate(&self) -> Option<u16> {
        None
    }

    /// Connect Ramp Up Rate (ConnRmpUpRte)
    ///
    /// Connect ramp up rate as a percentage of max current.
    fn connect_ramp_up_rate(&self) -> Option<u16> {
        None
    }

    /// Connect Ramp Down Rate (ConnRmpDnRte)
    ///
    /// Connect ramp down rate as a percentage of max current.
    fn connect_ramp_down_rate(&self) -> Option<u16> {
        None
    }

    /// Default Ramp Rate (AGra)
    ///
    /// Ramp rate specified in percent of max current.
    fn default_ramp_rate(&self) -> Option<u16> {
        None
    }

    /// Ramp Rate Scale Factor (Rmp_SF)
    ///
    /// Ramp Rate Scale Factor
    fn ramp_rate_scale_factor(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {
    /// Ramp Up Rate (NomRmpUpRte)
    ///
    /// Ramp up rate as a percentage of max current.
    fn set_ramp_up_rate(&mut self, value: u16) {}

    /// NomRmpDnRte (NomRmpDnRte)
    ///
    /// Ramp down rate as a percentage of max current.
    fn set_nom_rmp_dn_rte(&mut self, value: u16) {}

    /// Emergency Ramp Up Rate (EmgRmpUpRte)
    ///
    /// Emergency ramp up rate as a percentage of max current.
    fn set_emergency_ramp_up_rate(&mut self, value: u16) {}

    /// Emergency Ramp Down Rate (EmgRmpDnRte)
    ///
    /// Emergency ramp down rate as a percentage of max current.
    fn set_emergency_ramp_down_rate(&mut self, value: u16) {}

    /// Connect Ramp Up Rate (ConnRmpUpRte)
    ///
    /// Connect ramp up rate as a percentage of max current.
    fn set_connect_ramp_up_rate(&mut self, value: u16) {}

    /// Connect Ramp Down Rate (ConnRmpDnRte)
    ///
    /// Connect ramp down rate as a percentage of max current.
    fn set_connect_ramp_down_rate(&mut self, value: u16) {}

    /// Default Ramp Rate (AGra)
    ///
    /// Ramp rate specified in percent of max current.
    fn set_default_ramp_rate(&mut self, value: u16) {}
}
