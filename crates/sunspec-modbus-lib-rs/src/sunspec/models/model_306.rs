// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 6] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Ghi,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::Amps,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::Voltage,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::Temperature,
        size: 1,
        start_address: 5,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Ghi,
    Amps,
    Voltage,
    Temperature,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 306. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model306;

impl<'ad> ModelSpec<'ad> for Model306 {
    const MODEL_ID: u16 = 306;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        6
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model306,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(306);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Ghi => {
            if let Some(value) = adapter.ghi() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Amps => {
            if let Some(value) = adapter.amps() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Voltage => {
            if let Some(value) = adapter.voltage() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Temperature => {
            if let Some(value) = adapter.temperature() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model306,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// GHI (GHI)
    ///
    /// Global Horizontal Irradiance
    fn ghi(&self) -> Option<u16> {
        None
    }

    /// Amps (A)
    ///
    /// Current measurement at reference point
    fn amps(&self) -> Option<u16> {
        None
    }

    /// Voltage (V)
    ///
    /// Voltage measurement at reference point
    fn voltage(&self) -> Option<u16> {
        None
    }

    /// Temperature (Tmp)
    ///
    /// Temperature measurement at reference point
    fn temperature(&self) -> Option<u16> {
        None
    }
}

pub trait WriteAdapter {}

#[repr(C)]
pub struct Model306CallbackAdapter {
    context: *mut c_void,
    /// GHI (GHI)
    ///
    /// Global Horizontal Irradiance
    ghi_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Amps (A)
    ///
    /// Current measurement at reference point
    amps_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Voltage (V)
    ///
    /// Voltage measurement at reference point
    voltage_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Temperature (Tmp)
    ///
    /// Temperature measurement at reference point
    temperature_callback: Option<extern "C" fn(*const c_void) -> u16>,
}

impl ReadAdapter for Model306CallbackAdapter {
    fn ghi(&self) -> Option<u16> {
        self.ghi_callback.map(|callback| (callback)(self.context))
    }

    fn amps(&self) -> Option<u16> {
        self.amps_callback.map(|callback| (callback)(self.context))
    }

    fn voltage(&self) -> Option<u16> {
        self.voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn temperature(&self) -> Option<u16> {
        self.temperature_callback
            .map(|callback| (callback)(self.context))
    }
}

#[repr(C)]
pub struct Model306StatefulAdapter {
    /// GHI (GHI)
    ///
    /// Global Horizontal Irradiance
    pub ghi: u16,
    /// Amps (A)
    ///
    /// Current measurement at reference point
    pub amps: u16,
    /// Voltage (V)
    ///
    /// Voltage measurement at reference point
    pub voltage: u16,
    /// Temperature (Tmp)
    ///
    /// Temperature measurement at reference point
    pub temperature: u16,
}

impl ReadAdapter for Model306StatefulAdapter {
    fn ghi(&self) -> Option<u16> {
        Some(self.ghi)
    }

    fn amps(&self) -> Option<u16> {
        Some(self.amps)
    }

    fn voltage(&self) -> Option<u16> {
        Some(self.voltage)
    }

    fn temperature(&self) -> Option<u16> {
        Some(self.temperature)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 306. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_306: StaticModelSpec = StaticModelSpec {
    id: 306,
    length: model_306_c_length,
    writable: false,
    visit_read: model_306_c_visit_read,
    visit_write: model_306_c_visit_write,
};

fn model_306_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model306).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model306{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_306_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model306;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model306StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model306CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_306_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_306_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (kind, adapter, buffer);
    let model = Model306;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}
