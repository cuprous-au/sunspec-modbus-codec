// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model
// definitions in https://github.com/sunspec/models. Do not edit by hand - run
// `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 12] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Aid,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::N,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::Un,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::Status,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::VendorStatus,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::EventCode,
        size: 2,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::VendorEventCode,
        size: 2,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::Control,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::VendorControl,
        size: 2,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::ControlValue,
        size: 2,
        start_address: 14,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Aid,
    N,
    Un,
    Status,
    VendorStatus,
    EventCode,
    VendorEventCode,
    Control,
    VendorControl,
    ControlValue,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 2. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model2;

impl<'ad> ModelSpec<'ad> for Model2 {
    const MODEL_ID: u16 = 2;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        16
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model2,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(2);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Aid => {
            buffer.write_u16(adapter.aid());
        }
        Point::N => {
            buffer.write_u16(adapter.n());
        }
        Point::Un => {
            buffer.write_u16(adapter.un());
        }
        Point::Status => {
            buffer.write_u16(adapter.status() as u16);
        }
        Point::VendorStatus => {
            if let Some(value) = adapter.vendor_status() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EventCode => {
            buffer.write_u32(adapter.event_code(), offset);
        }
        Point::VendorEventCode => {
            if let Some(value) = adapter.vendor_event_code() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Control => {
            if let Some(value) = adapter.control() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::VendorControl => {
            if let Some(value) = adapter.vendor_control() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ControlValue => {
            if let Some(value) = adapter.control_value() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model2,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// AID (AID)
    ///
    /// Aggregated model id
    fn aid(&self) -> u16;

    /// N (N)
    ///
    /// Number of aggregated models
    fn n(&self) -> u16;

    /// UN (UN)
    ///
    /// Update Number. Incrementing number each time the mapping is changed. If the number is not changed from the last reading the direct access to a specific offset will result in reading the same logical model as before. Otherwise the entire model must be read to refresh the changes
    fn un(&self) -> u16;

    /// Status (St)
    ///
    /// Enumerated status code
    fn status(&self) -> St;

    /// Vendor Status (StVnd)
    ///
    /// Vendor specific status code
    fn vendor_status(&self) -> Option<u16> {
        None
    }

    /// Event Code (Evt)
    ///
    /// Bitmask event code
    fn event_code(&self) -> u32;

    /// Vendor Event Code (EvtVnd)
    ///
    /// Vendor specific event code
    fn vendor_event_code(&self) -> Option<u32> {
        None
    }

    /// Control (Ctl)
    ///
    /// Control register for all aggregated devices
    fn control(&self) -> Option<Ctl> {
        None
    }

    /// Vendor Control (CtlVnd)
    ///
    /// Vendor control register for all aggregated devices
    fn vendor_control(&self) -> Option<u32> {
        None
    }

    /// Control Value (CtlVl)
    ///
    /// Numerical value used as a parameter to the control
    fn control_value(&self) -> Option<u32> {
        None
    }
}

pub trait WriteAdapter {}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model2Ctl {
    None = 0,
    Automatic = 1,
    ForceOff = 2,
    Test = 3,
    Throttle = 4,
}

impl Model2Ctl {
    pub fn from_repr(repr: u16) -> Option<Model2Ctl> {
        match repr {
            0 => Some(Model2Ctl::None),
            1 => Some(Model2Ctl::Automatic),
            2 => Some(Model2Ctl::ForceOff),
            3 => Some(Model2Ctl::Test),
            4 => Some(Model2Ctl::Throttle),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model2Ctl`] - only `Model2Ctl` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Ctl` distinct from another model's point of the same name.
pub type Ctl = Model2Ctl;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model2St {
    Off = 1,
    On = 2,
    Full = 3,
    Fault = 4,
}

impl Model2St {
    pub fn from_repr(repr: u16) -> Option<Model2St> {
        match repr {
            1 => Some(Model2St::Off),
            2 => Some(Model2St::On),
            3 => Some(Model2St::Full),
            4 => Some(Model2St::Fault),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model2St`] - only `Model2St` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `St` distinct from another model's point of the same name.
pub type St = Model2St;

#[repr(C)]
pub struct Model2CallbackAdapter {
    context: *mut c_void,
    /// AID (AID)
    ///
    /// Aggregated model id
    aid_callback: extern "C" fn(*const c_void) -> u16,
    /// N (N)
    ///
    /// Number of aggregated models
    n_callback: extern "C" fn(*const c_void) -> u16,
    /// UN (UN)
    ///
    /// Update Number. Incrementing number each time the mapping is changed. If the number is not changed from the last reading the direct access to a specific offset will result in reading the same logical model as before. Otherwise the entire model must be read to refresh the changes
    un_callback: extern "C" fn(*const c_void) -> u16,
    /// Status (St)
    ///
    /// Enumerated status code
    status_callback: extern "C" fn(*const c_void) -> Model2St,
    /// Vendor Status (StVnd)
    ///
    /// Vendor specific status code
    vendor_status_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Event Code (Evt)
    ///
    /// Bitmask event code
    event_code_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Event Code (EvtVnd)
    ///
    /// Vendor specific event code
    vendor_event_code_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Control (Ctl)
    ///
    /// Control register for all aggregated devices
    control_callback: Option<extern "C" fn(*const c_void) -> Model2Ctl>,
    /// Vendor Control (CtlVnd)
    ///
    /// Vendor control register for all aggregated devices
    vendor_control_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Control Value (CtlVl)
    ///
    /// Numerical value used as a parameter to the control
    control_value_callback: Option<extern "C" fn(*const c_void) -> u32>,
}

impl ReadAdapter for Model2CallbackAdapter {
    fn aid(&self) -> u16 {
        (self.aid_callback)(self.context)
    }

    fn n(&self) -> u16 {
        (self.n_callback)(self.context)
    }

    fn un(&self) -> u16 {
        (self.un_callback)(self.context)
    }

    fn status(&self) -> St {
        (self.status_callback)(self.context)
    }

    fn vendor_status(&self) -> Option<u16> {
        self.vendor_status_callback
            .map(|callback| (callback)(self.context))
    }

    fn event_code(&self) -> u32 {
        (self.event_code_callback)(self.context)
    }

    fn vendor_event_code(&self) -> Option<u32> {
        self.vendor_event_code_callback
            .map(|callback| (callback)(self.context))
    }

    fn control(&self) -> Option<Ctl> {
        self.control_callback
            .map(|callback| (callback)(self.context))
    }

    fn vendor_control(&self) -> Option<u32> {
        self.vendor_control_callback
            .map(|callback| (callback)(self.context))
    }

    fn control_value(&self) -> Option<u32> {
        self.control_value_callback
            .map(|callback| (callback)(self.context))
    }
}

#[repr(C)]
pub struct Model2StatefulAdapter {
    /// AID (AID)
    ///
    /// Aggregated model id
    pub aid: u16,
    /// N (N)
    ///
    /// Number of aggregated models
    pub n: u16,
    /// UN (UN)
    ///
    /// Update Number. Incrementing number each time the mapping is changed. If the number is not changed from the last reading the direct access to a specific offset will result in reading the same logical model as before. Otherwise the entire model must be read to refresh the changes
    pub un: u16,
    /// Status (St)
    ///
    /// Enumerated status code
    pub status: Model2St,
    /// Vendor Status (StVnd)
    ///
    /// Vendor specific status code
    pub vendor_status: u16,
    /// Event Code (Evt)
    ///
    /// Bitmask event code
    pub event_code: u32,
    /// Vendor Event Code (EvtVnd)
    ///
    /// Vendor specific event code
    pub vendor_event_code: u32,
    /// Control (Ctl)
    ///
    /// Control register for all aggregated devices
    pub control: Model2Ctl,
    /// Vendor Control (CtlVnd)
    ///
    /// Vendor control register for all aggregated devices
    pub vendor_control: u32,
    /// Control Value (CtlVl)
    ///
    /// Numerical value used as a parameter to the control
    pub control_value: u32,
}

impl ReadAdapter for Model2StatefulAdapter {
    fn aid(&self) -> u16 {
        self.aid
    }

    fn n(&self) -> u16 {
        self.n
    }

    fn un(&self) -> u16 {
        self.un
    }

    fn status(&self) -> St {
        self.status
    }

    fn vendor_status(&self) -> Option<u16> {
        Some(self.vendor_status)
    }

    fn event_code(&self) -> u32 {
        self.event_code
    }

    fn vendor_event_code(&self) -> Option<u32> {
        Some(self.vendor_event_code)
    }

    fn control(&self) -> Option<Ctl> {
        Some(self.control)
    }

    fn vendor_control(&self) -> Option<u32> {
        Some(self.vendor_control)
    }

    fn control_value(&self) -> Option<u32> {
        Some(self.control_value)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 2. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_2: StaticModelSpec = StaticModelSpec {
    id: 2,
    length: model_2_c_length,
    writable: false,
    visit_read: model_2_c_visit_read,
    visit_write: model_2_c_visit_write,
};

fn model_2_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model2).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model2{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_2_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model2;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model2StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model2CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_2_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_2_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (kind, adapter, buffer);
    let model = Model2;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}
