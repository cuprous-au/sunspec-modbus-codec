// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 45] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::PowerFactorEnableWInjEnable,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::PowerFactorReversionEnableWInj,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::PfReversionTimeWInj,
        size: 2,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::PfReversionTimeRemWInj,
        size: 2,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::PowerFactorEnableWAbsEnable,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::PowerFactorReversionEnableWAbs,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::PfReversionTimeWAbs,
        size: 2,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::PfReversionTimeRemWAbs,
        size: 2,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::LimitMaxPowerPctEnable,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::LimitMaxPowerPctSetpoint,
        size: 1,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::ReversionLimitMaxPowerPct,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::ReversionLimitMaxPowerPctEnable,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::LimitMaxPowerPctReversionTime,
        size: 2,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::LimitMaxPowerPctRevTimeRem,
        size: 2,
        start_address: 20,
    },
    PointDetails {
        point: |()| Point::ActivePowerEnable,
        size: 1,
        start_address: 22,
    },
    PointDetails {
        point: |()| Point::ActivePowerMode,
        size: 1,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::ActivePowerSetpointW,
        size: 2,
        start_address: 24,
    },
    PointDetails {
        point: |()| Point::ReversionActivePowerW,
        size: 2,
        start_address: 26,
    },
    PointDetails {
        point: |()| Point::ActivePowerSetpointPct,
        size: 1,
        start_address: 28,
    },
    PointDetails {
        point: |()| Point::ReversionActivePowerPct,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::ReversionActivePowerEnable,
        size: 1,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::ActivePowerReversionTime,
        size: 2,
        start_address: 31,
    },
    PointDetails {
        point: |()| Point::ActivePowerRevTimeRem,
        size: 2,
        start_address: 33,
    },
    PointDetails {
        point: |()| Point::ReactivePowerEnable,
        size: 1,
        start_address: 35,
    },
    PointDetails {
        point: |()| Point::ReactivePowerMode,
        size: 1,
        start_address: 36,
    },
    PointDetails {
        point: |()| Point::ReactivePowerPriority,
        size: 1,
        start_address: 37,
    },
    PointDetails {
        point: |()| Point::ReactivePowerSetpointVars,
        size: 2,
        start_address: 38,
    },
    PointDetails {
        point: |()| Point::ReversionReactivePowerVars,
        size: 2,
        start_address: 40,
    },
    PointDetails {
        point: |()| Point::ReactivePowerSetpointPct,
        size: 1,
        start_address: 42,
    },
    PointDetails {
        point: |()| Point::ReversionReactivePowerPct,
        size: 1,
        start_address: 43,
    },
    PointDetails {
        point: |()| Point::ReversionReactivePowerEnable,
        size: 1,
        start_address: 44,
    },
    PointDetails {
        point: |()| Point::ReactivePowerReversionTime,
        size: 2,
        start_address: 45,
    },
    PointDetails {
        point: |()| Point::ReactivePowerRevTimeRem,
        size: 2,
        start_address: 47,
    },
    PointDetails {
        point: |()| Point::NormalRampRate,
        size: 1,
        start_address: 49,
    },
    PointDetails {
        point: |()| Point::NormalRampRateReference,
        size: 1,
        start_address: 50,
    },
    PointDetails {
        point: |()| Point::ReactivePowerRampRate,
        size: 1,
        start_address: 51,
    },
    PointDetails {
        point: |()| Point::AntiIslandingEnable,
        size: 1,
        start_address: 52,
    },
    PointDetails {
        point: |()| Point::PowerFactorScaleFactor,
        size: 1,
        start_address: 53,
    },
    PointDetails {
        point: |()| Point::LimitMaxPowerScaleFactor,
        size: 1,
        start_address: 54,
    },
    PointDetails {
        point: |()| Point::ActivePowerScaleFactor,
        size: 1,
        start_address: 55,
    },
    PointDetails {
        point: |()| Point::ActivePowerPctScaleFactor,
        size: 1,
        start_address: 56,
    },
    PointDetails {
        point: |()| Point::ReactivePowerScaleFactor,
        size: 1,
        start_address: 57,
    },
    PointDetails {
        point: |()| Point::ReactivePowerPctScaleFactor,
        size: 1,
        start_address: 58,
    },
];

static PFW_INJ_POINTS: [PointDetails<()>; 2] = [
    PointDetails {
        point: |()| Point::PowerFactorWInjPowerFactorWInj,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::PowerFactorWInjPowerFactorExcitationWInj,
        size: 1,
        start_address: 1,
    },
];

static PFW_INJ_RVRT_POINTS: [PointDetails<()>; 2] = [
    PointDetails {
        point: |()| Point::ReversionPowerFactorWInjReversionPowerFactorWInj,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ReversionPowerFactorWInjReversionPfExcitationWInj,
        size: 1,
        start_address: 1,
    },
];

static PFW_ABS_POINTS: [PointDetails<()>; 2] = [
    PointDetails {
        point: |()| Point::PowerFactorWAbsPowerFactorWAbs,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::PowerFactorWAbsPowerFactorExcitationWAbs,
        size: 1,
        start_address: 1,
    },
];

static PFW_ABS_RVRT_POINTS: [PointDetails<()>; 2] = [
    PointDetails {
        point: |()| Point::ReversionPowerFactorWAbsReversionPowerFactorWAbs,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ReversionPowerFactorWAbsReversionPfExcitationWAbs,
        size: 1,
        start_address: 1,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    PowerFactorEnableWInjEnable,
    PowerFactorReversionEnableWInj,
    PfReversionTimeWInj,
    PfReversionTimeRemWInj,
    PowerFactorEnableWAbsEnable,
    PowerFactorReversionEnableWAbs,
    PfReversionTimeWAbs,
    PfReversionTimeRemWAbs,
    LimitMaxPowerPctEnable,
    LimitMaxPowerPctSetpoint,
    ReversionLimitMaxPowerPct,
    ReversionLimitMaxPowerPctEnable,
    LimitMaxPowerPctReversionTime,
    LimitMaxPowerPctRevTimeRem,
    ActivePowerEnable,
    ActivePowerMode,
    ActivePowerSetpointW,
    ReversionActivePowerW,
    ActivePowerSetpointPct,
    ReversionActivePowerPct,
    ReversionActivePowerEnable,
    ActivePowerReversionTime,
    ActivePowerRevTimeRem,
    ReactivePowerEnable,
    ReactivePowerMode,
    ReactivePowerPriority,
    ReactivePowerSetpointVars,
    ReversionReactivePowerVars,
    ReactivePowerSetpointPct,
    ReversionReactivePowerPct,
    ReversionReactivePowerEnable,
    ReactivePowerReversionTime,
    ReactivePowerRevTimeRem,
    NormalRampRate,
    NormalRampRateReference,
    ReactivePowerRampRate,
    AntiIslandingEnable,
    PowerFactorScaleFactor,
    LimitMaxPowerScaleFactor,
    ActivePowerScaleFactor,
    ActivePowerPctScaleFactor,
    ReactivePowerScaleFactor,
    ReactivePowerPctScaleFactor,
    PowerFactorWInjPowerFactorWInj,
    PowerFactorWInjPowerFactorExcitationWInj,
    ReversionPowerFactorWInjReversionPowerFactorWInj,
    ReversionPowerFactorWInjReversionPfExcitationWInj,
    PowerFactorWAbsPowerFactorWAbs,
    PowerFactorWAbsPowerFactorExcitationWAbs,
    ReversionPowerFactorWAbsReversionPowerFactorWAbs,
    ReversionPowerFactorWAbsReversionPfExcitationWAbs,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 704. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model704;

impl<'ad> ModelSpec<'ad> for Model704 {
    const MODEL_ID: u16 = 704;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        67
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                PFW_INJ_POINTS
                    .iter()
                    .map(move |p| (59 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_INJ_RVRT_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_ABS_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_ABS_RVRT_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + 2 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                PFW_INJ_POINTS
                    .iter()
                    .map(move |p| (59 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_INJ_RVRT_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_ABS_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .chain(
                PFW_ABS_RVRT_POINTS
                    .iter()
                    .map(move |p| (59 + 2 + 2 + 2 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model704,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(704);
        }
        Point::ModelLength => {
            buffer.write_u16(65);
        }
        Point::PowerFactorEnableWInjEnable => {
            if let Some(value) = adapter.power_factor_enable_w_inj_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorReversionEnableWInj => {
            if let Some(value) = adapter.power_factor_reversion_enable_w_inj() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PfReversionTimeWInj => {
            if let Some(value) = adapter.pf_reversion_time_w_inj() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::PfReversionTimeRemWInj => {
            if let Some(value) = adapter.pf_reversion_time_rem_w_inj() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorEnableWAbsEnable => {
            if let Some(value) = adapter.power_factor_enable_w_abs_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorReversionEnableWAbs => {
            if let Some(value) = adapter.power_factor_reversion_enable_w_abs() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PfReversionTimeWAbs => {
            if let Some(value) = adapter.pf_reversion_time_w_abs() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::PfReversionTimeRemWAbs => {
            if let Some(value) = adapter.pf_reversion_time_rem_w_abs() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::LimitMaxPowerPctEnable => {
            if let Some(value) = adapter.limit_max_power_pct_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::LimitMaxPowerPctSetpoint => {
            if let Some(value) = adapter.limit_max_power_pct_setpoint() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionLimitMaxPowerPct => {
            if let Some(value) = adapter.reversion_limit_max_power_pct() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionLimitMaxPowerPctEnable => {
            if let Some(value) = adapter.reversion_limit_max_power_pct_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::LimitMaxPowerPctReversionTime => {
            if let Some(value) = adapter.limit_max_power_pct_reversion_time() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::LimitMaxPowerPctRevTimeRem => {
            if let Some(value) = adapter.limit_max_power_pct_rev_time_rem() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerEnable => {
            if let Some(value) = adapter.active_power_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerMode => {
            if let Some(value) = adapter.active_power_mode() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerSetpointW => {
            if let Some(value) = adapter.active_power_setpoint_w() {
                buffer.write_i32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionActivePowerW => {
            if let Some(value) = adapter.reversion_active_power_w() {
                buffer.write_i32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerSetpointPct => {
            if let Some(value) = adapter.active_power_setpoint_pct() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionActivePowerPct => {
            if let Some(value) = adapter.reversion_active_power_pct() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionActivePowerEnable => {
            if let Some(value) = adapter.reversion_active_power_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerReversionTime => {
            if let Some(value) = adapter.active_power_reversion_time() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerRevTimeRem => {
            if let Some(value) = adapter.active_power_rev_time_rem() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerEnable => {
            if let Some(value) = adapter.reactive_power_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerMode => {
            if let Some(value) = adapter.reactive_power_mode() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerPriority => {
            if let Some(value) = adapter.reactive_power_priority() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerSetpointVars => {
            if let Some(value) = adapter.reactive_power_setpoint_vars() {
                buffer.write_i32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionReactivePowerVars => {
            if let Some(value) = adapter.reversion_reactive_power_vars() {
                buffer.write_i32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerSetpointPct => {
            if let Some(value) = adapter.reactive_power_setpoint_pct() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionReactivePowerPct => {
            if let Some(value) = adapter.reversion_reactive_power_pct() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionReactivePowerEnable => {
            if let Some(value) = adapter.reversion_reactive_power_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerReversionTime => {
            if let Some(value) = adapter.reactive_power_reversion_time() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerRevTimeRem => {
            if let Some(value) = adapter.reactive_power_rev_time_rem() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::NormalRampRate => {
            if let Some(value) = adapter.normal_ramp_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::NormalRampRateReference => {
            if let Some(value) = adapter.normal_ramp_rate_reference() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerRampRate => {
            if let Some(value) = adapter.reactive_power_ramp_rate() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AntiIslandingEnable => {
            if let Some(value) = adapter.anti_islanding_enable() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorScaleFactor => {
            if let Some(value) = adapter.power_factor_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::LimitMaxPowerScaleFactor => {
            if let Some(value) = adapter.limit_max_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerScaleFactor => {
            if let Some(value) = adapter.active_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerPctScaleFactor => {
            if let Some(value) = adapter.active_power_pct_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerScaleFactor => {
            if let Some(value) = adapter.reactive_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerPctScaleFactor => {
            if let Some(value) = adapter.reactive_power_pct_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorWInjPowerFactorWInj => {
            if let Some(value) = adapter.power_factor_w_inj_power_factor_w_inj() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorWInjPowerFactorExcitationWInj => {
            if let Some(value) = adapter.power_factor_w_inj_power_factor_excitation_w_inj() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionPowerFactorWInjReversionPowerFactorWInj => {
            if let Some(value) = adapter.reversion_power_factor_w_inj_reversion_power_factor_w_inj()
            {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionPowerFactorWInjReversionPfExcitationWInj => {
            if let Some(value) =
                adapter.reversion_power_factor_w_inj_reversion_pf_excitation_w_inj()
            {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorWAbsPowerFactorWAbs => {
            if let Some(value) = adapter.power_factor_w_abs_power_factor_w_abs() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorWAbsPowerFactorExcitationWAbs => {
            if let Some(value) = adapter.power_factor_w_abs_power_factor_excitation_w_abs() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionPowerFactorWAbsReversionPowerFactorWAbs => {
            if let Some(value) = adapter.reversion_power_factor_w_abs_reversion_power_factor_w_abs()
            {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReversionPowerFactorWAbsReversionPfExcitationWAbs => {
            if let Some(value) =
                adapter.reversion_power_factor_w_abs_reversion_pf_excitation_w_abs()
            {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model704,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::PowerFactorEnableWInjEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                PfwInjEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_enable_w_inj_enable(enum_value);
            Ok(())
        }
        Point::PowerFactorReversionEnableWInj => {
            let value = buffer.read_u16()?;
            let enum_value =
                PfwInjEnaRvrt::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_reversion_enable_w_inj(enum_value);
            Ok(())
        }
        Point::PfReversionTimeWInj => {
            let value = buffer.read_u32()?;
            adapter.set_pf_reversion_time_w_inj(value);
            Ok(())
        }
        Point::PowerFactorEnableWAbsEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                PfwAbsEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_enable_w_abs_enable(enum_value);
            Ok(())
        }
        Point::PowerFactorReversionEnableWAbs => {
            let value = buffer.read_u16()?;
            let enum_value =
                PfwAbsEnaRvrt::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_reversion_enable_w_abs(enum_value);
            Ok(())
        }
        Point::PfReversionTimeWAbs => {
            let value = buffer.read_u32()?;
            adapter.set_pf_reversion_time_w_abs(value);
            Ok(())
        }
        Point::LimitMaxPowerPctEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                WMaxLimPctEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_limit_max_power_pct_enable(enum_value);
            Ok(())
        }
        Point::LimitMaxPowerPctSetpoint => {
            let value = buffer.read_u16()?;
            adapter.set_limit_max_power_pct_setpoint(value);
            Ok(())
        }
        Point::ReversionLimitMaxPowerPct => {
            let value = buffer.read_u16()?;
            adapter.set_reversion_limit_max_power_pct(value);
            Ok(())
        }
        Point::ReversionLimitMaxPowerPctEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                WMaxLimPctEnaRvrt::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reversion_limit_max_power_pct_enable(enum_value);
            Ok(())
        }
        Point::LimitMaxPowerPctReversionTime => {
            let value = buffer.read_u32()?;
            adapter.set_limit_max_power_pct_reversion_time(value);
            Ok(())
        }
        Point::ActivePowerEnable => {
            let value = buffer.read_u16()?;
            let enum_value = WSetEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_active_power_enable(enum_value);
            Ok(())
        }
        Point::ActivePowerMode => {
            let value = buffer.read_u16()?;
            let enum_value = WSetMod::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_active_power_mode(enum_value);
            Ok(())
        }
        Point::ActivePowerSetpointW => {
            let value = buffer.read_i32()?;
            adapter.set_active_power_setpoint_w(value);
            Ok(())
        }
        Point::ReversionActivePowerW => {
            let value = buffer.read_i32()?;
            adapter.set_reversion_active_power_w(value);
            Ok(())
        }
        Point::ActivePowerSetpointPct => {
            let value = buffer.read_i16()?;
            adapter.set_active_power_setpoint_pct(value);
            Ok(())
        }
        Point::ReversionActivePowerPct => {
            let value = buffer.read_i16()?;
            adapter.set_reversion_active_power_pct(value);
            Ok(())
        }
        Point::ReversionActivePowerEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                WSetEnaRvrt::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reversion_active_power_enable(enum_value);
            Ok(())
        }
        Point::ActivePowerReversionTime => {
            let value = buffer.read_u32()?;
            adapter.set_active_power_reversion_time(value);
            Ok(())
        }
        Point::ReactivePowerEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                VarSetEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reactive_power_enable(enum_value);
            Ok(())
        }
        Point::ReactivePowerMode => {
            let value = buffer.read_u16()?;
            let enum_value =
                VarSetMod::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reactive_power_mode(enum_value);
            Ok(())
        }
        Point::ReactivePowerPriority => {
            let value = buffer.read_u16()?;
            let enum_value =
                VarSetPri::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reactive_power_priority(enum_value);
            Ok(())
        }
        Point::ReactivePowerSetpointVars => {
            let value = buffer.read_i32()?;
            adapter.set_reactive_power_setpoint_vars(value);
            Ok(())
        }
        Point::ReversionReactivePowerVars => {
            let value = buffer.read_i32()?;
            adapter.set_reversion_reactive_power_vars(value);
            Ok(())
        }
        Point::ReactivePowerSetpointPct => {
            let value = buffer.read_i16()?;
            adapter.set_reactive_power_setpoint_pct(value);
            Ok(())
        }
        Point::ReversionReactivePowerPct => {
            let value = buffer.read_i16()?;
            adapter.set_reversion_reactive_power_pct(value);
            Ok(())
        }
        Point::ReversionReactivePowerEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                VarSetEnaRvrt::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reversion_reactive_power_enable(enum_value);
            Ok(())
        }
        Point::ReactivePowerReversionTime => {
            let value = buffer.read_u32()?;
            adapter.set_reactive_power_reversion_time(value);
            Ok(())
        }
        Point::NormalRampRate => {
            let value = buffer.read_u16()?;
            adapter.set_normal_ramp_rate(value);
            Ok(())
        }
        Point::NormalRampRateReference => {
            let value = buffer.read_u16()?;
            let enum_value = WRmpRef::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_normal_ramp_rate_reference(enum_value);
            Ok(())
        }
        Point::ReactivePowerRampRate => {
            let value = buffer.read_u16()?;
            adapter.set_reactive_power_ramp_rate(value);
            Ok(())
        }
        Point::AntiIslandingEnable => {
            let value = buffer.read_u16()?;
            let enum_value =
                AntiIslEna::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_anti_islanding_enable(enum_value);
            Ok(())
        }
        Point::PowerFactorWInjPowerFactorWInj => {
            let value = buffer.read_u16()?;
            adapter.set_power_factor_w_inj_power_factor_w_inj(value);
            Ok(())
        }
        Point::PowerFactorWInjPowerFactorExcitationWInj => {
            let value = buffer.read_u16()?;
            let enum_value = Ext::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_w_inj_power_factor_excitation_w_inj(enum_value);
            Ok(())
        }
        Point::ReversionPowerFactorWInjReversionPowerFactorWInj => {
            let value = buffer.read_u16()?;
            adapter.set_reversion_power_factor_w_inj_reversion_power_factor_w_inj(value);
            Ok(())
        }
        Point::ReversionPowerFactorWInjReversionPfExcitationWInj => {
            let value = buffer.read_u16()?;
            let enum_value = Ext::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reversion_power_factor_w_inj_reversion_pf_excitation_w_inj(enum_value);
            Ok(())
        }
        Point::PowerFactorWAbsPowerFactorWAbs => {
            let value = buffer.read_u16()?;
            adapter.set_power_factor_w_abs_power_factor_w_abs(value);
            Ok(())
        }
        Point::PowerFactorWAbsPowerFactorExcitationWAbs => {
            let value = buffer.read_u16()?;
            let enum_value = Ext::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_power_factor_w_abs_power_factor_excitation_w_abs(enum_value);
            Ok(())
        }
        Point::ReversionPowerFactorWAbsReversionPowerFactorWAbs => {
            let value = buffer.read_u16()?;
            adapter.set_reversion_power_factor_w_abs_reversion_power_factor_w_abs(value);
            Ok(())
        }
        Point::ReversionPowerFactorWAbsReversionPfExcitationWAbs => {
            let value = buffer.read_u16()?;
            let enum_value = Ext::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_reversion_power_factor_w_abs_reversion_pf_excitation_w_abs(enum_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Power Factor Enable (W Inj) Enable (PFWInjEna)
    ///
    /// Power factor enable when injecting active power.
    fn power_factor_enable_w_inj_enable(&self) -> Option<PfwInjEna> {
        None
    }

    /// Power Factor Reversion Enable (W Inj) (PFWInjEnaRvrt)
    ///
    /// Power factor reversion timer when injecting active power enable.
    fn power_factor_reversion_enable_w_inj(&self) -> Option<PfwInjEnaRvrt> {
        None
    }

    /// PF Reversion Time (W Inj) (PFWInjRvrtTms)
    ///
    /// Power factor reversion timer when injecting active power.
    fn pf_reversion_time_w_inj(&self) -> Option<u32> {
        None
    }

    /// PF Reversion Time Rem (W Inj) (PFWInjRvrtRem)
    ///
    /// Power factor reversion time remaining when injecting active power.
    fn pf_reversion_time_rem_w_inj(&self) -> Option<u32> {
        None
    }

    /// Power Factor Enable (W Abs) Enable (PFWAbsEna)
    ///
    /// Power factor enable when absorbing active power.
    fn power_factor_enable_w_abs_enable(&self) -> Option<PfwAbsEna> {
        None
    }

    /// Power Factor Reversion Enable (W Abs) (PFWAbsEnaRvrt)
    ///
    /// Power factor reversion timer when absorbing active power enable.
    fn power_factor_reversion_enable_w_abs(&self) -> Option<PfwAbsEnaRvrt> {
        None
    }

    /// PF Reversion Time (W Abs) (PFWAbsRvrtTms)
    ///
    /// Power factor reversion timer when absorbing active power.
    fn pf_reversion_time_w_abs(&self) -> Option<u32> {
        None
    }

    /// PF Reversion Time Rem (W Abs) (PFWAbsRvrtRem)
    ///
    /// Power factor reversion time remaining when absorbing active power.
    fn pf_reversion_time_rem_w_abs(&self) -> Option<u32> {
        None
    }

    /// Limit Max Power Pct Enable (WMaxLimPctEna)
    ///
    /// Limit maximum active power percent enable.
    fn limit_max_power_pct_enable(&self) -> Option<WMaxLimPctEna> {
        None
    }

    /// Limit Max Power Pct Setpoint (WMaxLimPct)
    ///
    /// Limit maximum active power percent value.
    fn limit_max_power_pct_setpoint(&self) -> Option<u16> {
        None
    }

    /// Reversion Limit Max Power Pct (WMaxLimPctRvrt)
    ///
    /// Reversion limit maximum active power percent value.
    fn reversion_limit_max_power_pct(&self) -> Option<u16> {
        None
    }

    /// Reversion Limit Max Power Pct Enable (WMaxLimPctEnaRvrt)
    ///
    /// Reversion limit maximum active power percent value enable.
    fn reversion_limit_max_power_pct_enable(&self) -> Option<WMaxLimPctEnaRvrt> {
        None
    }

    /// Limit Max Power Pct Reversion Time (WMaxLimPctRvrtTms)
    ///
    /// Limit maximum active power percent reversion time.
    fn limit_max_power_pct_reversion_time(&self) -> Option<u32> {
        None
    }

    /// Limit Max Power Pct Rev Time Rem (WMaxLimPctRvrtRem)
    ///
    /// Limit maximum active power percent reversion time remaining.
    fn limit_max_power_pct_rev_time_rem(&self) -> Option<u32> {
        None
    }

    /// Set Active Power Enable (WSetEna)
    ///
    /// Set active power enable.
    fn active_power_enable(&self) -> Option<WSetEna> {
        None
    }

    /// Set Active Power Mode (WSetMod)
    ///
    /// Set active power mode.
    fn active_power_mode(&self) -> Option<WSetMod> {
        None
    }

    /// Active Power Setpoint (W) (WSet)
    ///
    /// Active power setting value in watts.
    fn active_power_setpoint_w(&self) -> Option<i32> {
        None
    }

    /// Reversion Active Power (W) (WSetRvrt)
    ///
    /// Reversion active power setting value in watts.
    fn reversion_active_power_w(&self) -> Option<i32> {
        None
    }

    /// Active Power Setpoint (Pct) (WSetPct)
    ///
    /// Active power setting value as percent.
    fn active_power_setpoint_pct(&self) -> Option<i16> {
        None
    }

    /// Reversion Active Power (Pct) (WSetPctRvrt)
    ///
    /// Reversion active power setting value as percent.
    fn reversion_active_power_pct(&self) -> Option<i16> {
        None
    }

    /// Reversion Active Power Enable (WSetEnaRvrt)
    ///
    /// Reversion active power function enable.
    fn reversion_active_power_enable(&self) -> Option<WSetEnaRvrt> {
        None
    }

    /// Active Power Reversion Time (WSetRvrtTms)
    ///
    /// Set active power reversion time.
    fn active_power_reversion_time(&self) -> Option<u32> {
        None
    }

    /// Active Power Rev Time Rem (WSetRvrtRem)
    ///
    /// Set active power reversion time remaining.
    fn active_power_rev_time_rem(&self) -> Option<u32> {
        None
    }

    /// Set Reactive Power Enable (VarSetEna)
    ///
    /// Set reactive power enable.
    fn reactive_power_enable(&self) -> Option<VarSetEna> {
        None
    }

    /// Set Reactive Power Mode (VarSetMod)
    ///
    /// Set reactive power mode.
    fn reactive_power_mode(&self) -> Option<VarSetMod> {
        None
    }

    /// Reactive Power Priority (VarSetPri)
    ///
    /// Reactive power priority.
    fn reactive_power_priority(&self) -> Option<VarSetPri> {
        None
    }

    /// Reactive Power Setpoint (Vars) (VarSet)
    ///
    /// Reactive power setting value in vars.
    fn reactive_power_setpoint_vars(&self) -> Option<i32> {
        None
    }

    /// Reversion Reactive Power (Vars) (VarSetRvrt)
    ///
    /// Reversion reactive power setting value in vars.
    fn reversion_reactive_power_vars(&self) -> Option<i32> {
        None
    }

    /// Reactive Power Setpoint (Pct) (VarSetPct)
    ///
    /// Reactive power setting value as percent.
    fn reactive_power_setpoint_pct(&self) -> Option<i16> {
        None
    }

    /// Reversion Reactive Power (Pct) (VarSetPctRvrt)
    ///
    /// Reversion reactive power setting value as percent.
    fn reversion_reactive_power_pct(&self) -> Option<i16> {
        None
    }

    /// Reversion Reactive Power Enable (VarSetEnaRvrt)
    ///
    /// Reversion reactive power function enable.
    fn reversion_reactive_power_enable(&self) -> Option<VarSetEnaRvrt> {
        None
    }

    /// Reactive Power Reversion Time (VarSetRvrtTms)
    ///
    /// Set reactive power reversion time.
    fn reactive_power_reversion_time(&self) -> Option<u32> {
        None
    }

    /// Reactive Power Rev Time Rem (VarSetRvrtRem)
    ///
    /// Set reactive power reversion time remaining.
    fn reactive_power_rev_time_rem(&self) -> Option<u32> {
        None
    }

    /// Normal Ramp Rate (WRmp)
    ///
    /// Ramp rate for increases in active power during normal generation.
    fn normal_ramp_rate(&self) -> Option<u16> {
        None
    }

    /// Normal Ramp Rate Reference (WRmpRef)
    ///
    /// Ramp rate reference unit for increases in active power or current during normal generation.
    fn normal_ramp_rate_reference(&self) -> Option<WRmpRef> {
        None
    }

    /// Reactive Power Ramp Rate (VarRmp)
    ///
    /// Ramp rate based on max reactive power per second.
    fn reactive_power_ramp_rate(&self) -> Option<u16> {
        None
    }

    /// Anti-Islanding Enable (AntiIslEna)
    ///
    /// Anti-islanding enable.
    fn anti_islanding_enable(&self) -> Option<AntiIslEna> {
        None
    }

    /// Power Factor Scale Factor (PF_SF)
    ///
    /// Power factor scale factor.
    fn power_factor_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Limit Max Power Scale Factor (WMaxLimPct_SF)
    ///
    /// Limit maximum power scale factor.
    fn limit_max_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Active Power Scale Factor (WSet_SF)
    ///
    /// Active power scale factor.
    fn active_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Active Power Pct Scale Factor (WSetPct_SF)
    ///
    /// Active power pct scale factor.
    fn active_power_pct_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Reactive Power Scale Factor (VarSet_SF)
    ///
    /// Reactive power scale factor.
    fn reactive_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Reactive Power Pct Scale Factor (VarSetPct_SF)
    ///
    /// Reactive power pct scale factor.
    fn reactive_power_pct_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Power Factor (W Inj)  (PF)
    ///
    /// Power factor setpoint when injecting active power.
    fn power_factor_w_inj_power_factor_w_inj(&self) -> Option<u16> {
        None
    }

    /// Power Factor Excitation (W Inj) (Ext)
    ///
    /// Power factor excitation setpoint when injecting active power.
    fn power_factor_w_inj_power_factor_excitation_w_inj(&self) -> Option<Ext> {
        None
    }

    /// Reversion Power Factor (W Inj)  (PF)
    ///
    /// Reversion power factor setpoint when injecting active power.
    fn reversion_power_factor_w_inj_reversion_power_factor_w_inj(&self) -> Option<u16> {
        None
    }

    /// Reversion PF Excitation (W Inj) (Ext)
    ///
    /// Reversion power factor excitation setpoint when injecting active power.
    fn reversion_power_factor_w_inj_reversion_pf_excitation_w_inj(&self) -> Option<Ext> {
        None
    }

    /// Power Factor (W Abs)  (PF)
    ///
    /// Power factor setpoint when absorbing active power.
    fn power_factor_w_abs_power_factor_w_abs(&self) -> Option<u16> {
        None
    }

    /// Power Factor Excitation (W Abs) (Ext)
    ///
    /// Power factor excitation setpoint when absorbing active power.
    fn power_factor_w_abs_power_factor_excitation_w_abs(&self) -> Option<Ext> {
        None
    }

    /// Reversion Power Factor (W Abs)  (PF)
    ///
    /// Reversion power factor setpoint when absorbing active power.
    fn reversion_power_factor_w_abs_reversion_power_factor_w_abs(&self) -> Option<u16> {
        None
    }

    /// Reversion PF Excitation (W Abs) (Ext)
    ///
    /// Reversion power factor excitation setpoint when absorbing active power.
    fn reversion_power_factor_w_abs_reversion_pf_excitation_w_abs(&self) -> Option<Ext> {
        None
    }
}

pub trait WriteAdapter {
    /// Power Factor Enable (W Inj) Enable (PFWInjEna)
    ///
    /// Power factor enable when injecting active power.
    fn set_power_factor_enable_w_inj_enable(&mut self, value: PfwInjEna) {}

    /// Power Factor Reversion Enable (W Inj) (PFWInjEnaRvrt)
    ///
    /// Power factor reversion timer when injecting active power enable.
    fn set_power_factor_reversion_enable_w_inj(&mut self, value: PfwInjEnaRvrt) {}

    /// PF Reversion Time (W Inj) (PFWInjRvrtTms)
    ///
    /// Power factor reversion timer when injecting active power.
    fn set_pf_reversion_time_w_inj(&mut self, value: u32) {}

    /// Power Factor Enable (W Abs) Enable (PFWAbsEna)
    ///
    /// Power factor enable when absorbing active power.
    fn set_power_factor_enable_w_abs_enable(&mut self, value: PfwAbsEna) {}

    /// Power Factor Reversion Enable (W Abs) (PFWAbsEnaRvrt)
    ///
    /// Power factor reversion timer when absorbing active power enable.
    fn set_power_factor_reversion_enable_w_abs(&mut self, value: PfwAbsEnaRvrt) {}

    /// PF Reversion Time (W Abs) (PFWAbsRvrtTms)
    ///
    /// Power factor reversion timer when absorbing active power.
    fn set_pf_reversion_time_w_abs(&mut self, value: u32) {}

    /// Limit Max Power Pct Enable (WMaxLimPctEna)
    ///
    /// Limit maximum active power percent enable.
    fn set_limit_max_power_pct_enable(&mut self, value: WMaxLimPctEna) {}

    /// Limit Max Power Pct Setpoint (WMaxLimPct)
    ///
    /// Limit maximum active power percent value.
    fn set_limit_max_power_pct_setpoint(&mut self, value: u16) {}

    /// Reversion Limit Max Power Pct (WMaxLimPctRvrt)
    ///
    /// Reversion limit maximum active power percent value.
    fn set_reversion_limit_max_power_pct(&mut self, value: u16) {}

    /// Reversion Limit Max Power Pct Enable (WMaxLimPctEnaRvrt)
    ///
    /// Reversion limit maximum active power percent value enable.
    fn set_reversion_limit_max_power_pct_enable(&mut self, value: WMaxLimPctEnaRvrt) {}

    /// Limit Max Power Pct Reversion Time (WMaxLimPctRvrtTms)
    ///
    /// Limit maximum active power percent reversion time.
    fn set_limit_max_power_pct_reversion_time(&mut self, value: u32) {}

    /// Set Active Power Enable (WSetEna)
    ///
    /// Set active power enable.
    fn set_active_power_enable(&mut self, value: WSetEna) {}

    /// Set Active Power Mode (WSetMod)
    ///
    /// Set active power mode.
    fn set_active_power_mode(&mut self, value: WSetMod) {}

    /// Active Power Setpoint (W) (WSet)
    ///
    /// Active power setting value in watts.
    fn set_active_power_setpoint_w(&mut self, value: i32) {}

    /// Reversion Active Power (W) (WSetRvrt)
    ///
    /// Reversion active power setting value in watts.
    fn set_reversion_active_power_w(&mut self, value: i32) {}

    /// Active Power Setpoint (Pct) (WSetPct)
    ///
    /// Active power setting value as percent.
    fn set_active_power_setpoint_pct(&mut self, value: i16) {}

    /// Reversion Active Power (Pct) (WSetPctRvrt)
    ///
    /// Reversion active power setting value as percent.
    fn set_reversion_active_power_pct(&mut self, value: i16) {}

    /// Reversion Active Power Enable (WSetEnaRvrt)
    ///
    /// Reversion active power function enable.
    fn set_reversion_active_power_enable(&mut self, value: WSetEnaRvrt) {}

    /// Active Power Reversion Time (WSetRvrtTms)
    ///
    /// Set active power reversion time.
    fn set_active_power_reversion_time(&mut self, value: u32) {}

    /// Set Reactive Power Enable (VarSetEna)
    ///
    /// Set reactive power enable.
    fn set_reactive_power_enable(&mut self, value: VarSetEna) {}

    /// Set Reactive Power Mode (VarSetMod)
    ///
    /// Set reactive power mode.
    fn set_reactive_power_mode(&mut self, value: VarSetMod) {}

    /// Reactive Power Priority (VarSetPri)
    ///
    /// Reactive power priority.
    fn set_reactive_power_priority(&mut self, value: VarSetPri) {}

    /// Reactive Power Setpoint (Vars) (VarSet)
    ///
    /// Reactive power setting value in vars.
    fn set_reactive_power_setpoint_vars(&mut self, value: i32) {}

    /// Reversion Reactive Power (Vars) (VarSetRvrt)
    ///
    /// Reversion reactive power setting value in vars.
    fn set_reversion_reactive_power_vars(&mut self, value: i32) {}

    /// Reactive Power Setpoint (Pct) (VarSetPct)
    ///
    /// Reactive power setting value as percent.
    fn set_reactive_power_setpoint_pct(&mut self, value: i16) {}

    /// Reversion Reactive Power (Pct) (VarSetPctRvrt)
    ///
    /// Reversion reactive power setting value as percent.
    fn set_reversion_reactive_power_pct(&mut self, value: i16) {}

    /// Reversion Reactive Power Enable (VarSetEnaRvrt)
    ///
    /// Reversion reactive power function enable.
    fn set_reversion_reactive_power_enable(&mut self, value: VarSetEnaRvrt) {}

    /// Reactive Power Reversion Time (VarSetRvrtTms)
    ///
    /// Set reactive power reversion time.
    fn set_reactive_power_reversion_time(&mut self, value: u32) {}

    /// Normal Ramp Rate (WRmp)
    ///
    /// Ramp rate for increases in active power during normal generation.
    fn set_normal_ramp_rate(&mut self, value: u16) {}

    /// Normal Ramp Rate Reference (WRmpRef)
    ///
    /// Ramp rate reference unit for increases in active power or current during normal generation.
    fn set_normal_ramp_rate_reference(&mut self, value: WRmpRef) {}

    /// Reactive Power Ramp Rate (VarRmp)
    ///
    /// Ramp rate based on max reactive power per second.
    fn set_reactive_power_ramp_rate(&mut self, value: u16) {}

    /// Anti-Islanding Enable (AntiIslEna)
    ///
    /// Anti-islanding enable.
    fn set_anti_islanding_enable(&mut self, value: AntiIslEna) {}

    /// Power Factor (W Inj)  (PF)
    ///
    /// Power factor setpoint when injecting active power.
    fn set_power_factor_w_inj_power_factor_w_inj(&mut self, value: u16) {}

    /// Power Factor Excitation (W Inj) (Ext)
    ///
    /// Power factor excitation setpoint when injecting active power.
    fn set_power_factor_w_inj_power_factor_excitation_w_inj(&mut self, value: Ext) {}

    /// Reversion Power Factor (W Inj)  (PF)
    ///
    /// Reversion power factor setpoint when injecting active power.
    fn set_reversion_power_factor_w_inj_reversion_power_factor_w_inj(&mut self, value: u16) {}

    /// Reversion PF Excitation (W Inj) (Ext)
    ///
    /// Reversion power factor excitation setpoint when injecting active power.
    fn set_reversion_power_factor_w_inj_reversion_pf_excitation_w_inj(&mut self, value: Ext) {}

    /// Power Factor (W Abs)  (PF)
    ///
    /// Power factor setpoint when absorbing active power.
    fn set_power_factor_w_abs_power_factor_w_abs(&mut self, value: u16) {}

    /// Power Factor Excitation (W Abs) (Ext)
    ///
    /// Power factor excitation setpoint when absorbing active power.
    fn set_power_factor_w_abs_power_factor_excitation_w_abs(&mut self, value: Ext) {}

    /// Reversion Power Factor (W Abs)  (PF)
    ///
    /// Reversion power factor setpoint when absorbing active power.
    fn set_reversion_power_factor_w_abs_reversion_power_factor_w_abs(&mut self, value: u16) {}

    /// Reversion PF Excitation (W Abs) (Ext)
    ///
    /// Reversion power factor excitation setpoint when absorbing active power.
    fn set_reversion_power_factor_w_abs_reversion_pf_excitation_w_abs(&mut self, value: Ext) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704AntiIslEna {
    /// Disabled
    ///
    /// Anti-islanding is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Anti-islanding is enabled.
    Enabled = 1,
}

impl Model704AntiIslEna {
    pub fn from_repr(repr: u16) -> Option<Model704AntiIslEna> {
        match repr {
            0 => Some(Model704AntiIslEna::Disabled),
            1 => Some(Model704AntiIslEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704AntiIslEna`] - only `Model704AntiIslEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `AntiIslEna` distinct from another model's point of the same name.
pub type AntiIslEna = Model704AntiIslEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704Ext {
    /// Over-Excited
    ///
    /// Power factor over-excited excitation.
    OverExcited = 0,
    /// Under-Excited
    ///
    /// Power factor under-excited excitation.
    UnderExcited = 1,
}

impl Model704Ext {
    pub fn from_repr(repr: u16) -> Option<Model704Ext> {
        match repr {
            0 => Some(Model704Ext::OverExcited),
            1 => Some(Model704Ext::UnderExcited),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704Ext`] - only `Model704Ext` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `Ext` distinct from another model's point of the same name.
pub type Ext = Model704Ext;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704PfwAbsEna {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704PfwAbsEna {
    pub fn from_repr(repr: u16) -> Option<Model704PfwAbsEna> {
        match repr {
            0 => Some(Model704PfwAbsEna::Disabled),
            1 => Some(Model704PfwAbsEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704PfwAbsEna`] - only `Model704PfwAbsEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `PfwAbsEna` distinct from another model's point of the same name.
pub type PfwAbsEna = Model704PfwAbsEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704PfwAbsEnaRvrt {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704PfwAbsEnaRvrt {
    pub fn from_repr(repr: u16) -> Option<Model704PfwAbsEnaRvrt> {
        match repr {
            0 => Some(Model704PfwAbsEnaRvrt::Disabled),
            1 => Some(Model704PfwAbsEnaRvrt::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704PfwAbsEnaRvrt`] - only `Model704PfwAbsEnaRvrt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `PfwAbsEnaRvrt` distinct from another model's point of the same name.
pub type PfwAbsEnaRvrt = Model704PfwAbsEnaRvrt;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704PfwInjEna {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704PfwInjEna {
    pub fn from_repr(repr: u16) -> Option<Model704PfwInjEna> {
        match repr {
            0 => Some(Model704PfwInjEna::Disabled),
            1 => Some(Model704PfwInjEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704PfwInjEna`] - only `Model704PfwInjEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `PfwInjEna` distinct from another model's point of the same name.
pub type PfwInjEna = Model704PfwInjEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704PfwInjEnaRvrt {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704PfwInjEnaRvrt {
    pub fn from_repr(repr: u16) -> Option<Model704PfwInjEnaRvrt> {
        match repr {
            0 => Some(Model704PfwInjEnaRvrt::Disabled),
            1 => Some(Model704PfwInjEnaRvrt::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704PfwInjEnaRvrt`] - only `Model704PfwInjEnaRvrt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `PfwInjEnaRvrt` distinct from another model's point of the same name.
pub type PfwInjEnaRvrt = Model704PfwInjEnaRvrt;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704VarSetEna {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704VarSetEna {
    pub fn from_repr(repr: u16) -> Option<Model704VarSetEna> {
        match repr {
            0 => Some(Model704VarSetEna::Disabled),
            1 => Some(Model704VarSetEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704VarSetEna`] - only `Model704VarSetEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `VarSetEna` distinct from another model's point of the same name.
pub type VarSetEna = Model704VarSetEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704VarSetEnaRvrt {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704VarSetEnaRvrt {
    pub fn from_repr(repr: u16) -> Option<Model704VarSetEnaRvrt> {
        match repr {
            0 => Some(Model704VarSetEnaRvrt::Disabled),
            1 => Some(Model704VarSetEnaRvrt::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704VarSetEnaRvrt`] - only `Model704VarSetEnaRvrt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `VarSetEnaRvrt` distinct from another model's point of the same name.
pub type VarSetEnaRvrt = Model704VarSetEnaRvrt;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704VarSetMod {
    /// Reactive Power As Watt Max Pct
    ///
    /// Reactive power setting is percent of maximum active power.
    WMaxPct = 0,
    /// Reactive Power As Var Max Pct
    ///
    /// Reactive power setting is percent of maximum reactive power.
    VarMaxPct = 1,
    /// Reactive Power As Var Avail Pct
    ///
    /// Reactive power setting is percent of available reactive  power.
    VarAvailPct = 2,
    /// Reactive Power As VA Max Pct
    ///
    /// Reactive power setting is percent of maximum apparent power.
    VaMaxPct = 3,
    /// Reactive Power As Vars
    ///
    /// Reactive power is in vars.
    Vars = 4,
}

impl Model704VarSetMod {
    pub fn from_repr(repr: u16) -> Option<Model704VarSetMod> {
        match repr {
            0 => Some(Model704VarSetMod::WMaxPct),
            1 => Some(Model704VarSetMod::VarMaxPct),
            2 => Some(Model704VarSetMod::VarAvailPct),
            3 => Some(Model704VarSetMod::VaMaxPct),
            4 => Some(Model704VarSetMod::Vars),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704VarSetMod`] - only `Model704VarSetMod` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `VarSetMod` distinct from another model's point of the same name.
pub type VarSetMod = Model704VarSetMod;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704VarSetPri {
    /// Active Power Priority
    ///
    /// Active power priority.
    Active = 0,
    /// Reactive Power Priority
    ///
    /// Reactive power priority.
    Reactive = 1,
    /// Vendor Power Priority
    ///
    /// Power priority is vendor specific mode.
    Vendor = 2,
}

impl Model704VarSetPri {
    pub fn from_repr(repr: u16) -> Option<Model704VarSetPri> {
        match repr {
            0 => Some(Model704VarSetPri::Active),
            1 => Some(Model704VarSetPri::Reactive),
            2 => Some(Model704VarSetPri::Vendor),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704VarSetPri`] - only `Model704VarSetPri` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `VarSetPri` distinct from another model's point of the same name.
pub type VarSetPri = Model704VarSetPri;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WMaxLimPctEna {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704WMaxLimPctEna {
    pub fn from_repr(repr: u16) -> Option<Model704WMaxLimPctEna> {
        match repr {
            0 => Some(Model704WMaxLimPctEna::Disabled),
            1 => Some(Model704WMaxLimPctEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WMaxLimPctEna`] - only `Model704WMaxLimPctEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WMaxLimPctEna` distinct from another model's point of the same name.
pub type WMaxLimPctEna = Model704WMaxLimPctEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WMaxLimPctEnaRvrt {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704WMaxLimPctEnaRvrt {
    pub fn from_repr(repr: u16) -> Option<Model704WMaxLimPctEnaRvrt> {
        match repr {
            0 => Some(Model704WMaxLimPctEnaRvrt::Disabled),
            1 => Some(Model704WMaxLimPctEnaRvrt::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WMaxLimPctEnaRvrt`] - only `Model704WMaxLimPctEnaRvrt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WMaxLimPctEnaRvrt` distinct from another model's point of the same name.
pub type WMaxLimPctEnaRvrt = Model704WMaxLimPctEnaRvrt;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WRmpRef {
    /// Max Current Ramp
    ///
    /// Ramp based on percent of max current per second.
    AMax = 0,
    /// Max Active Power Ramp
    ///
    /// Ramp based on percent of max active power per second.
    WMax = 1,
}

impl Model704WRmpRef {
    pub fn from_repr(repr: u16) -> Option<Model704WRmpRef> {
        match repr {
            0 => Some(Model704WRmpRef::AMax),
            1 => Some(Model704WRmpRef::WMax),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WRmpRef`] - only `Model704WRmpRef` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WRmpRef` distinct from another model's point of the same name.
pub type WRmpRef = Model704WRmpRef;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WSetEna {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704WSetEna {
    pub fn from_repr(repr: u16) -> Option<Model704WSetEna> {
        match repr {
            0 => Some(Model704WSetEna::Disabled),
            1 => Some(Model704WSetEna::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WSetEna`] - only `Model704WSetEna` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WSetEna` distinct from another model's point of the same name.
pub type WSetEna = Model704WSetEna;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WSetEnaRvrt {
    /// Disabled
    ///
    /// Function is disabled.
    Disabled = 0,
    /// Enabled
    ///
    /// Function is enabled.
    Enabled = 1,
}

impl Model704WSetEnaRvrt {
    pub fn from_repr(repr: u16) -> Option<Model704WSetEnaRvrt> {
        match repr {
            0 => Some(Model704WSetEnaRvrt::Disabled),
            1 => Some(Model704WSetEnaRvrt::Enabled),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WSetEnaRvrt`] - only `Model704WSetEnaRvrt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WSetEnaRvrt` distinct from another model's point of the same name.
pub type WSetEnaRvrt = Model704WSetEnaRvrt;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model704WSetMod {
    /// Active Power As Max Percent
    ///
    /// Active power setting is percentage of maximum active power.
    WMaxPct = 0,
    /// Active Power As Watts
    ///
    /// Active power setting is in watts.
    Watts = 1,
}

impl Model704WSetMod {
    pub fn from_repr(repr: u16) -> Option<Model704WSetMod> {
        match repr {
            0 => Some(Model704WSetMod::WMaxPct),
            1 => Some(Model704WSetMod::Watts),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model704WSetMod`] - only `Model704WSetMod` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `WSetMod` distinct from another model's point of the same name.
pub type WSetMod = Model704WSetMod;
