// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::{CStr, c_char, c_void};
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_19::*;

#[repr(C)]
pub struct Model19CallbackAdapter {
    context: *mut c_void,
    /// Name (Nam)
    ///
    /// Interface name
    name_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Name (Nam)
    ///
    /// Interface name
    set_name_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    rate_callback: extern "C" fn(*const c_void) -> u32,
    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    set_rate_callback: extern "C" fn(u32, *mut c_void),
    /// Bits (Bits)
    ///
    /// Number of data bits per character
    bits_callback: extern "C" fn(*const c_void) -> u16,
    /// Bits (Bits)
    ///
    /// Number of data bits per character
    set_bits_callback: extern "C" fn(u16, *mut c_void),
    /// Parity (Pty)
    ///
    /// Parity setting
    parity_callback: extern "C" fn(*const c_void) -> Model19Pty,
    /// Parity (Pty)
    ///
    /// Parity setting
    set_parity_callback: extern "C" fn(Model19Pty, *mut c_void),
    /// Duplex (Dup)
    ///
    /// Duplex mode
    duplex_callback: Option<extern "C" fn(*const c_void) -> Model19Dup>,
    /// Duplex (Dup)
    ///
    /// Duplex mode
    set_duplex_callback: Option<extern "C" fn(Model19Dup, *mut c_void)>,
    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    flow_control_callback: Option<extern "C" fn(*const c_void) -> Model19Flw>,
    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    set_flow_control_callback: Option<extern "C" fn(Model19Flw, *mut c_void)>,
    /// Authentication (Auth)
    ///
    /// Authentication method
    authentication_callback: Option<extern "C" fn(*const c_void) -> Model19Auth>,
    /// Username (UsrNam)
    ///
    /// Username for authentication
    username_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Password (Pw)
    ///
    /// Password for authentication
    password_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
}

impl ReadAdapter for Model19CallbackAdapter {
    fn name(&self) -> Option<&CStr> {
        self.name_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn rate(&self) -> u32 {
        (self.rate_callback)(self.context)
    }

    fn bits(&self) -> u16 {
        (self.bits_callback)(self.context)
    }

    fn parity(&self) -> Pty {
        (self.parity_callback)(self.context)
    }

    fn duplex(&self) -> Option<Dup> {
        self.duplex_callback
            .map(|callback| (callback)(self.context))
    }

    fn flow_control(&self) -> Option<Flw> {
        self.flow_control_callback
            .map(|callback| (callback)(self.context))
    }

    fn authentication(&self) -> Option<Auth> {
        self.authentication_callback
            .map(|callback| (callback)(self.context))
    }

    fn username(&self) -> Option<&CStr> {
        self.username_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn password(&self) -> Option<&CStr> {
        self.password_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }
}

impl WriteAdapter for Model19CallbackAdapter {
    /// Name (Nam)
    ///
    /// Interface name
    fn set_name(&mut self, value: &CStr) {
        if let Some(callback) = self.set_name_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }

    /// Rate (Rte)
    ///
    /// Interface baud rate in bits per second
    fn set_rate(&mut self, value: u32) {
        (self.set_rate_callback)(value, self.context);
    }

    /// Bits (Bits)
    ///
    /// Number of data bits per character
    fn set_bits(&mut self, value: u16) {
        (self.set_bits_callback)(value, self.context);
    }

    /// Parity (Pty)
    ///
    /// Parity setting
    fn set_parity(&mut self, value: Pty) {
        (self.set_parity_callback)(value, self.context);
    }

    /// Duplex (Dup)
    ///
    /// Duplex mode
    fn set_duplex(&mut self, value: Dup) {
        if let Some(callback) = self.set_duplex_callback {
            (callback)(value, self.context);
        };
    }

    /// Flow Control (Flw)
    ///
    /// Flow Control Method
    fn set_flow_control(&mut self, value: Flw) {
        if let Some(callback) = self.set_flow_control_callback {
            (callback)(value, self.context);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 19. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_19: StaticModelSpec = StaticModelSpec {
    id: 19,
    length: model_19_c_length,
    writable: true,
    visit_read: model_19_c_visit_read,
    visit_write: model_19_c_visit_write,
};

fn model_19_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model19).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model19CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_19_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model19;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model19CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_19_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_19_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model19;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model19CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 19 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_19_callback(
    adapter: *mut Model19CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_19,
        adapter: adapter as *mut c_void,
    }
}
