// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model
// definitions in https://github.com/sunspec/models. Do not edit by hand - run
// `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 51] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::ActivePowerMaxRating,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::ActivePowerOverExcitedRating,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::WOvrExtRtgPf,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::ActivePowerUnderExcitedRating,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::WUndExtRtgPf,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::ApparentPowerMaxRating,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::ReactivePowerInjectedRating,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::ReactivePowerAbsorbedRating,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::ChargeRateMaxRating,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::DischargeRateMaxRating,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::ChargeRateMaxVaRating,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::DischargeRateMaxVaRating,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::AcVoltageNominalRating,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::AcVoltageMaxRating,
        size: 1,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::AcVoltageMinRating,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::AcCurrentMaxRating,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::PfOverExcitedRatingUnused,
        size: 1,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::PfUnderExcitedRatingUnused,
        size: 1,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::ReactiveSusceptance,
        size: 1,
        start_address: 20,
    },
    PointDetails {
        point: |()| Point::NormalOperatingCategory,
        size: 1,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::AbnormalOperatingCategory,
        size: 1,
        start_address: 22,
    },
    PointDetails {
        point: |()| Point::SupportedControlModes,
        size: 2,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::IntIslandCatRtg,
        size: 1,
        start_address: 25,
    },
    PointDetails {
        point: |()| Point::ActivePowerMaxSetting,
        size: 1,
        start_address: 26,
    },
    PointDetails {
        point: |()| Point::ActivePowerOverExcitedSetting,
        size: 1,
        start_address: 27,
    },
    PointDetails {
        point: |()| Point::WOvrExtPf,
        size: 1,
        start_address: 28,
    },
    PointDetails {
        point: |()| Point::ActivePowerUnderExcitedSetting,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::WUndExtPf,
        size: 1,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::ApparentPowerMaxSetting,
        size: 1,
        start_address: 31,
    },
    PointDetails {
        point: |()| Point::ReactivePowerInjectedSetting,
        size: 1,
        start_address: 32,
    },
    PointDetails {
        point: |()| Point::ReactivePowerAbsorbedSetting,
        size: 1,
        start_address: 33,
    },
    PointDetails {
        point: |()| Point::ChargeRateMaxSetting,
        size: 1,
        start_address: 34,
    },
    PointDetails {
        point: |()| Point::DischargeRateMaxSetting,
        size: 1,
        start_address: 35,
    },
    PointDetails {
        point: |()| Point::ChargeRateMaxVaSetting,
        size: 1,
        start_address: 36,
    },
    PointDetails {
        point: |()| Point::DischargeRateMaxVaSetting,
        size: 1,
        start_address: 37,
    },
    PointDetails {
        point: |()| Point::NominalAcVoltageSetting,
        size: 1,
        start_address: 38,
    },
    PointDetails {
        point: |()| Point::AcVoltageMaxSetting,
        size: 1,
        start_address: 39,
    },
    PointDetails {
        point: |()| Point::AcVoltageMinSetting,
        size: 1,
        start_address: 40,
    },
    PointDetails {
        point: |()| Point::AcCurrentMaxSetting,
        size: 1,
        start_address: 41,
    },
    PointDetails {
        point: |()| Point::PfOverExcitedSettingUnused,
        size: 1,
        start_address: 42,
    },
    PointDetails {
        point: |()| Point::PfUnderExcitedSettingUnused,
        size: 1,
        start_address: 43,
    },
    PointDetails {
        point: |()| Point::IntIslandCat,
        size: 1,
        start_address: 44,
    },
    PointDetails {
        point: |()| Point::ActivePowerScaleFactor,
        size: 1,
        start_address: 45,
    },
    PointDetails {
        point: |()| Point::PowerFactorScaleFactor,
        size: 1,
        start_address: 46,
    },
    PointDetails {
        point: |()| Point::ApparentPowerScaleFactor,
        size: 1,
        start_address: 47,
    },
    PointDetails {
        point: |()| Point::ReactivePowerScaleFactor,
        size: 1,
        start_address: 48,
    },
    PointDetails {
        point: |()| Point::VoltageScaleFactor,
        size: 1,
        start_address: 49,
    },
    PointDetails {
        point: |()| Point::CurrentScaleFactor,
        size: 1,
        start_address: 50,
    },
    PointDetails {
        point: |()| Point::SusceptanceScaleFactor,
        size: 1,
        start_address: 51,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    ActivePowerMaxRating,
    ActivePowerOverExcitedRating,
    WOvrExtRtgPf,
    ActivePowerUnderExcitedRating,
    WUndExtRtgPf,
    ApparentPowerMaxRating,
    ReactivePowerInjectedRating,
    ReactivePowerAbsorbedRating,
    ChargeRateMaxRating,
    DischargeRateMaxRating,
    ChargeRateMaxVaRating,
    DischargeRateMaxVaRating,
    AcVoltageNominalRating,
    AcVoltageMaxRating,
    AcVoltageMinRating,
    AcCurrentMaxRating,
    PfOverExcitedRatingUnused,
    PfUnderExcitedRatingUnused,
    ReactiveSusceptance,
    NormalOperatingCategory,
    AbnormalOperatingCategory,
    SupportedControlModes,
    IntIslandCatRtg,
    ActivePowerMaxSetting,
    ActivePowerOverExcitedSetting,
    WOvrExtPf,
    ActivePowerUnderExcitedSetting,
    WUndExtPf,
    ApparentPowerMaxSetting,
    ReactivePowerInjectedSetting,
    ReactivePowerAbsorbedSetting,
    ChargeRateMaxSetting,
    DischargeRateMaxSetting,
    ChargeRateMaxVaSetting,
    DischargeRateMaxVaSetting,
    NominalAcVoltageSetting,
    AcVoltageMaxSetting,
    AcVoltageMinSetting,
    AcCurrentMaxSetting,
    PfOverExcitedSettingUnused,
    PfUnderExcitedSettingUnused,
    IntIslandCat,
    ActivePowerScaleFactor,
    PowerFactorScaleFactor,
    ApparentPowerScaleFactor,
    ReactivePowerScaleFactor,
    VoltageScaleFactor,
    CurrentScaleFactor,
    SusceptanceScaleFactor,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 702. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model702;

impl<'ad> ModelSpec<'ad> for Model702 {
    const MODEL_ID: u16 = 702;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        52
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model702,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(702);
        }
        Point::ModelLength => {
            buffer.write_u16(50);
        }
        Point::ActivePowerMaxRating => {
            if let Some(value) = adapter.active_power_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerOverExcitedRating => {
            if let Some(value) = adapter.active_power_over_excited_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WOvrExtRtgPf => {
            if let Some(value) = adapter.w_ovr_ext_rtg_pf() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerUnderExcitedRating => {
            if let Some(value) = adapter.active_power_under_excited_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WUndExtRtgPf => {
            if let Some(value) = adapter.w_und_ext_rtg_pf() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ApparentPowerMaxRating => {
            if let Some(value) = adapter.apparent_power_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerInjectedRating => {
            if let Some(value) = adapter.reactive_power_injected_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerAbsorbedRating => {
            if let Some(value) = adapter.reactive_power_absorbed_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChargeRateMaxRating => {
            if let Some(value) = adapter.charge_rate_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DischargeRateMaxRating => {
            if let Some(value) = adapter.discharge_rate_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChargeRateMaxVaRating => {
            if let Some(value) = adapter.charge_rate_max_va_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DischargeRateMaxVaRating => {
            if let Some(value) = adapter.discharge_rate_max_va_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcVoltageNominalRating => {
            if let Some(value) = adapter.ac_voltage_nominal_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcVoltageMaxRating => {
            if let Some(value) = adapter.ac_voltage_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcVoltageMinRating => {
            if let Some(value) = adapter.ac_voltage_min_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcCurrentMaxRating => {
            if let Some(value) = adapter.ac_current_max_rating() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfOverExcitedRatingUnused => {
            if let Some(value) = adapter.pf_over_excited_rating_unused() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfUnderExcitedRatingUnused => {
            if let Some(value) = adapter.pf_under_excited_rating_unused() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactiveSusceptance => {
            if let Some(value) = adapter.reactive_susceptance() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::NormalOperatingCategory => {
            if let Some(value) = adapter.normal_operating_category() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::AbnormalOperatingCategory => {
            if let Some(value) = adapter.abnormal_operating_category() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::SupportedControlModes => {
            if let Some(value) = adapter.supported_control_modes() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::IntIslandCatRtg => {
            if let Some(value) = adapter.int_island_cat_rtg() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerMaxSetting => {
            if let Some(value) = adapter.active_power_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerOverExcitedSetting => {
            if let Some(value) = adapter.active_power_over_excited_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WOvrExtPf => {
            if let Some(value) = adapter.w_ovr_ext_pf() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerUnderExcitedSetting => {
            if let Some(value) = adapter.active_power_under_excited_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WUndExtPf => {
            if let Some(value) = adapter.w_und_ext_pf() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ApparentPowerMaxSetting => {
            if let Some(value) = adapter.apparent_power_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerInjectedSetting => {
            if let Some(value) = adapter.reactive_power_injected_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerAbsorbedSetting => {
            if let Some(value) = adapter.reactive_power_absorbed_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChargeRateMaxSetting => {
            if let Some(value) = adapter.charge_rate_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DischargeRateMaxSetting => {
            if let Some(value) = adapter.discharge_rate_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChargeRateMaxVaSetting => {
            if let Some(value) = adapter.charge_rate_max_va_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DischargeRateMaxVaSetting => {
            if let Some(value) = adapter.discharge_rate_max_va_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::NominalAcVoltageSetting => {
            if let Some(value) = adapter.nominal_ac_voltage_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcVoltageMaxSetting => {
            if let Some(value) = adapter.ac_voltage_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcVoltageMinSetting => {
            if let Some(value) = adapter.ac_voltage_min_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::AcCurrentMaxSetting => {
            if let Some(value) = adapter.ac_current_max_setting() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfOverExcitedSettingUnused => {
            if let Some(value) = adapter.pf_over_excited_setting_unused() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfUnderExcitedSettingUnused => {
            if let Some(value) = adapter.pf_under_excited_setting_unused() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::IntIslandCat => {
            if let Some(value) = adapter.int_island_cat() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ActivePowerScaleFactor => {
            if let Some(value) = adapter.active_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PowerFactorScaleFactor => {
            if let Some(value) = adapter.power_factor_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ApparentPowerScaleFactor => {
            if let Some(value) = adapter.apparent_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ReactivePowerScaleFactor => {
            if let Some(value) = adapter.reactive_power_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VoltageScaleFactor => {
            if let Some(value) = adapter.voltage_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::CurrentScaleFactor => {
            if let Some(value) = adapter.current_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::SusceptanceScaleFactor => {
            if let Some(value) = adapter.susceptance_scale_factor() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model702,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::ActivePowerMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_active_power_max_setting(value);
            Ok(())
        }
        Point::ActivePowerOverExcitedSetting => {
            let value = buffer.read_u16()?;
            adapter.set_active_power_over_excited_setting(value);
            Ok(())
        }
        Point::WOvrExtPf => {
            let value = buffer.read_u16()?;
            adapter.set_w_ovr_ext_pf(value);
            Ok(())
        }
        Point::ActivePowerUnderExcitedSetting => {
            let value = buffer.read_u16()?;
            adapter.set_active_power_under_excited_setting(value);
            Ok(())
        }
        Point::WUndExtPf => {
            let value = buffer.read_u16()?;
            adapter.set_w_und_ext_pf(value);
            Ok(())
        }
        Point::ApparentPowerMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_apparent_power_max_setting(value);
            Ok(())
        }
        Point::ReactivePowerInjectedSetting => {
            let value = buffer.read_u16()?;
            adapter.set_reactive_power_injected_setting(value);
            Ok(())
        }
        Point::ReactivePowerAbsorbedSetting => {
            let value = buffer.read_u16()?;
            adapter.set_reactive_power_absorbed_setting(value);
            Ok(())
        }
        Point::ChargeRateMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_charge_rate_max_setting(value);
            Ok(())
        }
        Point::DischargeRateMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_discharge_rate_max_setting(value);
            Ok(())
        }
        Point::ChargeRateMaxVaSetting => {
            let value = buffer.read_u16()?;
            adapter.set_charge_rate_max_va_setting(value);
            Ok(())
        }
        Point::DischargeRateMaxVaSetting => {
            let value = buffer.read_u16()?;
            adapter.set_discharge_rate_max_va_setting(value);
            Ok(())
        }
        Point::NominalAcVoltageSetting => {
            let value = buffer.read_u16()?;
            adapter.set_nominal_ac_voltage_setting(value);
            Ok(())
        }
        Point::AcVoltageMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_ac_voltage_max_setting(value);
            Ok(())
        }
        Point::AcVoltageMinSetting => {
            let value = buffer.read_u16()?;
            adapter.set_ac_voltage_min_setting(value);
            Ok(())
        }
        Point::AcCurrentMaxSetting => {
            let value = buffer.read_u16()?;
            adapter.set_ac_current_max_setting(value);
            Ok(())
        }
        Point::PfOverExcitedSettingUnused => {
            let value = buffer.read_u16()?;
            adapter.set_pf_over_excited_setting_unused(value);
            Ok(())
        }
        Point::PfUnderExcitedSettingUnused => {
            let value = buffer.read_u16()?;
            adapter.set_pf_under_excited_setting_unused(value);
            Ok(())
        }
        Point::IntIslandCat => {
            let value = buffer.read_u16()?;
            adapter.set_int_island_cat(value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// Active Power Max Rating (WMaxRtg)
    ///
    /// Maximum active power rating at unity power factor in watts.
    fn active_power_max_rating(&self) -> Option<u16> {
        None
    }

    /// Active Power (Over-Excited) Rating (WOvrExtRtg)
    ///
    /// Active power rating at specified over-excited power factor in watts.
    fn active_power_over_excited_rating(&self) -> Option<u16> {
        None
    }

    /// Specified Over-Excited PF (WOvrExtRtgPF)
    ///
    /// Specified over-excited power factor.
    fn w_ovr_ext_rtg_pf(&self) -> Option<u16> {
        None
    }

    /// Active Power (Under-Excited) Rating (WUndExtRtg)
    ///
    /// Active power rating at specified under-excited power factor in watts.
    fn active_power_under_excited_rating(&self) -> Option<u16> {
        None
    }

    /// Specified Under-Excited PF (WUndExtRtgPF)
    ///
    /// Specified under-excited power factor.
    fn w_und_ext_rtg_pf(&self) -> Option<u16> {
        None
    }

    /// Apparent Power Max Rating (VAMaxRtg)
    ///
    /// Maximum apparent power rating in voltamperes.
    fn apparent_power_max_rating(&self) -> Option<u16> {
        None
    }

    /// Reactive Power Injected Rating (VarMaxInjRtg)
    ///
    /// Maximum injected reactive power rating in vars.
    fn reactive_power_injected_rating(&self) -> Option<u16> {
        None
    }

    /// Reactive Power Absorbed Rating (VarMaxAbsRtg)
    ///
    /// Maximum absorbed reactive power rating in vars.
    fn reactive_power_absorbed_rating(&self) -> Option<u16> {
        None
    }

    /// Charge Rate Max Rating (WChaRteMaxRtg)
    ///
    /// Maximum active power charge rate in watts.
    fn charge_rate_max_rating(&self) -> Option<u16> {
        None
    }

    /// Discharge Rate Max Rating (WDisChaRteMaxRtg)
    ///
    /// Maximum active power discharge rate in watts.
    fn discharge_rate_max_rating(&self) -> Option<u16> {
        None
    }

    /// Charge Rate Max VA Rating (VAChaRteMaxRtg)
    ///
    /// Maximum apparent power charge rate in voltamperes.
    fn charge_rate_max_va_rating(&self) -> Option<u16> {
        None
    }

    /// Discharge Rate Max VA Rating (VADisChaRteMaxRtg)
    ///
    /// Maximum apparent power discharge rate in voltamperes.
    fn discharge_rate_max_va_rating(&self) -> Option<u16> {
        None
    }

    /// AC Voltage Nominal Rating (VNomRtg)
    ///
    /// AC voltage nominal rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn ac_voltage_nominal_rating(&self) -> Option<u16> {
        None
    }

    /// AC Voltage Max Rating (VMaxRtg)
    ///
    /// AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn ac_voltage_max_rating(&self) -> Option<u16> {
        None
    }

    /// AC Voltage Min Rating (VMinRtg)
    ///
    /// AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn ac_voltage_min_rating(&self) -> Option<u16> {
        None
    }

    /// AC Current Max Rating (AMaxRtg)
    ///
    /// AC current maximum rating in amps.
    fn ac_current_max_rating(&self) -> Option<u16> {
        None
    }

    /// PF Over-Excited Rating (Unused) (PFOvrExtRtg)
    ///
    /// Unused. Please use WOvrExtRtgPF.
    ///
    /// This point is duplicative of WOvrExtRtgPF.
    fn pf_over_excited_rating_unused(&self) -> Option<u16> {
        None
    }

    /// PF Under-Excited Rating (Unused) (PFUndExtRtg)
    ///
    /// Unused. Please use WUndExtRtgPF.
    ///
    /// This point is duplicative of WUndExtRtgPF.
    fn pf_under_excited_rating_unused(&self) -> Option<u16> {
        None
    }

    /// Reactive Susceptance (ReactSusceptRtg)
    ///
    /// Reactive susceptance that remains connected to the Area EPS in the cease to energize and trip state.
    fn reactive_susceptance(&self) -> Option<u16> {
        None
    }

    /// Normal Operating Category (NorOpCatRtg)
    ///
    /// Normal operating performance category as specified in IEEE 1547-2018.
    fn normal_operating_category(&self) -> Option<NorOpCatRtg> {
        None
    }

    /// Abnormal Operating Category (AbnOpCatRtg)
    ///
    /// Abnormal operating performance category as specified in IEEE 1547-2018.
    fn abnormal_operating_category(&self) -> Option<AbnOpCatRtg> {
        None
    }

    /// Supported Control Modes (CtrlModes)
    ///
    /// Supported control mode functions.
    fn supported_control_modes(&self) -> Option<u32> {
        None
    }

    /// Intentional Island Categories (IntIslandCatRtg)
    ///
    /// Intentional island categories.
    fn int_island_cat_rtg(&self) -> Option<u16> {
        None
    }

    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    fn active_power_max_setting(&self) -> Option<u16> {
        None
    }

    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    fn active_power_over_excited_setting(&self) -> Option<u16> {
        None
    }

    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    fn w_ovr_ext_pf(&self) -> Option<u16> {
        None
    }

    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    fn active_power_under_excited_setting(&self) -> Option<u16> {
        None
    }

    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    fn w_und_ext_pf(&self) -> Option<u16> {
        None
    }

    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    fn apparent_power_max_setting(&self) -> Option<u16> {
        None
    }

    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    fn reactive_power_injected_setting(&self) -> Option<u16> {
        None
    }

    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    fn reactive_power_absorbed_setting(&self) -> Option<u16> {
        None
    }

    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    fn charge_rate_max_setting(&self) -> Option<u16> {
        None
    }

    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    fn discharge_rate_max_setting(&self) -> Option<u16> {
        None
    }

    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    fn charge_rate_max_va_setting(&self) -> Option<u16> {
        None
    }

    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    fn discharge_rate_max_va_setting(&self) -> Option<u16> {
        None
    }

    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn nominal_ac_voltage_setting(&self) -> Option<u16> {
        None
    }

    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn ac_voltage_max_setting(&self) -> Option<u16> {
        None
    }

    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn ac_voltage_min_setting(&self) -> Option<u16> {
        None
    }

    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    fn ac_current_max_setting(&self) -> Option<u16> {
        None
    }

    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    fn pf_over_excited_setting_unused(&self) -> Option<u16> {
        None
    }

    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    fn pf_under_excited_setting_unused(&self) -> Option<u16> {
        None
    }

    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    fn int_island_cat(&self) -> Option<u16> {
        None
    }

    /// Active Power Scale Factor (W_SF)
    ///
    /// Active power scale factor.
    fn active_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Power Factor Scale Factor (PF_SF)
    ///
    /// Power factor scale factor.
    fn power_factor_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Apparent Power Scale Factor (VA_SF)
    ///
    /// Apparent power scale factor.
    fn apparent_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Reactive Power Scale Factor (Var_SF)
    ///
    /// Reactive power scale factor.
    fn reactive_power_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Voltage Scale Factor (V_SF)
    ///
    /// Voltage scale factor.
    fn voltage_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Current Scale Factor (A_SF)
    ///
    /// Current scale factor.
    fn current_scale_factor(&self) -> Option<i16> {
        None
    }

    /// Susceptance Scale Factor (S_SF)
    ///
    /// Susceptance scale factor.
    fn susceptance_scale_factor(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    fn set_active_power_max_setting(&mut self, value: u16) {}

    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    fn set_active_power_over_excited_setting(&mut self, value: u16) {}

    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    fn set_w_ovr_ext_pf(&mut self, value: u16) {}

    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    fn set_active_power_under_excited_setting(&mut self, value: u16) {}

    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    fn set_w_und_ext_pf(&mut self, value: u16) {}

    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    fn set_apparent_power_max_setting(&mut self, value: u16) {}

    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    fn set_reactive_power_injected_setting(&mut self, value: u16) {}

    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    fn set_reactive_power_absorbed_setting(&mut self, value: u16) {}

    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    fn set_charge_rate_max_setting(&mut self, value: u16) {}

    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    fn set_discharge_rate_max_setting(&mut self, value: u16) {}

    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    fn set_charge_rate_max_va_setting(&mut self, value: u16) {}

    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    fn set_discharge_rate_max_va_setting(&mut self, value: u16) {}

    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_nominal_ac_voltage_setting(&mut self, value: u16) {}

    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_max_setting(&mut self, value: u16) {}

    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_min_setting(&mut self, value: u16) {}

    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    fn set_ac_current_max_setting(&mut self, value: u16) {}

    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    fn set_pf_over_excited_setting_unused(&mut self, value: u16) {}

    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    fn set_pf_under_excited_setting_unused(&mut self, value: u16) {}

    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    fn set_int_island_cat(&mut self, value: u16) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model702AbnOpCatRtg {
    /// Category I
    Cat1 = 0,
    /// Category II
    Cat2 = 1,
    /// Category III
    Cat3 = 2,
}

impl Model702AbnOpCatRtg {
    pub fn from_repr(repr: u16) -> Option<Model702AbnOpCatRtg> {
        match repr {
            0 => Some(Model702AbnOpCatRtg::Cat1),
            1 => Some(Model702AbnOpCatRtg::Cat2),
            2 => Some(Model702AbnOpCatRtg::Cat3),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model702AbnOpCatRtg`] - only `Model702AbnOpCatRtg` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `AbnOpCatRtg` distinct from another model's point of the same name.
pub type AbnOpCatRtg = Model702AbnOpCatRtg;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model702NorOpCatRtg {
    /// Category A
    CatA = 0,
    /// Category B
    CatB = 1,
}

impl Model702NorOpCatRtg {
    pub fn from_repr(repr: u16) -> Option<Model702NorOpCatRtg> {
        match repr {
            0 => Some(Model702NorOpCatRtg::CatA),
            1 => Some(Model702NorOpCatRtg::CatB),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model702NorOpCatRtg`] - only `Model702NorOpCatRtg` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `NorOpCatRtg` distinct from another model's point of the same name.
pub type NorOpCatRtg = Model702NorOpCatRtg;

#[repr(C)]
pub struct Model702CallbackAdapter {
    context: *mut c_void,
    /// Active Power Max Rating (WMaxRtg)
    ///
    /// Maximum active power rating at unity power factor in watts.
    active_power_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power (Over-Excited) Rating (WOvrExtRtg)
    ///
    /// Active power rating at specified over-excited power factor in watts.
    active_power_over_excited_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Specified Over-Excited PF (WOvrExtRtgPF)
    ///
    /// Specified over-excited power factor.
    w_ovr_ext_rtg_pf_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power (Under-Excited) Rating (WUndExtRtg)
    ///
    /// Active power rating at specified under-excited power factor in watts.
    active_power_under_excited_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Specified Under-Excited PF (WUndExtRtgPF)
    ///
    /// Specified under-excited power factor.
    w_und_ext_rtg_pf_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Apparent Power Max Rating (VAMaxRtg)
    ///
    /// Maximum apparent power rating in voltamperes.
    apparent_power_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Reactive Power Injected Rating (VarMaxInjRtg)
    ///
    /// Maximum injected reactive power rating in vars.
    reactive_power_injected_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Reactive Power Absorbed Rating (VarMaxAbsRtg)
    ///
    /// Maximum absorbed reactive power rating in vars.
    reactive_power_absorbed_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Charge Rate Max Rating (WChaRteMaxRtg)
    ///
    /// Maximum active power charge rate in watts.
    charge_rate_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Discharge Rate Max Rating (WDisChaRteMaxRtg)
    ///
    /// Maximum active power discharge rate in watts.
    discharge_rate_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Charge Rate Max VA Rating (VAChaRteMaxRtg)
    ///
    /// Maximum apparent power charge rate in voltamperes.
    charge_rate_max_va_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Discharge Rate Max VA Rating (VADisChaRteMaxRtg)
    ///
    /// Maximum apparent power discharge rate in voltamperes.
    discharge_rate_max_va_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Voltage Nominal Rating (VNomRtg)
    ///
    /// AC voltage nominal rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    ac_voltage_nominal_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Voltage Max Rating (VMaxRtg)
    ///
    /// AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    ac_voltage_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Voltage Min Rating (VMinRtg)
    ///
    /// AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    ac_voltage_min_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Current Max Rating (AMaxRtg)
    ///
    /// AC current maximum rating in amps.
    ac_current_max_rating_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// PF Over-Excited Rating (Unused) (PFOvrExtRtg)
    ///
    /// Unused. Please use WOvrExtRtgPF.
    ///
    /// This point is duplicative of WOvrExtRtgPF.
    pf_over_excited_rating_unused_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// PF Under-Excited Rating (Unused) (PFUndExtRtg)
    ///
    /// Unused. Please use WUndExtRtgPF.
    ///
    /// This point is duplicative of WUndExtRtgPF.
    pf_under_excited_rating_unused_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Reactive Susceptance (ReactSusceptRtg)
    ///
    /// Reactive susceptance that remains connected to the Area EPS in the cease to energize and trip state.
    reactive_susceptance_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Normal Operating Category (NorOpCatRtg)
    ///
    /// Normal operating performance category as specified in IEEE 1547-2018.
    normal_operating_category_callback: Option<extern "C" fn(*const c_void) -> Model702NorOpCatRtg>,
    /// Abnormal Operating Category (AbnOpCatRtg)
    ///
    /// Abnormal operating performance category as specified in IEEE 1547-2018.
    abnormal_operating_category_callback:
        Option<extern "C" fn(*const c_void) -> Model702AbnOpCatRtg>,
    /// Supported Control Modes (CtrlModes)
    ///
    /// Supported control mode functions.
    supported_control_modes_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Intentional Island Categories (IntIslandCatRtg)
    ///
    /// Intentional island categories.
    int_island_cat_rtg_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    active_power_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    set_active_power_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    active_power_over_excited_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    set_active_power_over_excited_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    w_ovr_ext_pf_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    set_w_ovr_ext_pf_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    active_power_under_excited_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    set_active_power_under_excited_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    w_und_ext_pf_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    set_w_und_ext_pf_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    apparent_power_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    set_apparent_power_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    reactive_power_injected_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    set_reactive_power_injected_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    reactive_power_absorbed_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    set_reactive_power_absorbed_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    charge_rate_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    set_charge_rate_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    discharge_rate_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    set_discharge_rate_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    charge_rate_max_va_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    set_charge_rate_max_va_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    discharge_rate_max_va_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    set_discharge_rate_max_va_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    nominal_ac_voltage_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    set_nominal_ac_voltage_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    ac_voltage_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    set_ac_voltage_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    ac_voltage_min_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    set_ac_voltage_min_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    ac_current_max_setting_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    set_ac_current_max_setting_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    pf_over_excited_setting_unused_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    set_pf_over_excited_setting_unused_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    pf_under_excited_setting_unused_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    set_pf_under_excited_setting_unused_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    int_island_cat_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    set_int_island_cat_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Active Power Scale Factor (W_SF)
    ///
    /// Active power scale factor.
    active_power_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Power Factor Scale Factor (PF_SF)
    ///
    /// Power factor scale factor.
    power_factor_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Apparent Power Scale Factor (VA_SF)
    ///
    /// Apparent power scale factor.
    apparent_power_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Reactive Power Scale Factor (Var_SF)
    ///
    /// Reactive power scale factor.
    reactive_power_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Voltage Scale Factor (V_SF)
    ///
    /// Voltage scale factor.
    voltage_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Current Scale Factor (A_SF)
    ///
    /// Current scale factor.
    current_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Susceptance Scale Factor (S_SF)
    ///
    /// Susceptance scale factor.
    susceptance_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model702CallbackAdapter {
    fn active_power_max_rating(&self) -> Option<u16> {
        self.active_power_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_over_excited_rating(&self) -> Option<u16> {
        self.active_power_over_excited_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn w_ovr_ext_rtg_pf(&self) -> Option<u16> {
        self.w_ovr_ext_rtg_pf_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_under_excited_rating(&self) -> Option<u16> {
        self.active_power_under_excited_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn w_und_ext_rtg_pf(&self) -> Option<u16> {
        self.w_und_ext_rtg_pf_callback
            .map(|callback| (callback)(self.context))
    }

    fn apparent_power_max_rating(&self) -> Option<u16> {
        self.apparent_power_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_power_injected_rating(&self) -> Option<u16> {
        self.reactive_power_injected_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_power_absorbed_rating(&self) -> Option<u16> {
        self.reactive_power_absorbed_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn charge_rate_max_rating(&self) -> Option<u16> {
        self.charge_rate_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn discharge_rate_max_rating(&self) -> Option<u16> {
        self.discharge_rate_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn charge_rate_max_va_rating(&self) -> Option<u16> {
        self.charge_rate_max_va_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn discharge_rate_max_va_rating(&self) -> Option<u16> {
        self.discharge_rate_max_va_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_voltage_nominal_rating(&self) -> Option<u16> {
        self.ac_voltage_nominal_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_voltage_max_rating(&self) -> Option<u16> {
        self.ac_voltage_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_voltage_min_rating(&self) -> Option<u16> {
        self.ac_voltage_min_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_current_max_rating(&self) -> Option<u16> {
        self.ac_current_max_rating_callback
            .map(|callback| (callback)(self.context))
    }

    fn pf_over_excited_rating_unused(&self) -> Option<u16> {
        self.pf_over_excited_rating_unused_callback
            .map(|callback| (callback)(self.context))
    }

    fn pf_under_excited_rating_unused(&self) -> Option<u16> {
        self.pf_under_excited_rating_unused_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_susceptance(&self) -> Option<u16> {
        self.reactive_susceptance_callback
            .map(|callback| (callback)(self.context))
    }

    fn normal_operating_category(&self) -> Option<NorOpCatRtg> {
        self.normal_operating_category_callback
            .map(|callback| (callback)(self.context))
    }

    fn abnormal_operating_category(&self) -> Option<AbnOpCatRtg> {
        self.abnormal_operating_category_callback
            .map(|callback| (callback)(self.context))
    }

    fn supported_control_modes(&self) -> Option<u32> {
        self.supported_control_modes_callback
            .map(|callback| (callback)(self.context))
    }

    fn int_island_cat_rtg(&self) -> Option<u16> {
        self.int_island_cat_rtg_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_max_setting(&self) -> Option<u16> {
        self.active_power_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_over_excited_setting(&self) -> Option<u16> {
        self.active_power_over_excited_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn w_ovr_ext_pf(&self) -> Option<u16> {
        self.w_ovr_ext_pf_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_under_excited_setting(&self) -> Option<u16> {
        self.active_power_under_excited_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn w_und_ext_pf(&self) -> Option<u16> {
        self.w_und_ext_pf_callback
            .map(|callback| (callback)(self.context))
    }

    fn apparent_power_max_setting(&self) -> Option<u16> {
        self.apparent_power_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_power_injected_setting(&self) -> Option<u16> {
        self.reactive_power_injected_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_power_absorbed_setting(&self) -> Option<u16> {
        self.reactive_power_absorbed_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn charge_rate_max_setting(&self) -> Option<u16> {
        self.charge_rate_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn discharge_rate_max_setting(&self) -> Option<u16> {
        self.discharge_rate_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn charge_rate_max_va_setting(&self) -> Option<u16> {
        self.charge_rate_max_va_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn discharge_rate_max_va_setting(&self) -> Option<u16> {
        self.discharge_rate_max_va_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn nominal_ac_voltage_setting(&self) -> Option<u16> {
        self.nominal_ac_voltage_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_voltage_max_setting(&self) -> Option<u16> {
        self.ac_voltage_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_voltage_min_setting(&self) -> Option<u16> {
        self.ac_voltage_min_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn ac_current_max_setting(&self) -> Option<u16> {
        self.ac_current_max_setting_callback
            .map(|callback| (callback)(self.context))
    }

    fn pf_over_excited_setting_unused(&self) -> Option<u16> {
        self.pf_over_excited_setting_unused_callback
            .map(|callback| (callback)(self.context))
    }

    fn pf_under_excited_setting_unused(&self) -> Option<u16> {
        self.pf_under_excited_setting_unused_callback
            .map(|callback| (callback)(self.context))
    }

    fn int_island_cat(&self) -> Option<u16> {
        self.int_island_cat_callback
            .map(|callback| (callback)(self.context))
    }

    fn active_power_scale_factor(&self) -> Option<i16> {
        self.active_power_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn power_factor_scale_factor(&self) -> Option<i16> {
        self.power_factor_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn apparent_power_scale_factor(&self) -> Option<i16> {
        self.apparent_power_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn reactive_power_scale_factor(&self) -> Option<i16> {
        self.reactive_power_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn voltage_scale_factor(&self) -> Option<i16> {
        self.voltage_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn current_scale_factor(&self) -> Option<i16> {
        self.current_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }

    fn susceptance_scale_factor(&self) -> Option<i16> {
        self.susceptance_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model702CallbackAdapter {
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    fn set_active_power_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_active_power_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    fn set_active_power_over_excited_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_active_power_over_excited_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    fn set_w_ovr_ext_pf(&mut self, value: u16) {
        if let Some(callback) = self.set_w_ovr_ext_pf_callback {
            (callback)(value, self.context);
        };
    }

    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    fn set_active_power_under_excited_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_active_power_under_excited_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    fn set_w_und_ext_pf(&mut self, value: u16) {
        if let Some(callback) = self.set_w_und_ext_pf_callback {
            (callback)(value, self.context);
        };
    }

    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    fn set_apparent_power_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_apparent_power_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    fn set_reactive_power_injected_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_reactive_power_injected_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    fn set_reactive_power_absorbed_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_reactive_power_absorbed_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    fn set_charge_rate_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_charge_rate_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    fn set_discharge_rate_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_discharge_rate_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    fn set_charge_rate_max_va_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_charge_rate_max_va_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    fn set_discharge_rate_max_va_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_discharge_rate_max_va_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_nominal_ac_voltage_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_nominal_ac_voltage_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_ac_voltage_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_min_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_ac_voltage_min_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    fn set_ac_current_max_setting(&mut self, value: u16) {
        if let Some(callback) = self.set_ac_current_max_setting_callback {
            (callback)(value, self.context);
        };
    }

    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    fn set_pf_over_excited_setting_unused(&mut self, value: u16) {
        if let Some(callback) = self.set_pf_over_excited_setting_unused_callback {
            (callback)(value, self.context);
        };
    }

    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    fn set_pf_under_excited_setting_unused(&mut self, value: u16) {
        if let Some(callback) = self.set_pf_under_excited_setting_unused_callback {
            (callback)(value, self.context);
        };
    }

    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    fn set_int_island_cat(&mut self, value: u16) {
        if let Some(callback) = self.set_int_island_cat_callback {
            (callback)(value, self.context);
        };
    }
}

#[repr(C)]
pub struct Model702StatefulAdapter {
    /// Active Power Max Rating (WMaxRtg)
    ///
    /// Maximum active power rating at unity power factor in watts.
    pub active_power_max_rating: u16,
    /// Active Power (Over-Excited) Rating (WOvrExtRtg)
    ///
    /// Active power rating at specified over-excited power factor in watts.
    pub active_power_over_excited_rating: u16,
    /// Specified Over-Excited PF (WOvrExtRtgPF)
    ///
    /// Specified over-excited power factor.
    pub w_ovr_ext_rtg_pf: u16,
    /// Active Power (Under-Excited) Rating (WUndExtRtg)
    ///
    /// Active power rating at specified under-excited power factor in watts.
    pub active_power_under_excited_rating: u16,
    /// Specified Under-Excited PF (WUndExtRtgPF)
    ///
    /// Specified under-excited power factor.
    pub w_und_ext_rtg_pf: u16,
    /// Apparent Power Max Rating (VAMaxRtg)
    ///
    /// Maximum apparent power rating in voltamperes.
    pub apparent_power_max_rating: u16,
    /// Reactive Power Injected Rating (VarMaxInjRtg)
    ///
    /// Maximum injected reactive power rating in vars.
    pub reactive_power_injected_rating: u16,
    /// Reactive Power Absorbed Rating (VarMaxAbsRtg)
    ///
    /// Maximum absorbed reactive power rating in vars.
    pub reactive_power_absorbed_rating: u16,
    /// Charge Rate Max Rating (WChaRteMaxRtg)
    ///
    /// Maximum active power charge rate in watts.
    pub charge_rate_max_rating: u16,
    /// Discharge Rate Max Rating (WDisChaRteMaxRtg)
    ///
    /// Maximum active power discharge rate in watts.
    pub discharge_rate_max_rating: u16,
    /// Charge Rate Max VA Rating (VAChaRteMaxRtg)
    ///
    /// Maximum apparent power charge rate in voltamperes.
    pub charge_rate_max_va_rating: u16,
    /// Discharge Rate Max VA Rating (VADisChaRteMaxRtg)
    ///
    /// Maximum apparent power discharge rate in voltamperes.
    pub discharge_rate_max_va_rating: u16,
    /// AC Voltage Nominal Rating (VNomRtg)
    ///
    /// AC voltage nominal rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub ac_voltage_nominal_rating: u16,
    /// AC Voltage Max Rating (VMaxRtg)
    ///
    /// AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub ac_voltage_max_rating: u16,
    /// AC Voltage Min Rating (VMinRtg)
    ///
    /// AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub ac_voltage_min_rating: u16,
    /// AC Current Max Rating (AMaxRtg)
    ///
    /// AC current maximum rating in amps.
    pub ac_current_max_rating: u16,
    /// PF Over-Excited Rating (Unused) (PFOvrExtRtg)
    ///
    /// Unused. Please use WOvrExtRtgPF.
    ///
    /// This point is duplicative of WOvrExtRtgPF.
    pub pf_over_excited_rating_unused: u16,
    /// PF Under-Excited Rating (Unused) (PFUndExtRtg)
    ///
    /// Unused. Please use WUndExtRtgPF.
    ///
    /// This point is duplicative of WUndExtRtgPF.
    pub pf_under_excited_rating_unused: u16,
    /// Reactive Susceptance (ReactSusceptRtg)
    ///
    /// Reactive susceptance that remains connected to the Area EPS in the cease to energize and trip state.
    pub reactive_susceptance: u16,
    /// Normal Operating Category (NorOpCatRtg)
    ///
    /// Normal operating performance category as specified in IEEE 1547-2018.
    pub normal_operating_category: Model702NorOpCatRtg,
    /// Abnormal Operating Category (AbnOpCatRtg)
    ///
    /// Abnormal operating performance category as specified in IEEE 1547-2018.
    pub abnormal_operating_category: Model702AbnOpCatRtg,
    /// Supported Control Modes (CtrlModes)
    ///
    /// Supported control mode functions.
    pub supported_control_modes: u32,
    /// Intentional Island Categories (IntIslandCatRtg)
    ///
    /// Intentional island categories.
    pub int_island_cat_rtg: u16,
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    pub active_power_max_setting: u16,
    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    pub active_power_over_excited_setting: u16,
    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    pub w_ovr_ext_pf: u16,
    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    pub active_power_under_excited_setting: u16,
    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    pub w_und_ext_pf: u16,
    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    pub apparent_power_max_setting: u16,
    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    pub reactive_power_injected_setting: u16,
    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    pub reactive_power_absorbed_setting: u16,
    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    pub charge_rate_max_setting: u16,
    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    pub discharge_rate_max_setting: u16,
    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    pub charge_rate_max_va_setting: u16,
    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    pub discharge_rate_max_va_setting: u16,
    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub nominal_ac_voltage_setting: u16,
    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub ac_voltage_max_setting: u16,
    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    pub ac_voltage_min_setting: u16,
    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    pub ac_current_max_setting: u16,
    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    pub pf_over_excited_setting_unused: u16,
    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    pub pf_under_excited_setting_unused: u16,
    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    pub int_island_cat: u16,
    /// Active Power Scale Factor (W_SF)
    ///
    /// Active power scale factor.
    pub active_power_scale_factor: i16,
    /// Power Factor Scale Factor (PF_SF)
    ///
    /// Power factor scale factor.
    pub power_factor_scale_factor: i16,
    /// Apparent Power Scale Factor (VA_SF)
    ///
    /// Apparent power scale factor.
    pub apparent_power_scale_factor: i16,
    /// Reactive Power Scale Factor (Var_SF)
    ///
    /// Reactive power scale factor.
    pub reactive_power_scale_factor: i16,
    /// Voltage Scale Factor (V_SF)
    ///
    /// Voltage scale factor.
    pub voltage_scale_factor: i16,
    /// Current Scale Factor (A_SF)
    ///
    /// Current scale factor.
    pub current_scale_factor: i16,
    /// Susceptance Scale Factor (S_SF)
    ///
    /// Susceptance scale factor.
    pub susceptance_scale_factor: i16,
}

impl ReadAdapter for Model702StatefulAdapter {
    fn active_power_max_rating(&self) -> Option<u16> {
        Some(self.active_power_max_rating)
    }

    fn active_power_over_excited_rating(&self) -> Option<u16> {
        Some(self.active_power_over_excited_rating)
    }

    fn w_ovr_ext_rtg_pf(&self) -> Option<u16> {
        Some(self.w_ovr_ext_rtg_pf)
    }

    fn active_power_under_excited_rating(&self) -> Option<u16> {
        Some(self.active_power_under_excited_rating)
    }

    fn w_und_ext_rtg_pf(&self) -> Option<u16> {
        Some(self.w_und_ext_rtg_pf)
    }

    fn apparent_power_max_rating(&self) -> Option<u16> {
        Some(self.apparent_power_max_rating)
    }

    fn reactive_power_injected_rating(&self) -> Option<u16> {
        Some(self.reactive_power_injected_rating)
    }

    fn reactive_power_absorbed_rating(&self) -> Option<u16> {
        Some(self.reactive_power_absorbed_rating)
    }

    fn charge_rate_max_rating(&self) -> Option<u16> {
        Some(self.charge_rate_max_rating)
    }

    fn discharge_rate_max_rating(&self) -> Option<u16> {
        Some(self.discharge_rate_max_rating)
    }

    fn charge_rate_max_va_rating(&self) -> Option<u16> {
        Some(self.charge_rate_max_va_rating)
    }

    fn discharge_rate_max_va_rating(&self) -> Option<u16> {
        Some(self.discharge_rate_max_va_rating)
    }

    fn ac_voltage_nominal_rating(&self) -> Option<u16> {
        Some(self.ac_voltage_nominal_rating)
    }

    fn ac_voltage_max_rating(&self) -> Option<u16> {
        Some(self.ac_voltage_max_rating)
    }

    fn ac_voltage_min_rating(&self) -> Option<u16> {
        Some(self.ac_voltage_min_rating)
    }

    fn ac_current_max_rating(&self) -> Option<u16> {
        Some(self.ac_current_max_rating)
    }

    fn pf_over_excited_rating_unused(&self) -> Option<u16> {
        Some(self.pf_over_excited_rating_unused)
    }

    fn pf_under_excited_rating_unused(&self) -> Option<u16> {
        Some(self.pf_under_excited_rating_unused)
    }

    fn reactive_susceptance(&self) -> Option<u16> {
        Some(self.reactive_susceptance)
    }

    fn normal_operating_category(&self) -> Option<NorOpCatRtg> {
        Some(self.normal_operating_category)
    }

    fn abnormal_operating_category(&self) -> Option<AbnOpCatRtg> {
        Some(self.abnormal_operating_category)
    }

    fn supported_control_modes(&self) -> Option<u32> {
        Some(self.supported_control_modes)
    }

    fn int_island_cat_rtg(&self) -> Option<u16> {
        Some(self.int_island_cat_rtg)
    }

    fn active_power_max_setting(&self) -> Option<u16> {
        Some(self.active_power_max_setting)
    }

    fn active_power_over_excited_setting(&self) -> Option<u16> {
        Some(self.active_power_over_excited_setting)
    }

    fn w_ovr_ext_pf(&self) -> Option<u16> {
        Some(self.w_ovr_ext_pf)
    }

    fn active_power_under_excited_setting(&self) -> Option<u16> {
        Some(self.active_power_under_excited_setting)
    }

    fn w_und_ext_pf(&self) -> Option<u16> {
        Some(self.w_und_ext_pf)
    }

    fn apparent_power_max_setting(&self) -> Option<u16> {
        Some(self.apparent_power_max_setting)
    }

    fn reactive_power_injected_setting(&self) -> Option<u16> {
        Some(self.reactive_power_injected_setting)
    }

    fn reactive_power_absorbed_setting(&self) -> Option<u16> {
        Some(self.reactive_power_absorbed_setting)
    }

    fn charge_rate_max_setting(&self) -> Option<u16> {
        Some(self.charge_rate_max_setting)
    }

    fn discharge_rate_max_setting(&self) -> Option<u16> {
        Some(self.discharge_rate_max_setting)
    }

    fn charge_rate_max_va_setting(&self) -> Option<u16> {
        Some(self.charge_rate_max_va_setting)
    }

    fn discharge_rate_max_va_setting(&self) -> Option<u16> {
        Some(self.discharge_rate_max_va_setting)
    }

    fn nominal_ac_voltage_setting(&self) -> Option<u16> {
        Some(self.nominal_ac_voltage_setting)
    }

    fn ac_voltage_max_setting(&self) -> Option<u16> {
        Some(self.ac_voltage_max_setting)
    }

    fn ac_voltage_min_setting(&self) -> Option<u16> {
        Some(self.ac_voltage_min_setting)
    }

    fn ac_current_max_setting(&self) -> Option<u16> {
        Some(self.ac_current_max_setting)
    }

    fn pf_over_excited_setting_unused(&self) -> Option<u16> {
        Some(self.pf_over_excited_setting_unused)
    }

    fn pf_under_excited_setting_unused(&self) -> Option<u16> {
        Some(self.pf_under_excited_setting_unused)
    }

    fn int_island_cat(&self) -> Option<u16> {
        Some(self.int_island_cat)
    }

    fn active_power_scale_factor(&self) -> Option<i16> {
        Some(self.active_power_scale_factor)
    }

    fn power_factor_scale_factor(&self) -> Option<i16> {
        Some(self.power_factor_scale_factor)
    }

    fn apparent_power_scale_factor(&self) -> Option<i16> {
        Some(self.apparent_power_scale_factor)
    }

    fn reactive_power_scale_factor(&self) -> Option<i16> {
        Some(self.reactive_power_scale_factor)
    }

    fn voltage_scale_factor(&self) -> Option<i16> {
        Some(self.voltage_scale_factor)
    }

    fn current_scale_factor(&self) -> Option<i16> {
        Some(self.current_scale_factor)
    }

    fn susceptance_scale_factor(&self) -> Option<i16> {
        Some(self.susceptance_scale_factor)
    }
}

impl WriteAdapter for Model702StatefulAdapter {
    /// Active Power Max Setting (WMax)
    ///
    /// Maximum active power setting used to adjust maximum active power setting.
    fn set_active_power_max_setting(&mut self, value: u16) {
        self.active_power_max_setting = value;
    }

    /// Active Power (Over-Excited) Setting (WMaxOvrExt)
    ///
    /// Active power setting at specified over-excited power factor in watts.
    fn set_active_power_over_excited_setting(&mut self, value: u16) {
        self.active_power_over_excited_setting = value;
    }

    /// Specified Over-Excited PF (WOvrExtPF)
    ///
    /// Specified over-excited power factor.
    fn set_w_ovr_ext_pf(&mut self, value: u16) {
        self.w_ovr_ext_pf = value;
    }

    /// Active Power (Under-Excited) Setting (WMaxUndExt)
    ///
    /// Active power setting at specified under-excited power factor in watts.
    fn set_active_power_under_excited_setting(&mut self, value: u16) {
        self.active_power_under_excited_setting = value;
    }

    /// Specified Under-Excited PF (WUndExtPF)
    ///
    /// Specified under-excited power factor.
    fn set_w_und_ext_pf(&mut self, value: u16) {
        self.w_und_ext_pf = value;
    }

    /// Apparent Power Max Setting (VAMax)
    ///
    /// Maximum apparent power setting used to adjust maximum apparent power rating.
    fn set_apparent_power_max_setting(&mut self, value: u16) {
        self.apparent_power_max_setting = value;
    }

    /// Reactive Power Injected Setting (VarMaxInj)
    ///
    /// Maximum injected reactive power setting used to adjust maximum injected reactive power rating.
    fn set_reactive_power_injected_setting(&mut self, value: u16) {
        self.reactive_power_injected_setting = value;
    }

    /// Reactive Power Absorbed Setting (VarMaxAbs)
    ///
    /// Maximum absorbed reactive power setting used to adjust maximum absorbed reactive power rating.
    fn set_reactive_power_absorbed_setting(&mut self, value: u16) {
        self.reactive_power_absorbed_setting = value;
    }

    /// Charge Rate Max Setting (WChaRteMax)
    ///
    /// Maximum active power charge rate setting used to adjust maximum active power charge rate rating.
    fn set_charge_rate_max_setting(&mut self, value: u16) {
        self.charge_rate_max_setting = value;
    }

    /// Discharge Rate Max Setting (WDisChaRteMax)
    ///
    /// Maximum active power discharge rate setting used to adjust maximum active power discharge rate rating.
    fn set_discharge_rate_max_setting(&mut self, value: u16) {
        self.discharge_rate_max_setting = value;
    }

    /// Charge Rate Max VA Setting (VAChaRteMax)
    ///
    /// Maximum apparent power charge rate setting used to adjust maximum apparent power charge rate rating.
    fn set_charge_rate_max_va_setting(&mut self, value: u16) {
        self.charge_rate_max_va_setting = value;
    }

    /// Discharge Rate Max VA Setting (VADisChaRteMax)
    ///
    /// Maximum apparent power discharge rate setting used to adjust maximum apparent power discharge rate rating.
    fn set_discharge_rate_max_va_setting(&mut self, value: u16) {
        self.discharge_rate_max_va_setting = value;
    }

    /// Nominal AC Voltage Setting (VNom)
    ///
    /// Nominal AC voltage setting.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_nominal_ac_voltage_setting(&mut self, value: u16) {
        self.nominal_ac_voltage_setting = value;
    }

    /// AC Voltage Max Setting (VMax)
    ///
    /// AC voltage maximum setting used to adjust AC voltage maximum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_max_setting(&mut self, value: u16) {
        self.ac_voltage_max_setting = value;
    }

    /// AC Voltage Min Setting (VMin)
    ///
    /// AC voltage minimum setting used to adjust AC voltage minimum rating.
    ///
    /// Voltages are LN for single phase DER (e.g. 120 V nominal), LL for split phase DER (e.g. 240 V nominal), and LL for three phase DER (e.g., 480 V nominal).
    fn set_ac_voltage_min_setting(&mut self, value: u16) {
        self.ac_voltage_min_setting = value;
    }

    /// AC Current Max Setting (AMax)
    ///
    /// Maximum AC current setting used to adjust maximum AC current rating.
    fn set_ac_current_max_setting(&mut self, value: u16) {
        self.ac_current_max_setting = value;
    }

    /// PF Over-Excited Setting (Unused) (PFOvrExt)
    ///
    /// Unused. Please use WOvrExtPF.
    ///
    /// This point is duplicative of WOvrExtPF.
    fn set_pf_over_excited_setting_unused(&mut self, value: u16) {
        self.pf_over_excited_setting_unused = value;
    }

    /// PF Under-Excited Setting (Unused) (PFUndExt)
    ///
    /// Unused. Please use WUndExtPF.
    ///
    /// This point is duplicative of WUndExtPF.
    fn set_pf_under_excited_setting_unused(&mut self, value: u16) {
        self.pf_under_excited_setting_unused = value;
    }

    /// Intentional Island Categories (IntIslandCat)
    ///
    /// Intentional island categories.
    fn set_int_island_cat(&mut self, value: u16) {
        self.int_island_cat = value;
    }
}

/// C-FFI dispatch descriptor for SunSpec model 702. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_702: StaticModelSpec = StaticModelSpec {
    id: 702,
    length: model_702_c_length,
    writable: true,
    visit_read: model_702_c_visit_read,
    visit_write: model_702_c_visit_write,
};

fn model_702_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model702).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model702{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_702_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model702;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model702StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model702CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_702_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_702_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model702;
    let adapter: Option<&mut dyn WriteAdapter> = match kind {
        1 => {
            Some(unsafe { &mut *(adapter as *mut Model702StatefulAdapter) } as &mut dyn WriteAdapter)
        }
        2 => {
            Some(unsafe { &mut *(adapter as *mut Model702CallbackAdapter) } as &mut dyn WriteAdapter)
        }
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}
