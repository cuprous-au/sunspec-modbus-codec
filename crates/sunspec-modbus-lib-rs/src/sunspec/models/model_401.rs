// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::c_void;

static POINTS: [PointDetails<()>; 13] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::DcaSf,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::DcAhrSf,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::DcvSf,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::Rating,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::N,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Event,
        size: 2,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::VendorEvent,
        size: 2,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::Amps,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::AmpHours,
        size: 2,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::Voltage,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::Temp,
        size: 1,
        start_address: 15,
    },
];

static STRING_POINTS: [PointDetails<()>; 5] = [
    PointDetails {
        point: |()| Point::StringId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::StringInputEvent,
        size: 2,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::StringInputEventVendor,
        size: 2,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::StringAmps,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::StringAmpHours,
        size: 2,
        start_address: 6,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    DcaSf,
    DcAhrSf,
    DcvSf,
    Rating,
    N,
    Event,
    VendorEvent,
    Amps,
    AmpHours,
    Voltage,
    Temp,
    StringId,
    StringInputEvent,
    StringInputEventVendor,
    StringAmps,
    StringAmpHours,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 401. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model401;

impl<'ad> ModelSpec<'ad> for Model401 {
    const MODEL_ID: u16 = 401;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        24
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                STRING_POINTS
                    .iter()
                    .map(move |p| (16 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                STRING_POINTS
                    .iter()
                    .map(move |p| (16 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model401,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(401);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::DcaSf => {
            buffer.write_i16(adapter.dca_sf());
        }
        Point::DcAhrSf => {
            if let Some(value) = adapter.dc_ahr_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::DcvSf => {
            if let Some(value) = adapter.dcv_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Rating => {
            buffer.write_u16(adapter.rating());
        }
        Point::N => {
            buffer.write_u16(adapter.n());
        }
        Point::Event => {
            buffer.write_u32(adapter.event(), offset);
        }
        Point::VendorEvent => {
            if let Some(value) = adapter.vendor_event() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Amps => {
            buffer.write_i16(adapter.amps());
        }
        Point::AmpHours => {
            if let Some(value) = adapter.amp_hours() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Voltage => {
            if let Some(value) = adapter.voltage() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Temp => {
            if let Some(value) = adapter.temp() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StringId => {
            buffer.write_u16(adapter.string_id());
        }
        Point::StringInputEvent => {
            buffer.write_u32(adapter.string_input_event(), offset);
        }
        Point::StringInputEventVendor => {
            if let Some(value) = adapter.string_input_event_vendor() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::StringAmps => {
            buffer.write_i16(adapter.string_amps());
        }
        Point::StringAmpHours => {
            if let Some(value) = adapter.string_amp_hours() {
                buffer.write_u32(value, offset);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model401,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// DCA_SF
    ///
    /// Current scale factor
    fn dca_sf(&self) -> i16;

    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    fn dc_ahr_sf(&self) -> Option<i16> {
        None
    }

    /// DCV_SF
    ///
    /// Voltage scale factor
    fn dcv_sf(&self) -> Option<i16> {
        None
    }

    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    fn rating(&self) -> u16;

    /// N (N)
    ///
    /// Number of Inputs
    fn n(&self) -> u16;

    /// Event (Evt)
    ///
    /// Events
    fn event(&self) -> u32;

    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    fn vendor_event(&self) -> Option<u32> {
        None
    }

    /// Amps (DCA)
    ///
    /// Total measured current
    fn amps(&self) -> i16;

    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    fn amp_hours(&self) -> Option<u32> {
        None
    }

    /// Voltage (DCV)
    ///
    /// Output Voltage
    fn voltage(&self) -> Option<u16> {
        None
    }

    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    fn temp(&self) -> Option<i16> {
        None
    }

    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    fn string_id(&self) -> u16;

    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    fn string_input_event(&self) -> u32;

    /// Input Event Vendor (InEvtVnd)
    ///
    /// String Input Vendor Event Flags
    fn string_input_event_vendor(&self) -> Option<u32> {
        None
    }

    /// Amps (InDCA)
    ///
    /// String Input Current
    fn string_amps(&self) -> i16;

    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    fn string_amp_hours(&self) -> Option<u32> {
        None
    }
}

pub trait WriteAdapter {}

#[repr(C)]
pub struct Model401CallbackAdapter {
    context: *mut c_void,
    /// DCA_SF
    ///
    /// Current scale factor
    dca_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    dc_ahr_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// DCV_SF
    ///
    /// Voltage scale factor
    dcv_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    rating_callback: extern "C" fn(*const c_void) -> u16,
    /// N (N)
    ///
    /// Number of Inputs
    n_callback: extern "C" fn(*const c_void) -> u16,
    /// Event (Evt)
    ///
    /// Events
    event_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    vendor_event_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Amps (DCA)
    ///
    /// Total measured current
    amps_callback: extern "C" fn(*const c_void) -> i16,
    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    amp_hours_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Voltage (DCV)
    ///
    /// Output Voltage
    voltage_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    temp_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    string_id_callback: extern "C" fn(*const c_void) -> u16,
    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    string_input_event_callback: extern "C" fn(*const c_void) -> u32,
    /// Input Event Vendor (InEvtVnd)
    ///
    /// String Input Vendor Event Flags
    string_input_event_vendor_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Amps (InDCA)
    ///
    /// String Input Current
    string_amps_callback: extern "C" fn(*const c_void) -> i16,
    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    string_amp_hours_callback: Option<extern "C" fn(*const c_void) -> u32>,
}

impl ReadAdapter for Model401CallbackAdapter {
    fn dca_sf(&self) -> i16 {
        (self.dca_sf_callback)(self.context)
    }

    fn dc_ahr_sf(&self) -> Option<i16> {
        self.dc_ahr_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn dcv_sf(&self) -> Option<i16> {
        self.dcv_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn rating(&self) -> u16 {
        (self.rating_callback)(self.context)
    }

    fn n(&self) -> u16 {
        (self.n_callback)(self.context)
    }

    fn event(&self) -> u32 {
        (self.event_callback)(self.context)
    }

    fn vendor_event(&self) -> Option<u32> {
        self.vendor_event_callback
            .map(|callback| (callback)(self.context))
    }

    fn amps(&self) -> i16 {
        (self.amps_callback)(self.context)
    }

    fn amp_hours(&self) -> Option<u32> {
        self.amp_hours_callback
            .map(|callback| (callback)(self.context))
    }

    fn voltage(&self) -> Option<u16> {
        self.voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn temp(&self) -> Option<i16> {
        self.temp_callback.map(|callback| (callback)(self.context))
    }

    fn string_id(&self) -> u16 {
        (self.string_id_callback)(self.context)
    }

    fn string_input_event(&self) -> u32 {
        (self.string_input_event_callback)(self.context)
    }

    fn string_input_event_vendor(&self) -> Option<u32> {
        self.string_input_event_vendor_callback
            .map(|callback| (callback)(self.context))
    }

    fn string_amps(&self) -> i16 {
        (self.string_amps_callback)(self.context)
    }

    fn string_amp_hours(&self) -> Option<u32> {
        self.string_amp_hours_callback
            .map(|callback| (callback)(self.context))
    }
}

#[repr(C)]
pub struct Model401StatefulAdapter {
    /// DCA_SF
    ///
    /// Current scale factor
    pub dca_sf: i16,
    /// DCAhr_SF
    ///
    /// Amp-hour scale factor
    pub dc_ahr_sf: i16,
    /// DCV_SF
    ///
    /// Voltage scale factor
    pub dcv_sf: i16,
    /// Rating (DCAMax)
    ///
    /// Maximum DC Current Rating
    pub rating: u16,
    /// N (N)
    ///
    /// Number of Inputs
    pub n: u16,
    /// Event (Evt)
    ///
    /// Events
    pub event: u32,
    /// Vendor Event (EvtVnd)
    ///
    /// Vendor defined events
    pub vendor_event: u32,
    /// Amps (DCA)
    ///
    /// Total measured current
    pub amps: i16,
    /// Amp-hours (DCAhr)
    ///
    /// Total metered Amp-hours
    pub amp_hours: u32,
    /// Voltage (DCV)
    ///
    /// Output Voltage
    pub voltage: u16,
    /// Temp (Tmp)
    ///
    /// Internal operating temperature
    pub temp: i16,
    /// ID (InID)
    ///
    /// Uniquely identifies this input set
    pub string_id: u16,
    /// Input Event (InEvt)
    ///
    /// String Input Event Flags
    pub string_input_event: u32,
    /// Input Event Vendor (InEvtVnd)
    ///
    /// String Input Vendor Event Flags
    pub string_input_event_vendor: u32,
    /// Amps (InDCA)
    ///
    /// String Input Current
    pub string_amps: i16,
    /// Amp-hours (InDCAhr)
    ///
    /// String Input Amp-Hours
    pub string_amp_hours: u32,
}

impl ReadAdapter for Model401StatefulAdapter {
    fn dca_sf(&self) -> i16 {
        self.dca_sf
    }

    fn dc_ahr_sf(&self) -> Option<i16> {
        Some(self.dc_ahr_sf)
    }

    fn dcv_sf(&self) -> Option<i16> {
        Some(self.dcv_sf)
    }

    fn rating(&self) -> u16 {
        self.rating
    }

    fn n(&self) -> u16 {
        self.n
    }

    fn event(&self) -> u32 {
        self.event
    }

    fn vendor_event(&self) -> Option<u32> {
        Some(self.vendor_event)
    }

    fn amps(&self) -> i16 {
        self.amps
    }

    fn amp_hours(&self) -> Option<u32> {
        Some(self.amp_hours)
    }

    fn voltage(&self) -> Option<u16> {
        Some(self.voltage)
    }

    fn temp(&self) -> Option<i16> {
        Some(self.temp)
    }

    fn string_id(&self) -> u16 {
        self.string_id
    }

    fn string_input_event(&self) -> u32 {
        self.string_input_event
    }

    fn string_input_event_vendor(&self) -> Option<u32> {
        Some(self.string_input_event_vendor)
    }

    fn string_amps(&self) -> i16 {
        self.string_amps
    }

    fn string_amp_hours(&self) -> Option<u32> {
        Some(self.string_amp_hours)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 401. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_401: StaticModelSpec = StaticModelSpec {
    id: 401,
    length: model_401_c_length,
    writable: false,
    visit_read: model_401_c_visit_read,
    visit_write: model_401_c_visit_write,
};

fn model_401_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model401).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model401{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_401_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model401;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model401StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model401CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_401_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_401_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (kind, adapter, buffer);
    let model = Model401;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}
