// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_127::*;

#[repr(C)]
pub struct Model127CallbackAdapter {
    context: *mut c_void,
    /// WGra (WGra)
    ///
    /// The slope of the reduction in the maximum allowed watts output as a function of frequency.
    w_gra_callback: extern "C" fn(*const c_void) -> u16,
    /// WGra (WGra)
    ///
    /// The slope of the reduction in the maximum allowed watts output as a function of frequency.
    set_w_gra_callback: extern "C" fn(u16, *mut c_void),
    /// HzStr (HzStr)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which a snapshot of the instantaneous power output is taken to act as the CAPPED power level (PM) and above which reduction in power output occurs.
    hz_str_callback: extern "C" fn(*const c_void) -> i16,
    /// HzStr (HzStr)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which a snapshot of the instantaneous power output is taken to act as the CAPPED power level (PM) and above which reduction in power output occurs.
    set_hz_str_callback: extern "C" fn(i16, *mut c_void),
    /// HzStop (HzStop)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which curtailed power output may return to normal and the cap on the power level value is removed.
    hz_stop_callback: extern "C" fn(*const c_void) -> i16,
    /// HzStop (HzStop)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which curtailed power output may return to normal and the cap on the power level value is removed.
    set_hz_stop_callback: extern "C" fn(i16, *mut c_void),
    /// HysEna (HysEna)
    ///
    /// Enable hysteresis
    hys_ena_callback: extern "C" fn(*const c_void) -> u16,
    /// HysEna (HysEna)
    ///
    /// Enable hysteresis
    set_hys_ena_callback: extern "C" fn(u16, *mut c_void),
    /// ModEna (ModEna)
    ///
    /// Is Parameterized Frequency-Watt control active.
    mod_ena_callback: extern "C" fn(*const c_void) -> u16,
    /// ModEna (ModEna)
    ///
    /// Is Parameterized Frequency-Watt control active.
    set_mod_ena_callback: extern "C" fn(u16, *mut c_void),
    /// HzStopWGra (HzStopWGra)
    ///
    /// The maximum time-based rate of change at which power output returns to normal after having been capped by an over frequency event.
    hz_stop_w_gra_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// HzStopWGra (HzStopWGra)
    ///
    /// The maximum time-based rate of change at which power output returns to normal after having been capped by an over frequency event.
    set_hz_stop_w_gra_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// WGra_SF (WGra_SF)
    ///
    /// Scale factor for output gradient.
    w_gra_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// HzStrStop_SF (HzStrStop_SF)
    ///
    /// Scale factor for frequency deviations.
    hz_str_stop_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// RmpIncDec_SF (RmpIncDec_SF)
    ///
    /// Scale factor for increment and decrement ramps.
    rmp_inc_dec_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model127CallbackAdapter {
    fn w_gra(&self) -> u16 {
        (self.w_gra_callback)(self.context)
    }

    fn hz_str(&self) -> i16 {
        (self.hz_str_callback)(self.context)
    }

    fn hz_stop(&self) -> i16 {
        (self.hz_stop_callback)(self.context)
    }

    fn hys_ena(&self) -> u16 {
        (self.hys_ena_callback)(self.context)
    }

    fn mod_ena(&self) -> u16 {
        (self.mod_ena_callback)(self.context)
    }

    fn hz_stop_w_gra(&self) -> Option<u16> {
        self.hz_stop_w_gra_callback
            .map(|callback| (callback)(self.context))
    }

    fn w_gra_sf(&self) -> Option<i16> {
        self.w_gra_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn hz_str_stop_sf(&self) -> Option<i16> {
        self.hz_str_stop_sf_callback
            .map(|callback| (callback)(self.context))
    }

    fn rmp_inc_dec_sf(&self) -> Option<i16> {
        self.rmp_inc_dec_sf_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model127CallbackAdapter {
    /// WGra (WGra)
    ///
    /// The slope of the reduction in the maximum allowed watts output as a function of frequency.
    fn set_w_gra(&mut self, value: u16) {
        (self.set_w_gra_callback)(value, self.context);
    }

    /// HzStr (HzStr)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which a snapshot of the instantaneous power output is taken to act as the CAPPED power level (PM) and above which reduction in power output occurs.
    fn set_hz_str(&mut self, value: i16) {
        (self.set_hz_str_callback)(value, self.context);
    }

    /// HzStop (HzStop)
    ///
    /// The frequency deviation from nominal frequency (ECPNomHz) at which curtailed power output may return to normal and the cap on the power level value is removed.
    fn set_hz_stop(&mut self, value: i16) {
        (self.set_hz_stop_callback)(value, self.context);
    }

    /// HysEna (HysEna)
    ///
    /// Enable hysteresis
    fn set_hys_ena(&mut self, value: u16) {
        (self.set_hys_ena_callback)(value, self.context);
    }

    /// ModEna (ModEna)
    ///
    /// Is Parameterized Frequency-Watt control active.
    fn set_mod_ena(&mut self, value: u16) {
        (self.set_mod_ena_callback)(value, self.context);
    }

    /// HzStopWGra (HzStopWGra)
    ///
    /// The maximum time-based rate of change at which power output returns to normal after having been capped by an over frequency event.
    fn set_hz_stop_w_gra(&mut self, value: u16) {
        if let Some(callback) = self.set_hz_stop_w_gra_callback {
            (callback)(value, self.context);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 127. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_127: StaticModelSpec = StaticModelSpec {
    id: 127,
    length: model_127_c_length,
    writable: true,
    visit_read: model_127_c_visit_read,
    visit_write: model_127_c_visit_write,
};

fn model_127_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model127).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model127CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_127_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model127;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model127CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_127_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_127_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model127;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model127CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 127 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_127_callback(
    adapter: *mut Model127CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_127,
        adapter: adapter as *mut c_void,
    }
}
