// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_145::*;

#[repr(C)]
pub struct Model145CallbackAdapter {
    context: *mut c_void,
    /// Ramp Up Rate (NomRmpUpRte)
    ///
    /// Ramp up rate as a percentage of max current.
    ramp_up_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Ramp Up Rate (NomRmpUpRte)
    ///
    /// Ramp up rate as a percentage of max current.
    set_ramp_up_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// NomRmpDnRte (NomRmpDnRte)
    ///
    /// Ramp down rate as a percentage of max current.
    nom_rmp_dn_rte_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// NomRmpDnRte (NomRmpDnRte)
    ///
    /// Ramp down rate as a percentage of max current.
    set_nom_rmp_dn_rte_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Emergency Ramp Up Rate (EmgRmpUpRte)
    ///
    /// Emergency ramp up rate as a percentage of max current.
    emergency_ramp_up_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Emergency Ramp Up Rate (EmgRmpUpRte)
    ///
    /// Emergency ramp up rate as a percentage of max current.
    set_emergency_ramp_up_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Emergency Ramp Down Rate (EmgRmpDnRte)
    ///
    /// Emergency ramp down rate as a percentage of max current.
    emergency_ramp_down_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Emergency Ramp Down Rate (EmgRmpDnRte)
    ///
    /// Emergency ramp down rate as a percentage of max current.
    set_emergency_ramp_down_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Connect Ramp Up Rate (ConnRmpUpRte)
    ///
    /// Connect ramp up rate as a percentage of max current.
    connect_ramp_up_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Connect Ramp Up Rate (ConnRmpUpRte)
    ///
    /// Connect ramp up rate as a percentage of max current.
    set_connect_ramp_up_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Connect Ramp Down Rate (ConnRmpDnRte)
    ///
    /// Connect ramp down rate as a percentage of max current.
    connect_ramp_down_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Connect Ramp Down Rate (ConnRmpDnRte)
    ///
    /// Connect ramp down rate as a percentage of max current.
    set_connect_ramp_down_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Default Ramp Rate (AGra)
    ///
    /// Ramp rate specified in percent of max current.
    default_ramp_rate_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Default Ramp Rate (AGra)
    ///
    /// Ramp rate specified in percent of max current.
    set_default_ramp_rate_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Ramp Rate Scale Factor (Rmp_SF)
    ///
    /// Ramp Rate Scale Factor
    ramp_rate_scale_factor_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model145CallbackAdapter {
    fn ramp_up_rate(&self) -> Option<u16> {
        self.ramp_up_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn nom_rmp_dn_rte(&self) -> Option<u16> {
        self.nom_rmp_dn_rte_callback
            .map(|callback| (callback)(self.context))
    }

    fn emergency_ramp_up_rate(&self) -> Option<u16> {
        self.emergency_ramp_up_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn emergency_ramp_down_rate(&self) -> Option<u16> {
        self.emergency_ramp_down_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn connect_ramp_up_rate(&self) -> Option<u16> {
        self.connect_ramp_up_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn connect_ramp_down_rate(&self) -> Option<u16> {
        self.connect_ramp_down_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn default_ramp_rate(&self) -> Option<u16> {
        self.default_ramp_rate_callback
            .map(|callback| (callback)(self.context))
    }

    fn ramp_rate_scale_factor(&self) -> Option<i16> {
        self.ramp_rate_scale_factor_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model145CallbackAdapter {
    /// Ramp Up Rate (NomRmpUpRte)
    ///
    /// Ramp up rate as a percentage of max current.
    fn set_ramp_up_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_ramp_up_rate_callback {
            (callback)(value, self.context);
        };
    }

    /// NomRmpDnRte (NomRmpDnRte)
    ///
    /// Ramp down rate as a percentage of max current.
    fn set_nom_rmp_dn_rte(&mut self, value: u16) {
        if let Some(callback) = self.set_nom_rmp_dn_rte_callback {
            (callback)(value, self.context);
        };
    }

    /// Emergency Ramp Up Rate (EmgRmpUpRte)
    ///
    /// Emergency ramp up rate as a percentage of max current.
    fn set_emergency_ramp_up_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_emergency_ramp_up_rate_callback {
            (callback)(value, self.context);
        };
    }

    /// Emergency Ramp Down Rate (EmgRmpDnRte)
    ///
    /// Emergency ramp down rate as a percentage of max current.
    fn set_emergency_ramp_down_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_emergency_ramp_down_rate_callback {
            (callback)(value, self.context);
        };
    }

    /// Connect Ramp Up Rate (ConnRmpUpRte)
    ///
    /// Connect ramp up rate as a percentage of max current.
    fn set_connect_ramp_up_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_connect_ramp_up_rate_callback {
            (callback)(value, self.context);
        };
    }

    /// Connect Ramp Down Rate (ConnRmpDnRte)
    ///
    /// Connect ramp down rate as a percentage of max current.
    fn set_connect_ramp_down_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_connect_ramp_down_rate_callback {
            (callback)(value, self.context);
        };
    }

    /// Default Ramp Rate (AGra)
    ///
    /// Ramp rate specified in percent of max current.
    fn set_default_ramp_rate(&mut self, value: u16) {
        if let Some(callback) = self.set_default_ramp_rate_callback {
            (callback)(value, self.context);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 145. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_145: StaticModelSpec = StaticModelSpec {
    id: 145,
    length: model_145_c_length,
    writable: true,
    visit_read: model_145_c_visit_read,
    visit_write: model_145_c_visit_write,
};

fn model_145_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model145).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model145CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_145_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model145;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model145CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_145_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_145_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model145;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model145CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 145 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_145_callback(
    adapter: *mut Model145CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_145,
        adapter: adapter as *mut c_void,
    }
}
