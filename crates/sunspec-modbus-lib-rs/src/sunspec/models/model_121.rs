// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 32] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::WMax,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::VRef,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::VRefOfs,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::VMax,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::VMin,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::VaMax,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::VArMaxQ1,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::VArMaxQ2,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::VArMaxQ3,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::VArMaxQ4,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::WGra,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::PfMinQ1,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::PfMinQ2,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::PfMinQ3,
        size: 1,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::PfMinQ4,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::VArAct,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::ClcTotVa,
        size: 1,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::MaxRmpRte,
        size: 1,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::EcpNomHz,
        size: 1,
        start_address: 20,
    },
    PointDetails {
        point: |()| Point::ConnPh,
        size: 1,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::WMaxSf,
        size: 1,
        start_address: 22,
    },
    PointDetails {
        point: |()| Point::VRefSf,
        size: 1,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::VRefOfsSf,
        size: 1,
        start_address: 24,
    },
    PointDetails {
        point: |()| Point::VMinMaxSf,
        size: 1,
        start_address: 25,
    },
    PointDetails {
        point: |()| Point::VaMaxSf,
        size: 1,
        start_address: 26,
    },
    PointDetails {
        point: |()| Point::VArMaxSf,
        size: 1,
        start_address: 27,
    },
    PointDetails {
        point: |()| Point::WGraSf,
        size: 1,
        start_address: 28,
    },
    PointDetails {
        point: |()| Point::PfMinSf,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::MaxRmpRteSf,
        size: 1,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::EcpNomHzSf,
        size: 1,
        start_address: 31,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    WMax,
    VRef,
    VRefOfs,
    VMax,
    VMin,
    VaMax,
    VArMaxQ1,
    VArMaxQ2,
    VArMaxQ3,
    VArMaxQ4,
    WGra,
    PfMinQ1,
    PfMinQ2,
    PfMinQ3,
    PfMinQ4,
    VArAct,
    ClcTotVa,
    MaxRmpRte,
    EcpNomHz,
    ConnPh,
    WMaxSf,
    VRefSf,
    VRefOfsSf,
    VMinMaxSf,
    VaMaxSf,
    VArMaxSf,
    WGraSf,
    PfMinSf,
    MaxRmpRteSf,
    EcpNomHzSf,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 121. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model121;

impl<'ad> ModelSpec<'ad> for Model121 {
    const MODEL_ID: u16 = 121;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        32
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model121,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(121);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::WMax => {
            buffer.write_u16(adapter.w_max());
        }
        Point::VRef => {
            buffer.write_u16(adapter.v_ref());
        }
        Point::VRefOfs => {
            buffer.write_i16(adapter.v_ref_ofs());
        }
        Point::VMax => {
            if let Some(value) = adapter.v_max() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VMin => {
            if let Some(value) = adapter.v_min() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VaMax => {
            if let Some(value) = adapter.va_max() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArMaxQ1 => {
            if let Some(value) = adapter.v_ar_max_q1() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArMaxQ2 => {
            if let Some(value) = adapter.v_ar_max_q2() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArMaxQ3 => {
            if let Some(value) = adapter.v_ar_max_q3() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArMaxQ4 => {
            if let Some(value) = adapter.v_ar_max_q4() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WGra => {
            if let Some(value) = adapter.w_gra() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfMinQ1 => {
            if let Some(value) = adapter.pf_min_q1() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfMinQ2 => {
            if let Some(value) = adapter.pf_min_q2() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfMinQ3 => {
            if let Some(value) = adapter.pf_min_q3() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfMinQ4 => {
            if let Some(value) = adapter.pf_min_q4() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArAct => {
            if let Some(value) = adapter.v_ar_act() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::ClcTotVa => {
            if let Some(value) = adapter.clc_tot_va() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::MaxRmpRte => {
            if let Some(value) = adapter.max_rmp_rte() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EcpNomHz => {
            if let Some(value) = adapter.ecp_nom_hz() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ConnPh => {
            if let Some(value) = adapter.conn_ph() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::WMaxSf => {
            buffer.write_i16(adapter.w_max_sf());
        }
        Point::VRefSf => {
            buffer.write_i16(adapter.v_ref_sf());
        }
        Point::VRefOfsSf => {
            buffer.write_i16(adapter.v_ref_ofs_sf());
        }
        Point::VMinMaxSf => {
            if let Some(value) = adapter.v_min_max_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VaMaxSf => {
            if let Some(value) = adapter.va_max_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::VArMaxSf => {
            if let Some(value) = adapter.v_ar_max_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::WGraSf => {
            if let Some(value) = adapter.w_gra_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::PfMinSf => {
            if let Some(value) = adapter.pf_min_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::MaxRmpRteSf => {
            if let Some(value) = adapter.max_rmp_rte_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::EcpNomHzSf => {
            if let Some(value) = adapter.ecp_nom_hz_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model121,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::WMax => {
            let value = buffer.read_u16()?;
            adapter.set_w_max(value);
            Ok(())
        }
        Point::VRef => {
            let value = buffer.read_u16()?;
            adapter.set_v_ref(value);
            Ok(())
        }
        Point::VRefOfs => {
            let value = buffer.read_i16()?;
            adapter.set_v_ref_ofs(value);
            Ok(())
        }
        Point::VMax => {
            let value = buffer.read_u16()?;
            adapter.set_v_max(value);
            Ok(())
        }
        Point::VMin => {
            let value = buffer.read_u16()?;
            adapter.set_v_min(value);
            Ok(())
        }
        Point::VaMax => {
            let value = buffer.read_u16()?;
            adapter.set_va_max(value);
            Ok(())
        }
        Point::VArMaxQ1 => {
            let value = buffer.read_i16()?;
            adapter.set_v_ar_max_q1(value);
            Ok(())
        }
        Point::VArMaxQ2 => {
            let value = buffer.read_i16()?;
            adapter.set_v_ar_max_q2(value);
            Ok(())
        }
        Point::VArMaxQ3 => {
            let value = buffer.read_i16()?;
            adapter.set_v_ar_max_q3(value);
            Ok(())
        }
        Point::VArMaxQ4 => {
            let value = buffer.read_i16()?;
            adapter.set_v_ar_max_q4(value);
            Ok(())
        }
        Point::WGra => {
            let value = buffer.read_u16()?;
            adapter.set_w_gra(value);
            Ok(())
        }
        Point::PfMinQ1 => {
            let value = buffer.read_i16()?;
            adapter.set_pf_min_q1(value);
            Ok(())
        }
        Point::PfMinQ2 => {
            let value = buffer.read_i16()?;
            adapter.set_pf_min_q2(value);
            Ok(())
        }
        Point::PfMinQ3 => {
            let value = buffer.read_i16()?;
            adapter.set_pf_min_q3(value);
            Ok(())
        }
        Point::PfMinQ4 => {
            let value = buffer.read_i16()?;
            adapter.set_pf_min_q4(value);
            Ok(())
        }
        Point::VArAct => {
            let value = buffer.read_u16()?;
            let enum_value = VArAct::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_v_ar_act(enum_value);
            Ok(())
        }
        Point::ClcTotVa => {
            let value = buffer.read_u16()?;
            let enum_value = ClcTotVa::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_clc_tot_va(enum_value);
            Ok(())
        }
        Point::MaxRmpRte => {
            let value = buffer.read_u16()?;
            adapter.set_max_rmp_rte(value);
            Ok(())
        }
        Point::EcpNomHz => {
            let value = buffer.read_u16()?;
            adapter.set_ecp_nom_hz(value);
            Ok(())
        }
        Point::ConnPh => {
            let value = buffer.read_u16()?;
            let enum_value = ConnPh::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_conn_ph(enum_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// WMax (WMax)
    ///
    /// Setting for maximum power output. Default to WRtg.
    fn w_max(&self) -> u16;

    /// VRef (VRef)
    ///
    /// Voltage at the PCC.
    fn v_ref(&self) -> u16;

    /// VRefOfs (VRefOfs)
    ///
    /// Offset from PCC to inverter.
    fn v_ref_ofs(&self) -> i16;

    /// VMax (VMax)
    ///
    /// Setpoint for maximum voltage.
    fn v_max(&self) -> Option<u16> {
        None
    }

    /// VMin (VMin)
    ///
    /// Setpoint for minimum voltage.
    fn v_min(&self) -> Option<u16> {
        None
    }

    /// VAMax (VAMax)
    ///
    /// Setpoint for maximum apparent power. Default to VARtg.
    fn va_max(&self) -> Option<u16> {
        None
    }

    /// VArMaxQ1 (VArMaxQ1)
    ///
    /// Setting for maximum reactive power in quadrant 1. Default to VArRtgQ1.
    fn v_ar_max_q1(&self) -> Option<i16> {
        None
    }

    /// VArMaxQ2 (VArMaxQ2)
    ///
    /// Setting for maximum reactive power in quadrant 2. Default to VArRtgQ2.
    fn v_ar_max_q2(&self) -> Option<i16> {
        None
    }

    /// VArMaxQ3 (VArMaxQ3)
    ///
    /// Setting for maximum reactive power in quadrant 3. Default to VArRtgQ3.
    fn v_ar_max_q3(&self) -> Option<i16> {
        None
    }

    /// VArMaxQ4 (VArMaxQ4)
    ///
    /// Setting for maximum reactive power in quadrant 4. Default to VArRtgQ4.
    fn v_ar_max_q4(&self) -> Option<i16> {
        None
    }

    /// WGra (WGra)
    ///
    /// Default ramp rate of change of active power due to command or internal action.
    fn w_gra(&self) -> Option<u16> {
        None
    }

    /// PFMinQ1 (PFMinQ1)
    ///
    /// Setpoint for minimum power factor value in quadrant 1. Default to PFRtgQ1.
    ///
    /// EEI sign convention.
    fn pf_min_q1(&self) -> Option<i16> {
        None
    }

    /// PFMinQ2 (PFMinQ2)
    ///
    /// Setpoint for minimum power factor value in quadrant 2. Default to PFRtgQ2.
    ///
    /// EEI sign convention.
    fn pf_min_q2(&self) -> Option<i16> {
        None
    }

    /// PFMinQ3 (PFMinQ3)
    ///
    /// Setpoint for minimum power factor value in quadrant 3. Default to PFRtgQ3.
    ///
    /// EEI sign convention.
    fn pf_min_q3(&self) -> Option<i16> {
        None
    }

    /// PFMinQ4 (PFMinQ4)
    ///
    /// Setpoint for minimum power factor value in quadrant 4. Default to PFRtgQ4.
    ///
    /// EEI sign convention.
    fn pf_min_q4(&self) -> Option<i16> {
        None
    }

    /// VArAct (VArAct)
    ///
    /// VAR action on change between charging and discharging: 1=switch 2=maintain VAR characterization.
    fn v_ar_act(&self) -> Option<VArAct> {
        None
    }

    /// ClcTotVA (ClcTotVA)
    ///
    /// Calculation method for total apparent power. 1=vector 2=arithmetic.
    fn clc_tot_va(&self) -> Option<ClcTotVa> {
        None
    }

    /// MaxRmpRte (MaxRmpRte)
    ///
    /// Setpoint for maximum ramp rate as percentage of nominal maximum ramp rate. This setting will limit the rate that watts delivery to the grid can increase or decrease in response to intermittent PV generation.
    fn max_rmp_rte(&self) -> Option<u16> {
        None
    }

    /// ECPNomHz (ECPNomHz)
    ///
    /// Setpoint for nominal frequency at the ECP.
    fn ecp_nom_hz(&self) -> Option<u16> {
        None
    }

    /// ConnPh (ConnPh)
    ///
    /// Identity of connected phase for single phase inverters. A=1 B=2 C=3.
    fn conn_ph(&self) -> Option<ConnPh> {
        None
    }

    /// WMax_SF (WMax_SF)
    ///
    /// Scale factor for real power.
    fn w_max_sf(&self) -> i16;

    /// VRef_SF (VRef_SF)
    ///
    /// Scale factor for voltage at the PCC.
    fn v_ref_sf(&self) -> i16;

    /// VRefOfs_SF (VRefOfs_SF)
    ///
    /// Scale factor for offset voltage.
    fn v_ref_ofs_sf(&self) -> i16;

    /// VMinMax_SF (VMinMax_SF)
    ///
    /// Scale factor for min/max voltages.
    fn v_min_max_sf(&self) -> Option<i16> {
        None
    }

    /// VAMax_SF (VAMax_SF)
    ///
    /// Scale factor for apparent power.
    fn va_max_sf(&self) -> Option<i16> {
        None
    }

    /// VArMax_SF (VArMax_SF)
    ///
    /// Scale factor for reactive power.
    fn v_ar_max_sf(&self) -> Option<i16> {
        None
    }

    /// WGra_SF (WGra_SF)
    ///
    /// Scale factor for default ramp rate.
    fn w_gra_sf(&self) -> Option<i16> {
        None
    }

    /// PFMin_SF (PFMin_SF)
    ///
    /// Scale factor for minimum power factor.
    fn pf_min_sf(&self) -> Option<i16> {
        None
    }

    /// MaxRmpRte_SF (MaxRmpRte_SF)
    ///
    /// Scale factor for maximum ramp percentage.
    fn max_rmp_rte_sf(&self) -> Option<i16> {
        None
    }

    /// ECPNomHz_SF (ECPNomHz_SF)
    ///
    /// Scale factor for nominal frequency.
    fn ecp_nom_hz_sf(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {
    /// WMax (WMax)
    ///
    /// Setting for maximum power output. Default to WRtg.
    fn set_w_max(&mut self, value: u16);

    /// VRef (VRef)
    ///
    /// Voltage at the PCC.
    fn set_v_ref(&mut self, value: u16);

    /// VRefOfs (VRefOfs)
    ///
    /// Offset from PCC to inverter.
    fn set_v_ref_ofs(&mut self, value: i16);

    /// VMax (VMax)
    ///
    /// Setpoint for maximum voltage.
    fn set_v_max(&mut self, value: u16) {}

    /// VMin (VMin)
    ///
    /// Setpoint for minimum voltage.
    fn set_v_min(&mut self, value: u16) {}

    /// VAMax (VAMax)
    ///
    /// Setpoint for maximum apparent power. Default to VARtg.
    fn set_va_max(&mut self, value: u16) {}

    /// VArMaxQ1 (VArMaxQ1)
    ///
    /// Setting for maximum reactive power in quadrant 1. Default to VArRtgQ1.
    fn set_v_ar_max_q1(&mut self, value: i16) {}

    /// VArMaxQ2 (VArMaxQ2)
    ///
    /// Setting for maximum reactive power in quadrant 2. Default to VArRtgQ2.
    fn set_v_ar_max_q2(&mut self, value: i16) {}

    /// VArMaxQ3 (VArMaxQ3)
    ///
    /// Setting for maximum reactive power in quadrant 3. Default to VArRtgQ3.
    fn set_v_ar_max_q3(&mut self, value: i16) {}

    /// VArMaxQ4 (VArMaxQ4)
    ///
    /// Setting for maximum reactive power in quadrant 4. Default to VArRtgQ4.
    fn set_v_ar_max_q4(&mut self, value: i16) {}

    /// WGra (WGra)
    ///
    /// Default ramp rate of change of active power due to command or internal action.
    fn set_w_gra(&mut self, value: u16) {}

    /// PFMinQ1 (PFMinQ1)
    ///
    /// Setpoint for minimum power factor value in quadrant 1. Default to PFRtgQ1.
    ///
    /// EEI sign convention.
    fn set_pf_min_q1(&mut self, value: i16) {}

    /// PFMinQ2 (PFMinQ2)
    ///
    /// Setpoint for minimum power factor value in quadrant 2. Default to PFRtgQ2.
    ///
    /// EEI sign convention.
    fn set_pf_min_q2(&mut self, value: i16) {}

    /// PFMinQ3 (PFMinQ3)
    ///
    /// Setpoint for minimum power factor value in quadrant 3. Default to PFRtgQ3.
    ///
    /// EEI sign convention.
    fn set_pf_min_q3(&mut self, value: i16) {}

    /// PFMinQ4 (PFMinQ4)
    ///
    /// Setpoint for minimum power factor value in quadrant 4. Default to PFRtgQ4.
    ///
    /// EEI sign convention.
    fn set_pf_min_q4(&mut self, value: i16) {}

    /// VArAct (VArAct)
    ///
    /// VAR action on change between charging and discharging: 1=switch 2=maintain VAR characterization.
    fn set_v_ar_act(&mut self, value: VArAct) {}

    /// ClcTotVA (ClcTotVA)
    ///
    /// Calculation method for total apparent power. 1=vector 2=arithmetic.
    fn set_clc_tot_va(&mut self, value: ClcTotVa) {}

    /// MaxRmpRte (MaxRmpRte)
    ///
    /// Setpoint for maximum ramp rate as percentage of nominal maximum ramp rate. This setting will limit the rate that watts delivery to the grid can increase or decrease in response to intermittent PV generation.
    fn set_max_rmp_rte(&mut self, value: u16) {}

    /// ECPNomHz (ECPNomHz)
    ///
    /// Setpoint for nominal frequency at the ECP.
    fn set_ecp_nom_hz(&mut self, value: u16) {}

    /// ConnPh (ConnPh)
    ///
    /// Identity of connected phase for single phase inverters. A=1 B=2 C=3.
    fn set_conn_ph(&mut self, value: ConnPh) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model121ClcTotVa {
    Vector = 1,
    Arithmetic = 2,
}

impl Model121ClcTotVa {
    pub fn from_repr(repr: u16) -> Option<Model121ClcTotVa> {
        match repr {
            1 => Some(Model121ClcTotVa::Vector),
            2 => Some(Model121ClcTotVa::Arithmetic),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model121ClcTotVa`] - only `Model121ClcTotVa` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `ClcTotVa` distinct from another model's point of the same name.
pub type ClcTotVa = Model121ClcTotVa;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model121ConnPh {
    A = 1,
    B = 2,
    C = 3,
}

impl Model121ConnPh {
    pub fn from_repr(repr: u16) -> Option<Model121ConnPh> {
        match repr {
            1 => Some(Model121ConnPh::A),
            2 => Some(Model121ConnPh::B),
            3 => Some(Model121ConnPh::C),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model121ConnPh`] - only `Model121ConnPh` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `ConnPh` distinct from another model's point of the same name.
pub type ConnPh = Model121ConnPh;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model121VArAct {
    Switch = 1,
    Maintain = 2,
}

impl Model121VArAct {
    pub fn from_repr(repr: u16) -> Option<Model121VArAct> {
        match repr {
            1 => Some(Model121VArAct::Switch),
            2 => Some(Model121VArAct::Maintain),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model121VArAct`] - only `Model121VArAct` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `VArAct` distinct from another model's point of the same name.
pub type VArAct = Model121VArAct;
