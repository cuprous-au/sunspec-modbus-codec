// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_708::*;

#[repr(C)]
pub struct Model708CallbackAdapter {
    context: *mut c_void,
    /// DER Trip HV Module Enable (Ena)
    ///
    /// DER high voltage trip control enable.
    der_trip_hv_module_enable_callback: extern "C" fn(*const c_void) -> Model708Ena,
    /// DER Trip HV Module Enable (Ena)
    ///
    /// DER high voltage trip control enable.
    set_der_trip_hv_module_enable_callback: extern "C" fn(Model708Ena, *mut c_void),
    /// Adopt Curve Request (AdptCrvReq)
    ///
    /// Index of curve points to adopt. First curve index is 1.
    adopt_curve_request_callback: extern "C" fn(*const c_void) -> u16,
    /// Adopt Curve Request (AdptCrvReq)
    ///
    /// Index of curve points to adopt. First curve index is 1.
    set_adopt_curve_request_callback: extern "C" fn(u16, *mut c_void),
    /// Adopt Curve Result (AdptCrvRslt)
    ///
    /// Result of last adopt curve operation.
    adopt_curve_result_callback: extern "C" fn(*const c_void) -> Model708AdptCrvRslt,
    /// Number Of Points (NPt)
    ///
    /// Number of curve points supported.
    number_of_points_callback: extern "C" fn(*const c_void) -> u16,
    /// Stored Curve Count (NCrvSet)
    ///
    /// Number of stored curves supported.
    stored_curve_count_callback: extern "C" fn(*const c_void) -> u16,
    /// Voltage Scale Factor (V_SF)
    ///
    /// Scale factor for curve voltage points.
    voltage_scale_factor_callback: extern "C" fn(*const c_void) -> i16,
    /// Time Point Scale Factor (Tms_SF)
    ///
    /// Scale factor for curve time points.
    time_point_scale_factor_callback: extern "C" fn(*const c_void) -> i16,
    /// Curve Access (ReadOnly)
    ///
    /// Curve read-write access.
    crv_curve_access_callback: extern "C" fn(*const c_void, u16) -> Model708ReadOnly,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in must trip curve.
    must_trip_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(*const c_void, u16) -> u16>,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in must trip curve.
    set_must_trip_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(u16, *mut c_void, u16)>,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in may trip curve.
    may_trip_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(*const c_void, u16) -> u16>,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in may trip curve.
    set_may_trip_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(u16, *mut c_void, u16)>,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in the momentary cessation curve.
    momentary_cessation_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(*const c_void, u16) -> u16>,
    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in the momentary cessation curve.
    set_momentary_cessation_curve_crv_number_of_active_points_callback:
        Option<extern "C" fn(u16, *mut c_void, u16)>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    must_trip_curve_pt_voltage_point_callback:
        Option<extern "C" fn(*const c_void, u16, u16) -> u16>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    set_must_trip_curve_pt_voltage_point_callback:
        Option<extern "C" fn(u16, *mut c_void, u16, u16)>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    must_trip_curve_pt_time_point_callback: Option<extern "C" fn(*const c_void, u16, u16) -> u32>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    set_must_trip_curve_pt_time_point_callback: Option<extern "C" fn(u32, *mut c_void, u16, u16)>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    may_trip_curve_pt_voltage_point_callback: Option<extern "C" fn(*const c_void, u16, u16) -> u16>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    set_may_trip_curve_pt_voltage_point_callback: Option<extern "C" fn(u16, *mut c_void, u16, u16)>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    may_trip_curve_pt_time_point_callback: Option<extern "C" fn(*const c_void, u16, u16) -> u32>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    set_may_trip_curve_pt_time_point_callback: Option<extern "C" fn(u32, *mut c_void, u16, u16)>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    momentary_cessation_curve_pt_voltage_point_callback:
        Option<extern "C" fn(*const c_void, u16, u16) -> u16>,
    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    set_momentary_cessation_curve_pt_voltage_point_callback:
        Option<extern "C" fn(u16, *mut c_void, u16, u16)>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    momentary_cessation_curve_pt_time_point_callback:
        Option<extern "C" fn(*const c_void, u16, u16) -> u32>,
    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    set_momentary_cessation_curve_pt_time_point_callback:
        Option<extern "C" fn(u32, *mut c_void, u16, u16)>,
}

impl ReadAdapter for Model708CallbackAdapter {
    fn der_trip_hv_module_enable(&self) -> Ena {
        (self.der_trip_hv_module_enable_callback)(self.context)
    }

    fn adopt_curve_request(&self) -> u16 {
        (self.adopt_curve_request_callback)(self.context)
    }

    fn adopt_curve_result(&self) -> AdptCrvRslt {
        (self.adopt_curve_result_callback)(self.context)
    }

    fn number_of_points(&self) -> u16 {
        (self.number_of_points_callback)(self.context)
    }

    fn stored_curve_count(&self) -> u16 {
        (self.stored_curve_count_callback)(self.context)
    }

    fn voltage_scale_factor(&self) -> i16 {
        (self.voltage_scale_factor_callback)(self.context)
    }

    fn time_point_scale_factor(&self) -> i16 {
        (self.time_point_scale_factor_callback)(self.context)
    }

    fn crv_curve_access(&self, crv_index: u16) -> ReadOnly {
        (self.crv_curve_access_callback)(self.context, crv_index)
    }

    fn must_trip_curve_crv_number_of_active_points(&self, crv_index: u16) -> Option<u16> {
        self.must_trip_curve_crv_number_of_active_points_callback
            .map(|callback| (callback)(self.context, crv_index))
    }

    fn may_trip_curve_crv_number_of_active_points(&self, crv_index: u16) -> Option<u16> {
        self.may_trip_curve_crv_number_of_active_points_callback
            .map(|callback| (callback)(self.context, crv_index))
    }

    fn momentary_cessation_curve_crv_number_of_active_points(&self, crv_index: u16) -> Option<u16> {
        self.momentary_cessation_curve_crv_number_of_active_points_callback
            .map(|callback| (callback)(self.context, crv_index))
    }

    fn must_trip_curve_pt_voltage_point(&self, crv_index: u16, pt_index: u16) -> Option<u16> {
        self.must_trip_curve_pt_voltage_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }

    fn must_trip_curve_pt_time_point(&self, crv_index: u16, pt_index: u16) -> Option<u32> {
        self.must_trip_curve_pt_time_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }

    fn may_trip_curve_pt_voltage_point(&self, crv_index: u16, pt_index: u16) -> Option<u16> {
        self.may_trip_curve_pt_voltage_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }

    fn may_trip_curve_pt_time_point(&self, crv_index: u16, pt_index: u16) -> Option<u32> {
        self.may_trip_curve_pt_time_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }

    fn momentary_cessation_curve_pt_voltage_point(
        &self,
        crv_index: u16,
        pt_index: u16,
    ) -> Option<u16> {
        self.momentary_cessation_curve_pt_voltage_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }

    fn momentary_cessation_curve_pt_time_point(
        &self,
        crv_index: u16,
        pt_index: u16,
    ) -> Option<u32> {
        self.momentary_cessation_curve_pt_time_point_callback
            .map(|callback| (callback)(self.context, crv_index, pt_index))
    }
}

impl WriteAdapter for Model708CallbackAdapter {
    /// DER Trip HV Module Enable (Ena)
    ///
    /// DER high voltage trip control enable.
    fn set_der_trip_hv_module_enable(&mut self, value: Ena) {
        (self.set_der_trip_hv_module_enable_callback)(value, self.context);
    }

    /// Adopt Curve Request (AdptCrvReq)
    ///
    /// Index of curve points to adopt. First curve index is 1.
    fn set_adopt_curve_request(&mut self, value: u16) {
        (self.set_adopt_curve_request_callback)(value, self.context);
    }

    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in must trip curve.
    fn set_must_trip_curve_crv_number_of_active_points(&mut self, value: u16, crv_index: u16) {
        if let Some(callback) = self.set_must_trip_curve_crv_number_of_active_points_callback {
            (callback)(value, self.context, crv_index);
        };
    }

    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in may trip curve.
    fn set_may_trip_curve_crv_number_of_active_points(&mut self, value: u16, crv_index: u16) {
        if let Some(callback) = self.set_may_trip_curve_crv_number_of_active_points_callback {
            (callback)(value, self.context, crv_index);
        };
    }

    /// Number Of Active Points (ActPt)
    ///
    /// Number of active points in the momentary cessation curve.
    fn set_momentary_cessation_curve_crv_number_of_active_points(
        &mut self,
        value: u16,
        crv_index: u16,
    ) {
        if let Some(callback) =
            self.set_momentary_cessation_curve_crv_number_of_active_points_callback
        {
            (callback)(value, self.context, crv_index);
        };
    }

    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    fn set_must_trip_curve_pt_voltage_point(&mut self, value: u16, crv_index: u16, pt_index: u16) {
        if let Some(callback) = self.set_must_trip_curve_pt_voltage_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }

    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    ///
    /// Internal curve conformance checks should be conducted when AdptCrvReq is set to 1, not on point writes.
    fn set_must_trip_curve_pt_time_point(&mut self, value: u32, crv_index: u16, pt_index: u16) {
        if let Some(callback) = self.set_must_trip_curve_pt_time_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }

    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    fn set_may_trip_curve_pt_voltage_point(&mut self, value: u16, crv_index: u16, pt_index: u16) {
        if let Some(callback) = self.set_may_trip_curve_pt_voltage_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }

    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    fn set_may_trip_curve_pt_time_point(&mut self, value: u32, crv_index: u16, pt_index: u16) {
        if let Some(callback) = self.set_may_trip_curve_pt_time_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }

    /// Voltage Point (V)
    ///
    /// Curve voltage point as percentage.
    fn set_momentary_cessation_curve_pt_voltage_point(
        &mut self,
        value: u16,
        crv_index: u16,
        pt_index: u16,
    ) {
        if let Some(callback) = self.set_momentary_cessation_curve_pt_voltage_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }

    /// Time Point (Tms)
    ///
    /// Curve time point in seconds.
    fn set_momentary_cessation_curve_pt_time_point(
        &mut self,
        value: u32,
        crv_index: u16,
        pt_index: u16,
    ) {
        if let Some(callback) = self.set_momentary_cessation_curve_pt_time_point_callback {
            (callback)(value, self.context, crv_index, pt_index);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 708. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_708: StaticModelSpec = StaticModelSpec {
    id: 708,
    length: model_708_c_length,
    writable: true,
    visit_read: model_708_c_visit_read,
    visit_write: model_708_c_visit_write,
};

fn model_708_c_length(repeat_count_0: u16, repeat_count_1: u16) -> u16 {
    (Model708 {
        stored_curve_count: repeat_count_0,
        number_of_points: repeat_count_1,
    })
    .model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model708CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_708_c_visit_read(
    adapter: *const c_void,
    repeat_count_0: u16,
    repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model708 {
        stored_curve_count: repeat_count_0,
        number_of_points: repeat_count_1,
    };
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model708CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_708_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_708_c_visit_write(
    adapter: *mut c_void,
    repeat_count_0: u16,
    repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model708 {
        stored_curve_count: repeat_count_0,
        number_of_points: repeat_count_1,
    };
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model708CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 708 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_708_callback(
    adapter: *mut Model708CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_708,
        adapter: adapter as *mut c_void,
    }
}
