// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;
use core::ffi::CStr;

static POINTS: [PointDetails<()>; 10] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Name,
        size: 4,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::Capabilities,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Config,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::Typ,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::Address,
        size: 20,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::Port,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::Username,
        size: 12,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::Password,
        size: 12,
        start_address: 42,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Name,
    Capabilities,
    Config,
    Typ,
    Address,
    Port,
    Username,
    Password,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 14. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model14;

impl<'ad> ModelSpec<'ad> for Model14 {
    const MODEL_ID: u16 = 14;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        54
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model14,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(14);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Name => {
            if let Some(value) = adapter.name() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Capabilities => {
            buffer.write_u16(adapter.capabilities());
        }
        Point::Config => {
            buffer.write_u16(adapter.config());
        }
        Point::Typ => {
            buffer.write_u16(adapter.typ());
        }
        Point::Address => {
            buffer.write_string(adapter.address(), offset);
        }
        Point::Port => {
            buffer.write_u16(adapter.port());
        }
        Point::Username => {
            if let Some(value) = adapter.username() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Password => {
            if let Some(value) = adapter.password() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model14,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::Name => {
            let value = buffer.read_string::<8>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_name(str_value);
            Ok(())
        }
        Point::Capabilities => {
            let value = buffer.read_u16()?;
            adapter.set_capabilities(value);
            Ok(())
        }
        Point::Config => {
            let value = buffer.read_u16()?;
            adapter.set_config(value);
            Ok(())
        }
        Point::Typ => {
            let value = buffer.read_u16()?;
            adapter.set_typ(value);
            Ok(())
        }
        Point::Address => {
            let value = buffer.read_string::<40>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_address(str_value);
            Ok(())
        }
        Point::Port => {
            let value = buffer.read_u16()?;
            adapter.set_port(value);
            Ok(())
        }
        Point::Username => {
            let value = buffer.read_string::<24>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_username(str_value);
            Ok(())
        }
        Point::Password => {
            let value = buffer.read_string::<24>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_password(str_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn name(&self) -> Option<&CStr> {
        None
    }

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn capabilities(&self) -> u16;

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn config(&self) -> u16;

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn typ(&self) -> u16;

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn address(&self) -> &CStr;

    /// Port (Port)
    ///
    /// Proxy port number
    fn port(&self) -> u16;

    /// Username (User)
    ///
    /// Proxy user name
    fn username(&self) -> Option<&CStr> {
        None
    }

    /// Password (Pw)
    ///
    /// Proxy password
    fn password(&self) -> Option<&CStr> {
        None
    }
}

pub trait WriteAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn set_name(&mut self, value: &CStr) {}

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn set_capabilities(&mut self, value: u16);

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn set_config(&mut self, value: u16);

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn set_typ(&mut self, value: u16);

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn set_address(&mut self, value: &CStr);

    /// Port (Port)
    ///
    /// Proxy port number
    fn set_port(&mut self, value: u16);

    /// Username (User)
    ///
    /// Proxy user name
    fn set_username(&mut self, value: &CStr) {}

    /// Password (Pw)
    ///
    /// Proxy password
    fn set_password(&mut self, value: &CStr) {}
}
