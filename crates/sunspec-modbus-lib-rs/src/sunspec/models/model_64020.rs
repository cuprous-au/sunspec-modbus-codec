// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;
use core::ffi::CStr;

static POINTS: [PointDetails<()>; 32] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Aux0Temperature,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::Aux1Temperature,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::Aux2Temperature,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::Aux3Temperature,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::Aux4Temperature,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::ProbeTemperature,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::MainTemperature,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::VoltageScaleFactorForTheSensors,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::CurrentScaleFactorForTheSensors,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::FrequencyScaleFactorForTheSensors,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::Sensor1Voltage,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::Sensor2Voltage,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::Sensor3Voltage,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::Sensor4Voltage,
        size: 1,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::Sensor5Voltage,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::Sensor6Voltage,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::Sensor7Voltage,
        size: 1,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::Sensor1Current,
        size: 1,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::Sensor2Current,
        size: 1,
        start_address: 20,
    },
    PointDetails {
        point: |()| Point::Sensor3Current,
        size: 1,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::Sensor4Current,
        size: 1,
        start_address: 22,
    },
    PointDetails {
        point: |()| Point::Sensor5Current,
        size: 1,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::Sensor6Current,
        size: 1,
        start_address: 24,
    },
    PointDetails {
        point: |()| Point::Sensor7Current,
        size: 1,
        start_address: 25,
    },
    PointDetails {
        point: |()| Point::Sensor8Frequency,
        size: 1,
        start_address: 26,
    },
    PointDetails {
        point: |()| Point::Relay1State,
        size: 1,
        start_address: 27,
    },
    PointDetails {
        point: |()| Point::Relay2State,
        size: 1,
        start_address: 28,
    },
    PointDetails {
        point: |()| Point::Relay3State,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::ResetTheAccumulators,
        size: 1,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::ResetTheSystem,
        size: 1,
        start_address: 31,
    },
];

static REPEATING_POINTS: [PointDetails<()>; 3] = [
    PointDetails {
        point: |()| Point::RepeatingSerialNumber,
        size: 9,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::RepeatingFirmwareVersion,
        size: 6,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::RepeatingHardwareVersion,
        size: 1,
        start_address: 15,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Aux0Temperature,
    Aux1Temperature,
    Aux2Temperature,
    Aux3Temperature,
    Aux4Temperature,
    ProbeTemperature,
    MainTemperature,
    VoltageScaleFactorForTheSensors,
    CurrentScaleFactorForTheSensors,
    FrequencyScaleFactorForTheSensors,
    Sensor1Voltage,
    Sensor2Voltage,
    Sensor3Voltage,
    Sensor4Voltage,
    Sensor5Voltage,
    Sensor6Voltage,
    Sensor7Voltage,
    Sensor1Current,
    Sensor2Current,
    Sensor3Current,
    Sensor4Current,
    Sensor5Current,
    Sensor6Current,
    Sensor7Current,
    Sensor8Frequency,
    Relay1State,
    Relay2State,
    Relay3State,
    ResetTheAccumulators,
    ResetTheSystem,
    RepeatingSerialNumber,
    RepeatingFirmwareVersion,
    RepeatingHardwareVersion,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 64020. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model64020;

impl<'ad> ModelSpec<'ad> for Model64020 {
    const MODEL_ID: u16 = 64020;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        48
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                REPEATING_POINTS
                    .iter()
                    .map(move |p| (32 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .chain(
                REPEATING_POINTS
                    .iter()
                    .map(move |p| (32 + p.start_address, p.size, (p.point)(()))),
            )
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model64020,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(64020);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Aux0Temperature => {
            if let Some(value) = adapter.aux_0_temperature() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Aux1Temperature => {
            if let Some(value) = adapter.aux_1_temperature() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Aux2Temperature => {
            if let Some(value) = adapter.aux_2_temperature() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Aux3Temperature => {
            if let Some(value) = adapter.aux_3_temperature() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Aux4Temperature => {
            if let Some(value) = adapter.aux_4_temperature() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ProbeTemperature => {
            buffer.write_i16(adapter.probe_temperature());
        }
        Point::MainTemperature => {
            buffer.write_i16(adapter.main_temperature());
        }
        Point::VoltageScaleFactorForTheSensors => {
            buffer.write_i16(adapter.voltage_scale_factor_for_the_sensors());
        }
        Point::CurrentScaleFactorForTheSensors => {
            buffer.write_i16(adapter.current_scale_factor_for_the_sensors());
        }
        Point::FrequencyScaleFactorForTheSensors => {
            buffer.write_i16(adapter.frequency_scale_factor_for_the_sensors());
        }
        Point::Sensor1Voltage => {
            if let Some(value) = adapter.sensor1_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor2Voltage => {
            if let Some(value) = adapter.sensor2_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor3Voltage => {
            if let Some(value) = adapter.sensor3_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor4Voltage => {
            if let Some(value) = adapter.sensor4_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor5Voltage => {
            if let Some(value) = adapter.sensor5_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor6Voltage => {
            if let Some(value) = adapter.sensor6_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor7Voltage => {
            if let Some(value) = adapter.sensor7_voltage() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor1Current => {
            if let Some(value) = adapter.sensor1_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor2Current => {
            if let Some(value) = adapter.sensor2_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor3Current => {
            if let Some(value) = adapter.sensor3_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor4Current => {
            if let Some(value) = adapter.sensor4_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor5Current => {
            if let Some(value) = adapter.sensor5_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor6Current => {
            if let Some(value) = adapter.sensor6_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor7Current => {
            if let Some(value) = adapter.sensor7_current() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Sensor8Frequency => {
            if let Some(value) = adapter.sensor8_frequency() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Relay1State => {
            if let Some(value) = adapter.relay_1_state() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Relay2State => {
            if let Some(value) = adapter.relay_2_state() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::Relay3State => {
            if let Some(value) = adapter.relay_3_state() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ResetTheAccumulators => {
            if let Some(value) = adapter.reset_the_accumulators() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ResetTheSystem => {
            if let Some(value) = adapter.reset_the_system() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::RepeatingSerialNumber => {
            buffer.write_string(adapter.repeating_serial_number(), offset);
        }
        Point::RepeatingFirmwareVersion => {
            buffer.write_string(adapter.repeating_firmware_version(), offset);
        }
        Point::RepeatingHardwareVersion => {
            buffer.write_u16(adapter.repeating_hardware_version());
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model64020,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    Err(ModbusException::IllegalDataAddress)
}

pub trait ReadAdapter {
    /// Aux 0 temperature (Aux0Tmp)
    fn aux_0_temperature(&self) -> Option<i16> {
        None
    }

    /// Aux 1 temperature (Aux1Tmp)
    fn aux_1_temperature(&self) -> Option<i16> {
        None
    }

    /// Aux 2 temperature (Aux2Tmp)
    fn aux_2_temperature(&self) -> Option<i16> {
        None
    }

    /// Aux 3 temperature (Aux3Tmp)
    fn aux_3_temperature(&self) -> Option<i16> {
        None
    }

    /// Aux 4 temperature (Aux4Tmp)
    fn aux_4_temperature(&self) -> Option<i16> {
        None
    }

    /// Probe Temperature (ProbeTmp)
    fn probe_temperature(&self) -> i16;

    /// Main Temperature (MainTmp)
    fn main_temperature(&self) -> i16;

    /// Voltage scale factor for the sensors (SensorV_SF)
    fn voltage_scale_factor_for_the_sensors(&self) -> i16;

    /// Current scale factor for the sensors (SensorA_SF)
    fn current_scale_factor_for_the_sensors(&self) -> i16;

    /// Frequency scale factor for the sensors (SensorHz_SF)
    fn frequency_scale_factor_for_the_sensors(&self) -> i16;

    /// Sensor1 Voltage (Sensor1Voltage)
    ///
    /// scale of 0-10V
    fn sensor1_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor2 Voltage (Sensor2Voltage)
    ///
    /// scale of 0-10V
    fn sensor2_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor3 Voltage (Sensor3Voltage)
    ///
    /// scale of 0-10V
    fn sensor3_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor4 Voltage (Sensor4Voltage)
    ///
    /// scale of 0-10V
    fn sensor4_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor5 Voltage (Sensor5Voltage)
    ///
    /// scale of 0-10V
    fn sensor5_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor6 Voltage (Sensor6Voltage)
    ///
    /// scale of 0-10V
    fn sensor6_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor7 Voltage (Sensor7Voltage)
    ///
    /// scale of 0-10V
    fn sensor7_voltage(&self) -> Option<i16> {
        None
    }

    /// Sensor1 Current (Sensor1Current)
    ///
    /// scale of 4-20mA
    fn sensor1_current(&self) -> Option<i16> {
        None
    }

    /// Sensor2 Current (Sensor2Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor2_current(&self) -> Option<i16> {
        None
    }

    /// Sensor3 Current (Sensor3Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor3_current(&self) -> Option<i16> {
        None
    }

    /// Sensor4 Current (Sensor4Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor4_current(&self) -> Option<i16> {
        None
    }

    /// Sensor5 Current (Sensor5Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor5_current(&self) -> Option<i16> {
        None
    }

    /// Sensor6 Current (Sensor6Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor6_current(&self) -> Option<i16> {
        None
    }

    /// Sensor7 Current (Sensor7Current)
    ///
    /// in 4-20mA or 4-20mA
    fn sensor7_current(&self) -> Option<i16> {
        None
    }

    /// Sensor8 frequency (Sensor8)
    ///
    /// frequency in Hz
    fn sensor8_frequency(&self) -> Option<u16> {
        None
    }

    /// Relay 1 state (Relay1)
    fn relay_1_state(&self) -> Option<u16> {
        None
    }

    /// Relay 2 state (Relay2)
    fn relay_2_state(&self) -> Option<u16> {
        None
    }

    /// Relay 3 state (Relay3)
    fn relay_3_state(&self) -> Option<u16> {
        None
    }

    /// Reset the accumulators (ResetAccumulators)
    ///
    /// always 0 in reading, used the code 0xC0DA during the writing for resetting them
    fn reset_the_accumulators(&self) -> Option<u16> {
        None
    }

    /// Reset the system (Reset)
    ///
    /// always 0 in reading, used the code 0xC0DA during the writing for resetting the system
    fn reset_the_system(&self) -> Option<u16> {
        None
    }

    /// Serial number (SerialNumber)
    ///
    /// strings of 16 characters
    fn repeating_serial_number(&self) -> &CStr;

    /// Firmware version (Firmware)
    ///
    /// string of 11 characters
    fn repeating_firmware_version(&self) -> &CStr;

    /// Hardware version (Hardware)
    fn repeating_hardware_version(&self) -> u16;
}

pub trait WriteAdapter {}
