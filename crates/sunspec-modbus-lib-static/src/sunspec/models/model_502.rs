// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_502::*;

#[repr(C)]
pub struct Model502CallbackAdapter {
    context: *mut c_void,
    /// A_SF
    ///
    /// Current scale factor
    a_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// V_SF
    ///
    /// Voltage scale factor
    v_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// W_SF
    ///
    /// Power scale factor
    w_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Wh_SF
    ///
    /// Energy scale factor
    wh_sf_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Status (Stat)
    ///
    /// Module Status Code
    status_callback: extern "C" fn(*const c_void) -> Model502Stat,
    /// Vendor Status (StatVend)
    ///
    /// Module Vendor Status Code
    vendor_status_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Events (Evt)
    ///
    /// Module Event Flags
    events_callback: extern "C" fn(*const c_void) -> u32,
    /// Vendor Module Event Flags (EvtVend)
    ///
    /// Vendor specific flags
    vendor_module_event_flags_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Control (Ctl)
    ///
    /// Module Control
    control_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// Control (Ctl)
    ///
    /// Module Control
    set_control_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    vendor_control_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    set_vendor_control_callback: Option<extern "C" fn(u32, *mut c_void)>,
    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    control_value_callback: Option<extern "C" fn(*const c_void) -> i32>,
    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    set_control_value_callback: Option<extern "C" fn(i32, *mut c_void)>,
    /// Timestamp (Tms)
    ///
    /// Time in seconds since 2000 epoch
    timestamp_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Output Current (OutA)
    ///
    /// Output Current
    output_current_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Output Voltage (OutV)
    ///
    /// Output Voltage
    output_voltage_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Output Energy (OutWh)
    ///
    /// Output Energy
    output_energy_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Output Power (OutPw)
    ///
    /// Output Power
    output_power_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Temp (Tmp)
    ///
    /// Module Temperature
    temp_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Input Current (InA)
    ///
    /// Input Current
    input_current_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Input Voltage (InV)
    ///
    /// Input Voltage
    input_voltage_callback: Option<extern "C" fn(*const c_void) -> i16>,
    /// Input Energy (InWh)
    ///
    /// Input Energy
    input_energy_callback: Option<extern "C" fn(*const c_void) -> u32>,
    /// Input Power (InW)
    ///
    /// Input Power
    input_power_callback: Option<extern "C" fn(*const c_void) -> i16>,
}

impl ReadAdapter for Model502CallbackAdapter {
    fn a_sf(&self) -> Option<i16> {
        self.a_sf_callback.map(|callback| (callback)(self.context))
    }

    fn v_sf(&self) -> Option<i16> {
        self.v_sf_callback.map(|callback| (callback)(self.context))
    }

    fn w_sf(&self) -> Option<i16> {
        self.w_sf_callback.map(|callback| (callback)(self.context))
    }

    fn wh_sf(&self) -> Option<i16> {
        self.wh_sf_callback.map(|callback| (callback)(self.context))
    }

    fn status(&self) -> Stat {
        (self.status_callback)(self.context)
    }

    fn vendor_status(&self) -> Option<u16> {
        self.vendor_status_callback
            .map(|callback| (callback)(self.context))
    }

    fn events(&self) -> u32 {
        (self.events_callback)(self.context)
    }

    fn vendor_module_event_flags(&self) -> Option<u32> {
        self.vendor_module_event_flags_callback
            .map(|callback| (callback)(self.context))
    }

    fn control(&self) -> Option<u16> {
        self.control_callback
            .map(|callback| (callback)(self.context))
    }

    fn vendor_control(&self) -> Option<u32> {
        self.vendor_control_callback
            .map(|callback| (callback)(self.context))
    }

    fn control_value(&self) -> Option<i32> {
        self.control_value_callback
            .map(|callback| (callback)(self.context))
    }

    fn timestamp(&self) -> Option<u32> {
        self.timestamp_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_current(&self) -> Option<i16> {
        self.output_current_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_voltage(&self) -> Option<i16> {
        self.output_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_energy(&self) -> Option<u32> {
        self.output_energy_callback
            .map(|callback| (callback)(self.context))
    }

    fn output_power(&self) -> Option<i16> {
        self.output_power_callback
            .map(|callback| (callback)(self.context))
    }

    fn temp(&self) -> Option<i16> {
        self.temp_callback.map(|callback| (callback)(self.context))
    }

    fn input_current(&self) -> Option<i16> {
        self.input_current_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_voltage(&self) -> Option<i16> {
        self.input_voltage_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_energy(&self) -> Option<u32> {
        self.input_energy_callback
            .map(|callback| (callback)(self.context))
    }

    fn input_power(&self) -> Option<i16> {
        self.input_power_callback
            .map(|callback| (callback)(self.context))
    }
}

impl WriteAdapter for Model502CallbackAdapter {
    /// Control (Ctl)
    ///
    /// Module Control
    fn set_control(&mut self, value: u16) {
        if let Some(callback) = self.set_control_callback {
            (callback)(value, self.context);
        };
    }

    /// Vendor Control (CtlVend)
    ///
    /// Vendor Module Control
    fn set_vendor_control(&mut self, value: u32) {
        if let Some(callback) = self.set_vendor_control_callback {
            (callback)(value, self.context);
        };
    }

    /// Control Value (CtlVal)
    ///
    /// Module Control Value
    fn set_control_value(&mut self, value: i32) {
        if let Some(callback) = self.set_control_value_callback {
            (callback)(value, self.context);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 502. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_502: StaticModelSpec = StaticModelSpec {
    id: 502,
    length: model_502_c_length,
    writable: true,
    visit_read: model_502_c_visit_read,
    visit_write: model_502_c_visit_write,
};

fn model_502_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model502).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model502CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_502_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model502;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model502CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_502_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_502_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model502;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model502CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 502 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_502_callback(
    adapter: *mut Model502CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_502,
        adapter: adapter as *mut c_void,
    }
}
