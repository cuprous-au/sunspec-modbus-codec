// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_125::*;

#[repr(C)]
pub struct Model125CallbackAdapter {
    context: *mut c_void,
    /// ModEna (ModEna)
    ///
    /// Is price-based charge/discharge mode active?
    mod_ena_callback: extern "C" fn(*const c_void) -> u16,
    /// ModEna (ModEna)
    ///
    /// Is price-based charge/discharge mode active?
    set_mod_ena_callback: extern "C" fn(u16, *mut c_void),
    /// SigType (SigType)
    ///
    /// Meaning of the pricing signal. When a Price schedule is used, type must match the schedule range variable description.
    sig_type_callback: Option<extern "C" fn(*const c_void) -> Model125SigType>,
    /// SigType (SigType)
    ///
    /// Meaning of the pricing signal. When a Price schedule is used, type must match the schedule range variable description.
    set_sig_type_callback: Option<extern "C" fn(Model125SigType, *mut c_void)>,
    /// Sig (Sig)
    ///
    /// Utility/ESP specific pricing signal. Content depends on pricing signal type. When H/M/L type is specified. Low=0; Med=1; High=2.
    sig_callback: extern "C" fn(*const c_void) -> i16,
    /// Sig (Sig)
    ///
    /// Utility/ESP specific pricing signal. Content depends on pricing signal type. When H/M/L type is specified. Low=0; Med=1; High=2.
    set_sig_callback: extern "C" fn(i16, *mut c_void),
    /// WinTms (WinTms)
    ///
    /// Time window for charge/discharge pricing change.
    win_tms_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// WinTms (WinTms)
    ///
    /// Time window for charge/discharge pricing change.
    set_win_tms_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// RvtTms (RvtTms)
    ///
    /// Timeout period for charge/discharge pricing change.
    rvt_tms_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// RvtTms (RvtTms)
    ///
    /// Timeout period for charge/discharge pricing change.
    set_rvt_tms_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// RmpTms (RmpTms)
    ///
    /// Ramp time for moving from current charge or discharge level to new level.
    rmp_tms_callback: Option<extern "C" fn(*const c_void) -> u16>,
    /// RmpTms (RmpTms)
    ///
    /// Ramp time for moving from current charge or discharge level to new level.
    set_rmp_tms_callback: Option<extern "C" fn(u16, *mut c_void)>,
    /// Sig_SF (Sig_SF)
    ///
    /// Pricing signal scale factor.
    sig_sf_callback: extern "C" fn(*const c_void) -> i16,
}

impl ReadAdapter for Model125CallbackAdapter {
    fn mod_ena(&self) -> u16 {
        (self.mod_ena_callback)(self.context)
    }

    fn sig_type(&self) -> Option<SigType> {
        self.sig_type_callback
            .map(|callback| (callback)(self.context))
    }

    fn sig(&self) -> i16 {
        (self.sig_callback)(self.context)
    }

    fn win_tms(&self) -> Option<u16> {
        self.win_tms_callback
            .map(|callback| (callback)(self.context))
    }

    fn rvt_tms(&self) -> Option<u16> {
        self.rvt_tms_callback
            .map(|callback| (callback)(self.context))
    }

    fn rmp_tms(&self) -> Option<u16> {
        self.rmp_tms_callback
            .map(|callback| (callback)(self.context))
    }

    fn sig_sf(&self) -> i16 {
        (self.sig_sf_callback)(self.context)
    }
}

impl WriteAdapter for Model125CallbackAdapter {
    /// ModEna (ModEna)
    ///
    /// Is price-based charge/discharge mode active?
    fn set_mod_ena(&mut self, value: u16) {
        (self.set_mod_ena_callback)(value, self.context);
    }

    /// SigType (SigType)
    ///
    /// Meaning of the pricing signal. When a Price schedule is used, type must match the schedule range variable description.
    fn set_sig_type(&mut self, value: SigType) {
        if let Some(callback) = self.set_sig_type_callback {
            (callback)(value, self.context);
        };
    }

    /// Sig (Sig)
    ///
    /// Utility/ESP specific pricing signal. Content depends on pricing signal type. When H/M/L type is specified. Low=0; Med=1; High=2.
    fn set_sig(&mut self, value: i16) {
        (self.set_sig_callback)(value, self.context);
    }

    /// WinTms (WinTms)
    ///
    /// Time window for charge/discharge pricing change.
    fn set_win_tms(&mut self, value: u16) {
        if let Some(callback) = self.set_win_tms_callback {
            (callback)(value, self.context);
        };
    }

    /// RvtTms (RvtTms)
    ///
    /// Timeout period for charge/discharge pricing change.
    fn set_rvt_tms(&mut self, value: u16) {
        if let Some(callback) = self.set_rvt_tms_callback {
            (callback)(value, self.context);
        };
    }

    /// RmpTms (RmpTms)
    ///
    /// Ramp time for moving from current charge or discharge level to new level.
    fn set_rmp_tms(&mut self, value: u16) {
        if let Some(callback) = self.set_rmp_tms_callback {
            (callback)(value, self.context);
        };
    }
}

/// C-FFI dispatch descriptor for SunSpec model 125. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_125: StaticModelSpec = StaticModelSpec {
    id: 125,
    length: model_125_c_length,
    writable: true,
    visit_read: model_125_c_visit_read,
    visit_write: model_125_c_visit_write,
};

fn model_125_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model125).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model125CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_125_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model125;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model125CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_125_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_125_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model125;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *mut Model125CallbackAdapter).as_mut() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}

/// Build a [`SunspecAdapter`] for model 125 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_125_callback(
    adapter: *mut Model125CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_125,
        adapter: adapter as *mut c_void,
    }
}
