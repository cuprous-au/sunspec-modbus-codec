// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::model::ModelSpec;
use core::cmp::min;

static POINTS: [PointDetails<()>; 26] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::WChaMax,
        size: 1,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::WChaGra,
        size: 1,
        start_address: 3,
    },
    PointDetails {
        point: |()| Point::WDisChaGra,
        size: 1,
        start_address: 4,
    },
    PointDetails {
        point: |()| Point::StorCtlMod,
        size: 1,
        start_address: 5,
    },
    PointDetails {
        point: |()| Point::VaChaMax,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::MinRsvPct,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::ChaState,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::StorAval,
        size: 1,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::InBatV,
        size: 1,
        start_address: 10,
    },
    PointDetails {
        point: |()| Point::ChaSt,
        size: 1,
        start_address: 11,
    },
    PointDetails {
        point: |()| Point::OutWRte,
        size: 1,
        start_address: 12,
    },
    PointDetails {
        point: |()| Point::InWRte,
        size: 1,
        start_address: 13,
    },
    PointDetails {
        point: |()| Point::InOutWRteWinTms,
        size: 1,
        start_address: 14,
    },
    PointDetails {
        point: |()| Point::InOutWRteRvrtTms,
        size: 1,
        start_address: 15,
    },
    PointDetails {
        point: |()| Point::InOutWRteRmpTms,
        size: 1,
        start_address: 16,
    },
    PointDetails {
        point: |()| Point::ChaGriSet,
        size: 1,
        start_address: 17,
    },
    PointDetails {
        point: |()| Point::WChaMaxSf,
        size: 1,
        start_address: 18,
    },
    PointDetails {
        point: |()| Point::WChaDisChaGraSf,
        size: 1,
        start_address: 19,
    },
    PointDetails {
        point: |()| Point::VaChaMaxSf,
        size: 1,
        start_address: 20,
    },
    PointDetails {
        point: |()| Point::MinRsvPctSf,
        size: 1,
        start_address: 21,
    },
    PointDetails {
        point: |()| Point::ChaStateSf,
        size: 1,
        start_address: 22,
    },
    PointDetails {
        point: |()| Point::StorAvalSf,
        size: 1,
        start_address: 23,
    },
    PointDetails {
        point: |()| Point::InBatVSf,
        size: 1,
        start_address: 24,
    },
    PointDetails {
        point: |()| Point::InOutWRteSf,
        size: 1,
        start_address: 25,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    WChaMax,
    WChaGra,
    WDisChaGra,
    StorCtlMod,
    VaChaMax,
    MinRsvPct,
    ChaState,
    StorAval,
    InBatV,
    ChaSt,
    OutWRte,
    InWRte,
    InOutWRteWinTms,
    InOutWRteRvrtTms,
    InOutWRteRmpTms,
    ChaGriSet,
    WChaMaxSf,
    WChaDisChaGraSf,
    VaChaMaxSf,
    MinRsvPctSf,
    ChaStateSf,
    StorAvalSf,
    InBatVSf,
    InOutWRteSf,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 124. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model124;

impl<'ad> ModelSpec<'ad> for Model124 {
    const MODEL_ID: u16 = 124;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        26
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model124,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(124);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::WChaMax => {
            buffer.write_u16(adapter.w_cha_max());
        }
        Point::WChaGra => {
            buffer.write_u16(adapter.w_cha_gra());
        }
        Point::WDisChaGra => {
            buffer.write_u16(adapter.w_dis_cha_gra());
        }
        Point::StorCtlMod => {
            buffer.write_u16(adapter.stor_ctl_mod());
        }
        Point::VaChaMax => {
            if let Some(value) = adapter.va_cha_max() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::MinRsvPct => {
            if let Some(value) = adapter.min_rsv_pct() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChaState => {
            if let Some(value) = adapter.cha_state() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StorAval => {
            if let Some(value) = adapter.stor_aval() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InBatV => {
            if let Some(value) = adapter.in_bat_v() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChaSt => {
            if let Some(value) = adapter.cha_st() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::OutWRte => {
            if let Some(value) = adapter.out_w_rte() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InWRte => {
            if let Some(value) = adapter.in_w_rte() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InOutWRteWinTms => {
            if let Some(value) = adapter.in_out_w_rte_win_tms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InOutWRteRvrtTms => {
            if let Some(value) = adapter.in_out_w_rte_rvrt_tms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InOutWRteRmpTms => {
            if let Some(value) = adapter.in_out_w_rte_rmp_tms() {
                buffer.write_u16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChaGriSet => {
            if let Some(value) = adapter.cha_gri_set() {
                buffer.write_u16(value as u16);
            } else {
                buffer.zero();
            }
        }
        Point::WChaMaxSf => {
            buffer.write_i16(adapter.w_cha_max_sf());
        }
        Point::WChaDisChaGraSf => {
            buffer.write_i16(adapter.w_cha_dis_cha_gra_sf());
        }
        Point::VaChaMaxSf => {
            if let Some(value) = adapter.va_cha_max_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::MinRsvPctSf => {
            if let Some(value) = adapter.min_rsv_pct_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::ChaStateSf => {
            if let Some(value) = adapter.cha_state_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::StorAvalSf => {
            if let Some(value) = adapter.stor_aval_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InBatVSf => {
            if let Some(value) = adapter.in_bat_v_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
        Point::InOutWRteSf => {
            if let Some(value) = adapter.in_out_w_rte_sf() {
                buffer.write_i16(value);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model124,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::WChaMax => {
            let value = buffer.read_u16()?;
            adapter.set_w_cha_max(value);
            Ok(())
        }
        Point::WChaGra => {
            let value = buffer.read_u16()?;
            adapter.set_w_cha_gra(value);
            Ok(())
        }
        Point::WDisChaGra => {
            let value = buffer.read_u16()?;
            adapter.set_w_dis_cha_gra(value);
            Ok(())
        }
        Point::StorCtlMod => {
            let value = buffer.read_u16()?;
            adapter.set_stor_ctl_mod(value);
            Ok(())
        }
        Point::VaChaMax => {
            let value = buffer.read_u16()?;
            adapter.set_va_cha_max(value);
            Ok(())
        }
        Point::MinRsvPct => {
            let value = buffer.read_u16()?;
            adapter.set_min_rsv_pct(value);
            Ok(())
        }
        Point::OutWRte => {
            let value = buffer.read_i16()?;
            adapter.set_out_w_rte(value);
            Ok(())
        }
        Point::InWRte => {
            let value = buffer.read_i16()?;
            adapter.set_in_w_rte(value);
            Ok(())
        }
        Point::InOutWRteWinTms => {
            let value = buffer.read_u16()?;
            adapter.set_in_out_w_rte_win_tms(value);
            Ok(())
        }
        Point::InOutWRteRvrtTms => {
            let value = buffer.read_u16()?;
            adapter.set_in_out_w_rte_rvrt_tms(value);
            Ok(())
        }
        Point::InOutWRteRmpTms => {
            let value = buffer.read_u16()?;
            adapter.set_in_out_w_rte_rmp_tms(value);
            Ok(())
        }
        Point::ChaGriSet => {
            let value = buffer.read_u16()?;
            let enum_value =
                ChaGriSet::from_repr(value).ok_or(ModbusException::IllegalDataValue)?;
            adapter.set_cha_gri_set(enum_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// WChaMax (WChaMax)
    ///
    /// Setpoint for maximum charge.
    fn w_cha_max(&self) -> u16;

    /// WChaGra (WChaGra)
    ///
    /// Setpoint for maximum charging rate. Default is MaxChaRte.
    fn w_cha_gra(&self) -> u16;

    /// WDisChaGra (WDisChaGra)
    ///
    /// Setpoint for maximum discharge rate. Default is MaxDisChaRte.
    fn w_dis_cha_gra(&self) -> u16;

    /// StorCtl_Mod (StorCtl_Mod)
    ///
    /// Activate hold/discharge/charge storage control mode.
    fn stor_ctl_mod(&self) -> u16;

    /// VAChaMax (VAChaMax)
    ///
    /// Setpoint for maximum charging VA.
    fn va_cha_max(&self) -> Option<u16> {
        None
    }

    /// MinRsvPct (MinRsvPct)
    ///
    /// Setpoint for minimum reserve for storage as a percentage of the nominal maximum storage.
    fn min_rsv_pct(&self) -> Option<u16> {
        None
    }

    /// ChaState (ChaState)
    ///
    /// Currently available energy as a percent of the capacity rating.
    fn cha_state(&self) -> Option<u16> {
        None
    }

    /// StorAval (StorAval)
    ///
    /// State of charge (ChaState) minus storage reserve (MinRsvPct) times capacity rating (AhrRtg).
    fn stor_aval(&self) -> Option<u16> {
        None
    }

    /// InBatV (InBatV)
    ///
    /// Internal battery voltage.
    fn in_bat_v(&self) -> Option<u16> {
        None
    }

    /// ChaSt (ChaSt)
    ///
    /// Charge status of storage device.
    fn cha_st(&self) -> Option<ChaSt> {
        None
    }

    /// OutWRte (OutWRte)
    ///
    /// Percent of max discharge rate.
    fn out_w_rte(&self) -> Option<i16> {
        None
    }

    /// InWRte (InWRte)
    ///
    /// Percent of max charging rate.
    fn in_w_rte(&self) -> Option<i16> {
        None
    }

    /// InOutWRte_WinTms (InOutWRte_WinTms)
    ///
    /// Time window for charge/discharge rate change.
    fn in_out_w_rte_win_tms(&self) -> Option<u16> {
        None
    }

    /// InOutWRte_RvrtTms (InOutWRte_RvrtTms)
    ///
    /// Timeout period for charge/discharge rate.
    fn in_out_w_rte_rvrt_tms(&self) -> Option<u16> {
        None
    }

    /// InOutWRte_RmpTms (InOutWRte_RmpTms)
    ///
    /// Ramp time for moving from current setpoint to new setpoint.
    fn in_out_w_rte_rmp_tms(&self) -> Option<u16> {
        None
    }

    /// ChaGriSet
    fn cha_gri_set(&self) -> Option<ChaGriSet> {
        None
    }

    /// WChaMax_SF (WChaMax_SF)
    ///
    /// Scale factor for maximum charge.
    fn w_cha_max_sf(&self) -> i16;

    /// WChaDisChaGra_SF (WChaDisChaGra_SF)
    ///
    /// Scale factor for maximum charge and discharge rate.
    fn w_cha_dis_cha_gra_sf(&self) -> i16;

    /// VAChaMax_SF (VAChaMax_SF)
    ///
    /// Scale factor for maximum charging VA.
    fn va_cha_max_sf(&self) -> Option<i16> {
        None
    }

    /// MinRsvPct_SF (MinRsvPct_SF)
    ///
    /// Scale factor for minimum reserve percentage.
    fn min_rsv_pct_sf(&self) -> Option<i16> {
        None
    }

    /// ChaState_SF (ChaState_SF)
    ///
    /// Scale factor for available energy percent.
    fn cha_state_sf(&self) -> Option<i16> {
        None
    }

    /// StorAval_SF (StorAval_SF)
    ///
    /// Scale factor for state of charge.
    fn stor_aval_sf(&self) -> Option<i16> {
        None
    }

    /// InBatV_SF (InBatV_SF)
    ///
    /// Scale factor for battery voltage.
    fn in_bat_v_sf(&self) -> Option<i16> {
        None
    }

    /// InOutWRte_SF (InOutWRte_SF)
    ///
    /// Scale factor for percent charge/discharge rate.
    fn in_out_w_rte_sf(&self) -> Option<i16> {
        None
    }
}

pub trait WriteAdapter {
    /// WChaMax (WChaMax)
    ///
    /// Setpoint for maximum charge.
    fn set_w_cha_max(&mut self, value: u16);

    /// WChaGra (WChaGra)
    ///
    /// Setpoint for maximum charging rate. Default is MaxChaRte.
    fn set_w_cha_gra(&mut self, value: u16);

    /// WDisChaGra (WDisChaGra)
    ///
    /// Setpoint for maximum discharge rate. Default is MaxDisChaRte.
    fn set_w_dis_cha_gra(&mut self, value: u16);

    /// StorCtl_Mod (StorCtl_Mod)
    ///
    /// Activate hold/discharge/charge storage control mode.
    fn set_stor_ctl_mod(&mut self, value: u16);

    /// VAChaMax (VAChaMax)
    ///
    /// Setpoint for maximum charging VA.
    fn set_va_cha_max(&mut self, value: u16) {}

    /// MinRsvPct (MinRsvPct)
    ///
    /// Setpoint for minimum reserve for storage as a percentage of the nominal maximum storage.
    fn set_min_rsv_pct(&mut self, value: u16) {}

    /// OutWRte (OutWRte)
    ///
    /// Percent of max discharge rate.
    fn set_out_w_rte(&mut self, value: i16) {}

    /// InWRte (InWRte)
    ///
    /// Percent of max charging rate.
    fn set_in_w_rte(&mut self, value: i16) {}

    /// InOutWRte_WinTms (InOutWRte_WinTms)
    ///
    /// Time window for charge/discharge rate change.
    fn set_in_out_w_rte_win_tms(&mut self, value: u16) {}

    /// InOutWRte_RvrtTms (InOutWRte_RvrtTms)
    ///
    /// Timeout period for charge/discharge rate.
    fn set_in_out_w_rte_rvrt_tms(&mut self, value: u16) {}

    /// InOutWRte_RmpTms (InOutWRte_RmpTms)
    ///
    /// Ramp time for moving from current setpoint to new setpoint.
    fn set_in_out_w_rte_rmp_tms(&mut self, value: u16) {}

    /// ChaGriSet
    fn set_cha_gri_set(&mut self, value: ChaGriSet) {}
}

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model124ChaGriSet {
    Pv = 0,
    Grid = 1,
}

impl Model124ChaGriSet {
    pub fn from_repr(repr: u16) -> Option<Model124ChaGriSet> {
        match repr {
            0 => Some(Model124ChaGriSet::Pv),
            1 => Some(Model124ChaGriSet::Grid),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model124ChaGriSet`] - only `Model124ChaGriSet` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `ChaGriSet` distinct from another model's point of the same name.
pub type ChaGriSet = Model124ChaGriSet;

#[derive(Clone, Copy, Debug, PartialEq)]
#[repr(u16)]
pub enum Model124ChaSt {
    Off = 1,
    Empty = 2,
    Discharging = 3,
    Charging = 4,
    Full = 5,
    Holding = 6,
    Testing = 7,
}

impl Model124ChaSt {
    pub fn from_repr(repr: u16) -> Option<Model124ChaSt> {
        match repr {
            1 => Some(Model124ChaSt::Off),
            2 => Some(Model124ChaSt::Empty),
            3 => Some(Model124ChaSt::Discharging),
            4 => Some(Model124ChaSt::Charging),
            5 => Some(Model124ChaSt::Full),
            6 => Some(Model124ChaSt::Holding),
            7 => Some(Model124ChaSt::Testing),
            _ => None,
        }
    }
}

/// Short, spec-matching alias for [`Model124ChaSt`] - only `Model124ChaSt` (unique per model) reaches the generated C header, since cbindgen has no module system to keep `ChaSt` distinct from another model's point of the same name.
pub type ChaSt = Model124ChaSt;
