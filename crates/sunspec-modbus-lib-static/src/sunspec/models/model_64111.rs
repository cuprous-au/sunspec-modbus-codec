// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::SunspecAdapter;
use core::ffi::c_void;
use sunspec_modbus_lib_rs::ModbusException;
use sunspec_modbus_lib_rs::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use sunspec_modbus_lib_rs::cursor::Cursor;
use sunspec_modbus_lib_rs::model::{ModelSpec, StaticModelSpec};
use sunspec_modbus_lib_rs::sunspec::models::model_64111::*;

#[repr(C)]
pub struct Model64111CallbackAdapter {
    context: *mut c_void,
    /// Port Number (Port)
    port_number_callback: extern "C" fn(*const c_void) -> u16,
    /// V_SF
    v_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// A_SF
    a_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// P_SF
    p_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// AH_SF
    ah_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// KWH_SF
    kwh_sf_callback: extern "C" fn(*const c_void) -> i16,
    /// Battery Voltage (BattV)
    battery_voltage_callback: extern "C" fn(*const c_void) -> u16,
    /// Array Voltage (ArrayV)
    array_voltage_callback: extern "C" fn(*const c_void) -> u16,
    /// Output Current (OutputA)
    output_current_callback: extern "C" fn(*const c_void) -> u16,
    /// Array Current (InputA)
    array_current_callback: extern "C" fn(*const c_void) -> u16,
    /// Operating State (ChargerSt)
    operating_state_callback: extern "C" fn(*const c_void) -> Model64111ChargerSt,
    /// Output Wattage (OutputW)
    output_wattage_callback: extern "C" fn(*const c_void) -> u16,
    /// Today's Minimum Battery Voltage (TodayMinBatV)
    today_s_minimum_battery_voltage_callback: extern "C" fn(*const c_void) -> u16,
    /// Today's Maximum Battery Voltage (TodayMaxBatV)
    today_s_maximum_battery_voltage_callback: extern "C" fn(*const c_void) -> u16,
    /// VOC (VOCV)
    voc_callback: extern "C" fn(*const c_void) -> u16,
    /// Today's Maximum VOC (TodayMaxVOC)
    today_s_maximum_voc_callback: extern "C" fn(*const c_void) -> u16,
    /// Today's kWh (TodaykWhOutput)
    today_s_k_wh_callback: extern "C" fn(*const c_void) -> u16,
    /// Today's AH (TodayAHOutput)
    today_s_ah_callback: extern "C" fn(*const c_void) -> u16,
    /// Lifetime kWh (LifeTimeKWHOut)
    lifetime_k_wh_callback: extern "C" fn(*const c_void) -> u16,
    /// Lifetime kAH (LifeTimeAHOut)
    lifetime_k_ah_callback: extern "C" fn(*const c_void) -> u16,
    /// Lifetime Maximum Output Wattage (LifeTimeMaxOut)
    lifetime_maximum_output_wattage_callback: extern "C" fn(*const c_void) -> u16,
    /// Lifetime Maximum Battery Voltage (LifeTimeMaxBatt)
    lifetime_maximum_battery_voltage_callback: extern "C" fn(*const c_void) -> u16,
    /// Lifetime Maximum VOC Voltage (LifeTimeMaxVOC)
    lifetime_maximum_voc_voltage_callback: extern "C" fn(*const c_void) -> u16,
}

impl ReadAdapter for Model64111CallbackAdapter {
    fn port_number(&self) -> u16 {
        (self.port_number_callback)(self.context)
    }

    fn v_sf(&self) -> i16 {
        (self.v_sf_callback)(self.context)
    }

    fn a_sf(&self) -> i16 {
        (self.a_sf_callback)(self.context)
    }

    fn p_sf(&self) -> i16 {
        (self.p_sf_callback)(self.context)
    }

    fn ah_sf(&self) -> i16 {
        (self.ah_sf_callback)(self.context)
    }

    fn kwh_sf(&self) -> i16 {
        (self.kwh_sf_callback)(self.context)
    }

    fn battery_voltage(&self) -> u16 {
        (self.battery_voltage_callback)(self.context)
    }

    fn array_voltage(&self) -> u16 {
        (self.array_voltage_callback)(self.context)
    }

    fn output_current(&self) -> u16 {
        (self.output_current_callback)(self.context)
    }

    fn array_current(&self) -> u16 {
        (self.array_current_callback)(self.context)
    }

    fn operating_state(&self) -> ChargerSt {
        (self.operating_state_callback)(self.context)
    }

    fn output_wattage(&self) -> u16 {
        (self.output_wattage_callback)(self.context)
    }

    fn today_s_minimum_battery_voltage(&self) -> u16 {
        (self.today_s_minimum_battery_voltage_callback)(self.context)
    }

    fn today_s_maximum_battery_voltage(&self) -> u16 {
        (self.today_s_maximum_battery_voltage_callback)(self.context)
    }

    fn voc(&self) -> u16 {
        (self.voc_callback)(self.context)
    }

    fn today_s_maximum_voc(&self) -> u16 {
        (self.today_s_maximum_voc_callback)(self.context)
    }

    fn today_s_k_wh(&self) -> u16 {
        (self.today_s_k_wh_callback)(self.context)
    }

    fn today_s_ah(&self) -> u16 {
        (self.today_s_ah_callback)(self.context)
    }

    fn lifetime_k_wh(&self) -> u16 {
        (self.lifetime_k_wh_callback)(self.context)
    }

    fn lifetime_k_ah(&self) -> u16 {
        (self.lifetime_k_ah_callback)(self.context)
    }

    fn lifetime_maximum_output_wattage(&self) -> u16 {
        (self.lifetime_maximum_output_wattage_callback)(self.context)
    }

    fn lifetime_maximum_battery_voltage(&self) -> u16 {
        (self.lifetime_maximum_battery_voltage_callback)(self.context)
    }

    fn lifetime_maximum_voc_voltage(&self) -> u16 {
        (self.lifetime_maximum_voc_voltage_callback)(self.context)
    }
}

/// C-FFI dispatch descriptor for SunSpec model 64111. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_64111: StaticModelSpec = StaticModelSpec {
    id: 64111,
    length: model_64111_c_length,
    writable: false,
    visit_read: model_64111_c_visit_read,
    visit_write: model_64111_c_visit_write,
};

fn model_64111_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model64111).model_length()
}

/// # Safety
/// `adapter` must be null or point to a live `Model64111CallbackAdapter`, valid for the
/// duration of the call.
unsafe fn model_64111_c_visit_read(
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model64111;
    // SAFETY: as required by this function's own contract.
    let adapter = unsafe { (adapter as *const Model64111CallbackAdapter).as_ref() };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_64111_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_64111_c_visit_write(
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let _ = (adapter, buffer);
    let model = Model64111;
    cursor.visit_source_block(model.model_length(), |_, _, _| {
        Err(ModbusException::IllegalDataAddress)
    });
}

/// Build a [`SunspecAdapter`] for model 64111 backed by a callback adapter.
#[unsafe(no_mangle)]
pub extern "C" fn sunspec_model_64111_callback(
    adapter: *mut Model64111CallbackAdapter,
) -> SunspecAdapter {
    SunspecAdapter {
        model_spec: &SUNSPEC_MODEL_64111,
        adapter: adapter as *mut c_void,
    }
}
