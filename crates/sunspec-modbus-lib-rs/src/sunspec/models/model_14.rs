// @generated by the sunspec-gen crate (crates/sunspec-gen) from the SunSpec model definitions in
// https://github.com/sunspec/models at tag v2026-04-20.
// Do not edit by hand - run `cargo run -p sunspec-gen` to regenerate.

use crate::ModbusException;
use crate::buffer::{ReadableRegisterBuffer, WritableRegisterBuffer};
use crate::cursor::Cursor;
use crate::model::{ModelSpec, StaticModelSpec};
use core::cmp::min;
use core::ffi::{CStr, c_char, c_void};

static POINTS: [PointDetails<()>; 10] = [
    PointDetails {
        point: |()| Point::ModelId,
        size: 1,
        start_address: 0,
    },
    PointDetails {
        point: |()| Point::ModelLength,
        size: 1,
        start_address: 1,
    },
    PointDetails {
        point: |()| Point::Name,
        size: 4,
        start_address: 2,
    },
    PointDetails {
        point: |()| Point::Capabilities,
        size: 1,
        start_address: 6,
    },
    PointDetails {
        point: |()| Point::Config,
        size: 1,
        start_address: 7,
    },
    PointDetails {
        point: |()| Point::Typ,
        size: 1,
        start_address: 8,
    },
    PointDetails {
        point: |()| Point::Address,
        size: 20,
        start_address: 9,
    },
    PointDetails {
        point: |()| Point::Port,
        size: 1,
        start_address: 29,
    },
    PointDetails {
        point: |()| Point::Username,
        size: 12,
        start_address: 30,
    },
    PointDetails {
        point: |()| Point::Password,
        size: 12,
        start_address: 42,
    },
];

#[derive(Debug)]
pub enum Point {
    ModelId,
    ModelLength,
    Name,
    Capabilities,
    Config,
    Typ,
    Address,
    Port,
    Username,
    Password,
}

struct PointDetails<GroupIndexArgs> {
    point: fn(GroupIndexArgs) -> Point,
    start_address: u16,
    size: u16,
}

/// Marker for SunSpec model 14. Add it to a [`Sunspec`](crate::Sunspec) model list.
pub struct Model14;

impl<'ad> ModelSpec<'ad> for Model14 {
    const MODEL_ID: u16 = 14;
    type ReadAdapter = dyn ReadAdapter + 'ad;
    type WriteAdapter = dyn WriteAdapter + 'ad;

    fn model_length(&self) -> u16 {
        54
    }

    fn traverse_points_read<'a>(
        &self,
        adapter: &Self::ReadAdapter,
        buffer: &mut WritableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            write_point_to_buffer(
                self,
                adapter,
                &point,
                &mut buffer.slice(cursor, word_count),
                point_offset,
            );
            cursor += word_count;
        }

        Ok(())
    }

    fn traverse_points_write<'a>(
        &self,
        adapter: &mut Self::WriteAdapter,
        buffer: &ReadableRegisterBuffer<'a>,
        offset: u16,
    ) -> Result<(), ModbusException> {
        let until = offset + buffer.len();
        let mut cursor = 0;

        let iter = POINTS
            .iter()
            .map(|p| (p.start_address, p.size, (p.point)(())))
            .skip_while(|(start, size, _)| offset >= start + size)
            .take_while(|(start, _, _)| until > *start);

        for (start, size, point) in iter {
            let point_offset = offset.saturating_sub(start);
            let word_count = min(size - point_offset, buffer.len() - cursor);
            if point_offset > 0 {
                return Err(ModbusException::IllegalDataAddress);
            }
            read_point_from_buffer(self, adapter, &point, &buffer.slice(cursor, word_count))?;
            cursor += word_count;
        }

        Ok(())
    }
}

pub(crate) fn write_point_to_buffer<'a>(
    model: &Model14,
    adapter: &dyn ReadAdapter,
    point: &Point,
    buffer: &mut WritableRegisterBuffer<'a>,
    offset: u16,
) {
    match point {
        Point::ModelId => {
            buffer.write_u16(14);
        }
        Point::ModelLength => {
            buffer.write_u16(model.model_length() - 2);
        }
        Point::Name => {
            if let Some(value) = adapter.name() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Capabilities => {
            buffer.write_u16(adapter.capabilities());
        }
        Point::Config => {
            buffer.write_u16(adapter.config());
        }
        Point::Typ => {
            buffer.write_u16(adapter.typ());
        }
        Point::Address => {
            buffer.write_string(adapter.address(), offset);
        }
        Point::Port => {
            buffer.write_u16(adapter.port());
        }
        Point::Username => {
            if let Some(value) = adapter.username() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
        Point::Password => {
            if let Some(value) = adapter.password() {
                buffer.write_string(value, offset);
            } else {
                buffer.zero();
            }
        }
    }
}

pub(crate) fn read_point_from_buffer<'a>(
    model: &Model14,
    adapter: &mut dyn WriteAdapter,
    point: &Point,
    buffer: &ReadableRegisterBuffer<'a>,
) -> Result<(), ModbusException> {
    match point {
        Point::Name => {
            let value = buffer.read_string::<8>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_name(str_value);
            Ok(())
        }
        Point::Capabilities => {
            let value = buffer.read_u16()?;
            adapter.set_capabilities(value);
            Ok(())
        }
        Point::Config => {
            let value = buffer.read_u16()?;
            adapter.set_config(value);
            Ok(())
        }
        Point::Typ => {
            let value = buffer.read_u16()?;
            adapter.set_typ(value);
            Ok(())
        }
        Point::Address => {
            let value = buffer.read_string::<40>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_address(str_value);
            Ok(())
        }
        Point::Port => {
            let value = buffer.read_u16()?;
            adapter.set_port(value);
            Ok(())
        }
        Point::Username => {
            let value = buffer.read_string::<24>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_username(str_value);
            Ok(())
        }
        Point::Password => {
            let value = buffer.read_string::<24>()?;
            let str_value =
                CStr::from_bytes_with_nul(&value).map_err(|_| ModbusException::IllegalDataValue)?;
            adapter.set_password(str_value);
            Ok(())
        }
        _ => Err(ModbusException::IllegalDataAddress),
    }
}

pub trait ReadAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn name(&self) -> Option<&CStr> {
        None
    }

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn capabilities(&self) -> u16;

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn config(&self) -> u16;

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn typ(&self) -> u16;

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn address(&self) -> &CStr;

    /// Port (Port)
    ///
    /// Proxy port number
    fn port(&self) -> u16;

    /// Username (User)
    ///
    /// Proxy user name
    fn username(&self) -> Option<&CStr> {
        None
    }

    /// Password (Pw)
    ///
    /// Proxy password
    fn password(&self) -> Option<&CStr> {
        None
    }
}

pub trait WriteAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn set_name(&mut self, value: &CStr) {}

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn set_capabilities(&mut self, value: u16);

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn set_config(&mut self, value: u16);

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn set_typ(&mut self, value: u16);

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn set_address(&mut self, value: &CStr);

    /// Port (Port)
    ///
    /// Proxy port number
    fn set_port(&mut self, value: u16);

    /// Username (User)
    ///
    /// Proxy user name
    fn set_username(&mut self, value: &CStr) {}

    /// Password (Pw)
    ///
    /// Proxy password
    fn set_password(&mut self, value: &CStr) {}
}

#[repr(C)]
pub struct Model14CallbackAdapter {
    context: *mut c_void,
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    name_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    set_name_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    capabilities_callback: extern "C" fn(*const c_void) -> u16,
    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    set_capabilities_callback: extern "C" fn(u16, *mut c_void),
    /// Config (Cfg)
    ///
    /// Set proxy address type
    config_callback: extern "C" fn(*const c_void) -> u16,
    /// Config (Cfg)
    ///
    /// Set proxy address type
    set_config_callback: extern "C" fn(u16, *mut c_void),
    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    typ_callback: extern "C" fn(*const c_void) -> u16,
    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    set_typ_callback: extern "C" fn(u16, *mut c_void),
    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    address_callback: extern "C" fn(*const c_void) -> *const c_char,
    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    set_address_callback: extern "C" fn(*const c_char, *mut c_void),
    /// Port (Port)
    ///
    /// Proxy port number
    port_callback: extern "C" fn(*const c_void) -> u16,
    /// Port (Port)
    ///
    /// Proxy port number
    set_port_callback: extern "C" fn(u16, *mut c_void),
    /// Username (User)
    ///
    /// Proxy user name
    username_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Username (User)
    ///
    /// Proxy user name
    set_username_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
    /// Password (Pw)
    ///
    /// Proxy password
    password_callback: Option<extern "C" fn(*const c_void) -> *const c_char>,
    /// Password (Pw)
    ///
    /// Proxy password
    set_password_callback: Option<extern "C" fn(*const c_char, *mut c_void)>,
}

impl ReadAdapter for Model14CallbackAdapter {
    fn name(&self) -> Option<&CStr> {
        self.name_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn capabilities(&self) -> u16 {
        (self.capabilities_callback)(self.context)
    }

    fn config(&self) -> u16 {
        (self.config_callback)(self.context)
    }

    fn typ(&self) -> u16 {
        (self.typ_callback)(self.context)
    }

    fn address(&self) -> &CStr {
        unsafe { CStr::from_ptr((self.address_callback)(self.context)) }
    }

    fn port(&self) -> u16 {
        (self.port_callback)(self.context)
    }

    fn username(&self) -> Option<&CStr> {
        self.username_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }

    fn password(&self) -> Option<&CStr> {
        self.password_callback
            .map(|callback| unsafe { CStr::from_ptr((callback)(self.context)) })
    }
}

impl WriteAdapter for Model14CallbackAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn set_name(&mut self, value: &CStr) {
        if let Some(callback) = self.set_name_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn set_capabilities(&mut self, value: u16) {
        (self.set_capabilities_callback)(value, self.context);
    }

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn set_config(&mut self, value: u16) {
        (self.set_config_callback)(value, self.context);
    }

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn set_typ(&mut self, value: u16) {
        (self.set_typ_callback)(value, self.context);
    }

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn set_address(&mut self, value: &CStr) {
        (self.set_address_callback)(value.as_ptr(), self.context);
    }

    /// Port (Port)
    ///
    /// Proxy port number
    fn set_port(&mut self, value: u16) {
        (self.set_port_callback)(value, self.context);
    }

    /// Username (User)
    ///
    /// Proxy user name
    fn set_username(&mut self, value: &CStr) {
        if let Some(callback) = self.set_username_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }

    /// Password (Pw)
    ///
    /// Proxy password
    fn set_password(&mut self, value: &CStr) {
        if let Some(callback) = self.set_password_callback {
            (callback)(value.as_ptr(), self.context);
        };
    }
}

#[repr(C)]
pub struct Model14StatefulAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    pub name: [c_char; 8],
    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    pub capabilities: u16,
    /// Config (Cfg)
    ///
    /// Set proxy address type
    pub config: u16,
    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    pub typ: u16,
    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    pub address: [c_char; 40],
    /// Port (Port)
    ///
    /// Proxy port number
    pub port: u16,
    /// Username (User)
    ///
    /// Proxy user name
    pub username: [c_char; 24],
    /// Password (Pw)
    ///
    /// Proxy password
    pub password: [c_char; 24],
}

impl ReadAdapter for Model14StatefulAdapter {
    fn name(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.name.as_ptr()) })
    }

    fn capabilities(&self) -> u16 {
        self.capabilities
    }

    fn config(&self) -> u16 {
        self.config
    }

    fn typ(&self) -> u16 {
        self.typ
    }

    fn address(&self) -> &CStr {
        unsafe { CStr::from_ptr(self.address.as_ptr()) }
    }

    fn port(&self) -> u16 {
        self.port
    }

    fn username(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.username.as_ptr()) })
    }

    fn password(&self) -> Option<&CStr> {
        Some(unsafe { CStr::from_ptr(self.password.as_ptr()) })
    }
}

impl WriteAdapter for Model14StatefulAdapter {
    /// name (Nam)
    ///
    /// Interface name (8 chars)
    fn set_name(&mut self, value: &CStr) {
        for (dest, src) in self.name.iter_mut().zip(value.to_bytes_with_nul().iter()) {
            *dest = *src as c_char;
        }
    }

    /// Capabilities (Cap)
    ///
    /// Proxy configuration capabilities
    fn set_capabilities(&mut self, value: u16) {
        self.capabilities = value;
    }

    /// Config (Cfg)
    ///
    /// Set proxy address type
    fn set_config(&mut self, value: u16) {
        self.config = value;
    }

    /// Type (Typ)
    ///
    /// Enumerate value. Proxy server type
    fn set_typ(&mut self, value: u16) {
        self.typ = value;
    }

    /// Address (Addr)
    ///
    /// IPv4 or IPv6 proxy hostname or dotted address (40 chars)
    fn set_address(&mut self, value: &CStr) {
        for (dest, src) in self
            .address
            .iter_mut()
            .zip(value.to_bytes_with_nul().iter())
        {
            *dest = *src as c_char;
        }
    }

    /// Port (Port)
    ///
    /// Proxy port number
    fn set_port(&mut self, value: u16) {
        self.port = value;
    }

    /// Username (User)
    ///
    /// Proxy user name
    fn set_username(&mut self, value: &CStr) {
        for (dest, src) in self
            .username
            .iter_mut()
            .zip(value.to_bytes_with_nul().iter())
        {
            *dest = *src as c_char;
        }
    }

    /// Password (Pw)
    ///
    /// Proxy password
    fn set_password(&mut self, value: &CStr) {
        for (dest, src) in self
            .password
            .iter_mut()
            .zip(value.to_bytes_with_nul().iter())
        {
            *dest = *src as c_char;
        }
    }
}

/// C-FFI dispatch descriptor for SunSpec model 14. A C `SunspecModelBinding` points
/// at this static, so the service functions dispatch with no model-id lookup.
#[unsafe(no_mangle)]
pub static SUNSPEC_MODEL_14: StaticModelSpec = StaticModelSpec {
    id: 14,
    length: model_14_c_length,
    writable: true,
    visit_read: model_14_c_visit_read,
    visit_write: model_14_c_visit_write,
};

fn model_14_c_length(_repeat_count_0: u16, _repeat_count_1: u16) -> u16 {
    (Model14).model_length()
}

/// # Safety
/// For `kind` 1 or 2, `adapter` must point to a live `Model14{Stateful,Callback}Adapter`,
/// valid for the duration of the call.
unsafe fn model_14_c_visit_read(
    kind: u8,
    adapter: *const c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &mut WritableRegisterBuffer<'_>,
) {
    let model = Model14;
    let adapter: Option<&dyn ReadAdapter> = match kind {
        1 => Some(unsafe { &*(adapter as *const Model14StatefulAdapter) } as &dyn ReadAdapter),
        2 => Some(unsafe { &*(adapter as *const Model14CallbackAdapter) } as &dyn ReadAdapter),
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| {
        let mut block = buffer.slice(from, len);
        match adapter {
            Some(adapter) => model.traverse_points_read(adapter, &mut block, offset),
            None => {
                block.fill(&[0xff, 0xff]);
                Ok(())
            }
        }
    });
}

/// # Safety
/// As for [`model_14_c_visit_read`], and `adapter` must be uniquely borrowable for the call.
unsafe fn model_14_c_visit_write(
    kind: u8,
    adapter: *mut c_void,
    _repeat_count_0: u16,
    _repeat_count_1: u16,
    cursor: &mut Cursor<ModbusException>,
    buffer: &ReadableRegisterBuffer<'_>,
) {
    let model = Model14;
    let adapter: Option<&mut dyn WriteAdapter> = match kind {
        1 => {
            Some(unsafe { &mut *(adapter as *mut Model14StatefulAdapter) } as &mut dyn WriteAdapter)
        }
        2 => {
            Some(unsafe { &mut *(adapter as *mut Model14CallbackAdapter) } as &mut dyn WriteAdapter)
        }
        _ => None,
    };
    cursor.visit_source_block(model.model_length(), |offset, from, len| match adapter {
        Some(adapter) => model.traverse_points_write(adapter, &buffer.slice(from, len), offset),
        None => Err(ModbusException::IllegalDataAddress),
    });
}
